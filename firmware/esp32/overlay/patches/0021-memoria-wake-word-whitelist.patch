diff --git a/main/CMakeLists.txt b/main/CMakeLists.txt
--- a/main/CMakeLists.txt
+++ b/main/CMakeLists.txt
@@ -35,6 +35,7 @@ set(SOURCES "audio/audio_codec.cc"
             "memoria/device_identity.cc"
             "memoria/memoria_audio_frame.cc"
             "memoria/memoria_activation_client.cc"
+            "memoria/memoria_wake_word.cc"
             "memoria/memoria_protocol.cc"
             "memoria/memoria_bootstrap.cc"
             "mcp_server.cc"
diff --git a/main/application.cc b/main/application.cc
--- a/main/application.cc
+++ b/main/application.cc
@@ -6,6 +6,7 @@
 #include "display.h"
 #include "mcp_server.h"
 #include "mqtt_protocol.h"
+#include "memoria/memoria_wake_word.h"
 #include "memoria_protocol.h"
 #include "memoria_bootstrap.h"
 #include "display/lcd_display.h"
@@ -618,6 +619,7 @@ void Application::ActivationTask() {
     if (!assets.partition_valid() || !assets.Apply()) {
         ESP_LOGE(TAG, "Memoria assets partition/model load failed");
         return;
     }
+    memoria::WakeWordRegistry::GetInstance().LoadFromNvs();
     if (!InitializeProtocol()) {
         return;
     }
diff --git a/main/audio/wake_words/custom_wake_word.cc b/main/audio/wake_words/custom_wake_word.cc
--- a/main/audio/wake_words/custom_wake_word.cc
+++ b/main/audio/wake_words/custom_wake_word.cc
@@ -2,6 +2,7 @@
 #include "audio_service.h"
 #include "system_info.h"
 #include "assets.h"
+#include "memoria/memoria_wake_word.h"
 
 #include <esp_log.h>
 #include <esp_mn_iface.h>
@@ -99,6 +100,18 @@ bool CustomWakeWord::Initialize(AudioCodec* codec, srmodel_list_t* models_list)
         ParseWakenetModelConfig();
     }
 
+#if CONFIG_BOARD_TYPE_MEMORIA_ATK_DNESP32S3_V1
+    std::string active_command;
+    std::string active_display;
+    if (memoria::WakeWordRegistry::GetInstance().ApplyActiveCommand(
+            &active_command, &active_display)) {
+        commands_.clear();
+        commands_.push_back({active_command, active_display, "wake"});
+        ESP_LOGI(TAG, "Using Memoria wake word command=%s display=%s",
+                 active_command.c_str(), active_display.c_str());
+    }
+#endif
+
     if (models_ == nullptr || models_->num == -1) {
         ESP_LOGE(TAG, "Failed to initialize wakenet model");
         return false;
diff --git a/scripts/build_default_assets.py b/scripts/build_default_assets.py
--- a/scripts/build_default_assets.py
+++ b/scripts/build_default_assets.py
@@ -618,6 +618,28 @@ def read_custom_wake_word_from_sdkconfig(sdkconfig_path):
     return None
 
 
+def is_memoria_board_sdkconfig(sdkconfig_path):
+    if not os.path.exists(sdkconfig_path):
+        return False
+    with io.open(sdkconfig_path, "r", encoding="utf-8") as handle:
+        return "CONFIG_BOARD_TYPE_MEMORIA_ATK_DNESP32S3_V1=y" in handle.read()
+
+
+def read_memoria_wake_word_commands():
+    return [
+        {
+            "command": "mo li",
+            "text": "茉莉",
+            "action": "wake",
+        },
+        {
+            "command": "mei mo li ya",
+            "text": "梅莫里亚",
+            "action": "wake",
+        },
+    ]
+
+
 def get_language_from_multinet_models(multinet_models):
     """
     Determine language from multinet model names
@@ -896,17 +918,18 @@ def main():
         language = get_language_from_multinet_models(multinet_model_names)
         
         # Build multinet_model info structure
+        commands = read_memoria_wake_word_commands() if is_memoria_board_sdkconfig(args.sdkconfig) else [
+            {
+                "command": custom_wake_word_config['wake_word'],
+                "text": custom_wake_word_config['display'],
+                "action": "wake"
+            }
+        ]
         multinet_model_info = {
             "language": language,
             "duration": 3000,  # Default duration in ms
             "threshold": custom_wake_word_config['threshold'],
-            "commands": [
-                {
-                    "command": custom_wake_word_config['wake_word'],
-                    "text": custom_wake_word_config['display'],
-                    "action": "wake"
-                }
-            ]
+            "commands": commands
         }
         print(f"  custom wake word: {custom_wake_word_config['wake_word']} ({custom_wake_word_config['display']})")
         print(f"  wake word language: {language}")
