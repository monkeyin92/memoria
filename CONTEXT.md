# Memoria 领域语言

Memoria 将实时语音对话沉淀为可追溯、可纠错、可撤销的个人长期档案，并在明确授权下逐步形成个人化的表达与声音。这里统一产品与领域语言，避免把说话人、人物、记忆和声音混为一谈。

## 身份与参与者

**账号身份（Account Identity）**：
由 Control API 签发并以稳定 `user_id` 表示的数据归属身份；注册凭据、会话令牌、记忆、声纹、人格和声音档案都绑定到它。账号登录只证明当前客户端持有账号凭据，不证明麦克风前的说话人就是账户主人。
_避免使用_：说话人身份、声纹身份、人物实体

**账户主人（Account Owner）**：
拥有 Memoria 账户、私人档案及其授权控制权的人。
_避免使用_：主人用户、本人账号、主说话人

**说话人身份（Speaker Identity）**：
某段语音在现实世界中的发声者身份；可以是账户主人、已知访客、未知访客或尚不能确定的人。
_避免使用_：用户、声纹、人物

**人物实体（Person Entity）**：
档案中被谈及的现实人物，不代表此人正在说话。
_避免使用_：说话人、联系人、用户

**访客（Guest）**：
与 Memoria 对话但不拥有账户私人档案的人；其会话默认与账户主人的长期记忆和人格学习隔离。
_避免使用_：非主人、陌生人、未授权用户

**说话人判定（Speaker Classification）**：
对当前话轮给出的 `owner / guest / uncertain` 概率性结论，而不是法律身份认证。
_避免使用_：登录、实名认证、绝对声纹认证

## 证据与记忆

**证据事件（Evidence Event）**：
一次不可变的、带来源和时间的观察记录，例如最终转写、实际已听助手文本、用户纠错或授权变更。
_避免使用_：聊天记录、记忆、事实

**权威转写（Canonical Transcript）**：
由实时转写、档案级复核和用户纠错共同形成的当前有效文本版本，旧版本仍保留来源关系。
_避免使用_：原始转写、最终真相

**记忆声明（Memory Claim）**：
从证据中提取、可被确认、质疑或撤销的一条事实性或观点性陈述。
_避免使用_：永久事实、画像字段、模型结论

**人生情节（Life Episode）**：
围绕一段时间、地点、人物和主题组织的一组相关事件与声明。
_避免使用_：每日总结、聊天主题、单条消息

**经验知识（Knowledge Artifact）**：
由账户主人经历沉淀的可复用方法、问答、原则或故事，并保留其证据来源和适用条件。
_避免使用_：百科知识、模型常识、摘要

**记忆状态（Memory Status）**：
记忆声明的生命周期状态：`candidate / confirmed / disputed / retracted`。
_避免使用_：真假标签、删除标记

**档案投影（Archive Projection）**：
从证据事件和记忆声明生成、可删除后重建的时间线、人物、关系、知识和检索视图。
_避免使用_：原始数据、唯一真相、主库

## 人格与声音

**人格特征（Persona Trait）**：
带证据、适用场景、置信度和版本的表达偏好、价值排序或决策习惯。
_避免使用_：性格标签、永久人设、系统提示词

**人格胶囊（Persona Capsule）**：
为一次具体对话按权限、主题和上下文预算组装的短期人格与记忆视图。
_避免使用_：完整人格、用户画像、全量记忆

**声纹档案（Speaker Profile）**：
用于判定谁在说话的生物特征模板及其登记证据、阈值和版本。
_避免使用_：声音克隆、音色、人格声音

**声音档案（Voice Profile）**：
用于合成授权声音的供应商音色标识、样本授权、版本和撤销状态。
_避免使用_：声纹、说话人身份、TTS 配置项

**表达计划（Delivery Plan）**：
只对当前回答生效的语速、停顿、措辞和情绪表达策略，不直接升级为长期人格特征。
_避免使用_：人格学习、声音档案、永久风格

**声音登记操作（Voice Enrollment Operation）**：
一次可恢复、可对账的供应商声音登记过程；保存稳定 enrollment key、供应商结果和 reconciliation 状态，避免网络结果不明确时重复创建声音。
_避免使用_：声音档案、一次 HTTP 请求、自动重试任务

**声音盲测（Voice Blind Trial）**：
由服务端隐藏候选映射的同文 A/B 试听；账户主人只看到槽位并提交主观偏好，不能看到哪个槽位是复刻候选。
_避免使用_：普通试听、客户端随机、客观质量测试

**声音质量探针（Voice Quality Measurement）**：
由可信服务端运行记录的首包、取消尾音和时间戳误差证据，与账户主人的主观盲测分开，二者都通过后声音档案才可激活。
_避免使用_：用户评分、浏览器自报、供应商宣传指标

## 授权与治理

**授权凭据（Consent Grant）**：
账户主人对某类数据采集、长期保存、人格学习或声音复刻作出的可撤销、带版本授权。
_避免使用_：一次确认、隐私设置、默认同意

**敏感动作授权（Action Authority）**：
综合账户会话、设备状态、说话人判定和二次确认决定某项动作是否可执行的结论。
_避免使用_：仅凭声纹认证、主人模式

**能力令牌（Capability Token）**：
服务间只授权单一内部能力的凭据；当前分为档案写入、记忆读取、人格读取和声音解析，不能作为账户身份或浏览器凭据。
_避免使用_：万能内部 token、用户 Bearer、内网可信

**删除墓碑（Deletion Tombstone）**：
账户或会话完成删除后保留的最小不可逆标记，用于拒绝旧令牌、迟到事件和旧 session 复活数据；不保存被删除的正文、生物特征或凭据。
_避免使用_：软删除数据、完整审计副本、可恢复备份
