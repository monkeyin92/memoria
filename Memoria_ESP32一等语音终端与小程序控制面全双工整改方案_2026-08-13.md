# Memoria ESP32 一等语音终端与小程序控制面整改方案

> 目标：将微信小程序彻底收敛为**设备初始化、账号绑定、Soul/Persona/个人信息配置、设备管理、对话记录与总结查询平台**；将 ESP32-S3 机器人确立为**唯一面向用户的实时语音终端**；实时语音链路直接朝“可打断、接近全双工”方向建设。

- 文档版本：v1.0
- 编制日期：2026-08-13
- 初始代码审查基线：`monkeyin92/memoria` 公共仓库 `main` 分支（仅指 2026-08-13 初始审查时点，不代表当前生产状态）
- 适用对象：产品经理、架构师、后端研发、Go 媒体研发、Python Voice Core/Agent 研发、ESP32 固件研发、微信小程序研发、测试、运维、安全
- 决策级别：目标架构与整改实施基线
- 前置说明：2026-08-13 初始审查时，仓库 `HANDOFF.md` 的最后状态日期为 2026-08-12；后续状态以本执行台账、当前 `HANDOFF.md` 与 `architecture-status.yaml` 的同步证据为准。本文始终把“源码存在”“生产接线”“功能启用”“真机验证”分别陈述，不用一个“已完成”覆盖四种状态。

## 执行台账（持续更新）

状态口径：`[x]` 只表示该条中明确写出的层级已经完成；`code / wired / enabled / verified` 不互相替代。正式 T1–T14、AEC 双讲、500 轮稳定性和微信双端验收仍按第 16–20 节独立判定。

### 2026-08-21 六项实时语音与供应链建议落地判断（当前基线）

本节将“回复投递不可静默丢失、先完成真实声音闭环、TTS 首帧基准、自然度受控实验、近端语音环与远端重推理、构建供应链门禁”纳入本方案的持续验收基线。判断严格拆分 `code / wired / enabled / verified`；其中 `production runtime verified` 只表示服务器运行态，不替代真实设备声音、DAC、Actual Heard、AEC、双讲或 T1–T14 证据。

| 建议 | code | wired | enabled | verified | 2026-08-21 当前证据与缺口 |
|---|---|---|---|---|---|
| P0 回复投递不可静默丢失 | mostly complete | complete | production canary | runtime only | 已新增进程内 session-scoped `ReplyDeliveryLedger`，并把 `session_epoch` 沿 media-v1 `GenerationControl/AssistantAudioFrame/PlaybackProgress`、Media Edge、WebRTC DataChannel、设备 JSON 控制面补齐；独立 text/PCM-free `reply-delivery-v1` 跨进程投影、Control API 幂等 SQLite 落盘/精确读取及 producer-side Fernet spool/重放已随 `20260821-121418-reply-delivery-projection` 生产启用。内部端点 401/首次写入/幂等重复/精确查询、真实 Provider、readiness 和零错误日志门禁通过；仍缺真实板卡同一 delivery 的首帧、播放结束、Actual Heard 与抢占终态证据。 |
| P0 先完成真实声音闭环再换模型 | mostly complete | mostly complete | Agent/Bridge canary | false | `UtteranceRouter` 已被 turn commit 与 barge-in 共用，Python 保持交互权威，没有第二套 Voice Core；ASR finalize、generation、`PlaybackInputGuard` 已接入同一控制链。真实板卡仍未取得稳定 TTS 首帧、播放结束和 Actual Heard，T1–T14 仍为 `0 pass / 14 blocked / 0 failed`。 |
| P1 TTS 首帧与打断基准 | partial | complete | production canary | runtime only | 已新增 `tts_first_frame`（输出源开始消费→Edge 接受首个 PCM 帧）及通用 `voice_first_frame_preempted_total`，并将 `tts_first_frame_p95_ms` 接入 Agent/Control API 的聚合 SLO 传输白名单；普通回复和 `tts_source` 才进入 TTS 时钟，预录 PCM 不混入。埋点已随 2026-08-21 候选生产启用，但零真机会话不能形成延迟样本；仍缺真实设备 Playback/Actual Heard 延迟、下行拒绝同代关联和缓存命中率基准，当前 first audio 不能等同于设备真正听到。 |
| P1 自然度作为受控实验 | guardrail only | not wired as A/B | false | false | 当前没有把呼吸、犹豫、停顿作为默认能力，且 AEC、双讲、自然 barge-in、精确 Actual Heard 尚未通过；尚无独立 naturalness voice profile 的 A/B 实验。 |
| P2 近端语音环 + 远端重推理 | complete | complete | production target/canary | runtime only | 当前边界为 `ESP32 → Go Media Edge → Python Voice Core/Agent`，Python 是交互权威；ESP32 不承载 30B 模型，也不新增第二套 Voice Core。网络延迟和离线需求尚未通过实测，因此暂不引入本地模型 sidecar。 |
| P1 构建供应链门禁 | partial | partial | component-scoped gates | false | 已有 `uv.lock`、Go/npm 锁定、冻结安装、可复现生成、镜像 provenance label、离线 delta build 和 Trivy；尚无覆盖所有 Go/Python/C++/ESP-IDF/容器链路的 SBOM、签名来源证明/attestation、Action 与基础镜像 digest 固定及统一放行门禁。当前 CI 仍有既有 import mismatch、golangci-lint 和 Trivy 高危项；不因供应链事件引入 Rust。 |

本节结论：P2 架构边界已落地；P0 回复终态投影已生产启用，但真实声音闭环仍未完成；P1 首帧埋点已启用，真实基准、自然度实验和供应链门禁仍只能记为部分具备或未启用。当前不换模型、不开放 `full_duplex_verified`，先完成首帧可听和回复投递终态证据。

### 2026-08-21 PCM pacing / ASR 尾部容差切流后的主流程真机话轮验收

本节是 2026-08-21 候选切流后的当前增量状态；前文历史条目的当时判断保留不改。这里的“主流程”只指“用户说完 → Agent 生成回复 → ESP32 收到并连续播放”，不等同于 Exact Playback、Actual Heard、可打断或 `full_duplex_verified`。

| 项目 | code | wired | enabled | verified | 当前证据与边界 |
|---|---|---|---|---|---|
| 生成端 PCM 实时发送节奏 | complete | Agent `MediaOutputStreamMixin` → Go Media Edge → ESP32 Direct v2 | `memoria-agent:20260821-135408-pcm-pacing-asr-tail` | real-device first playable frames | 提交/tag：`714196f31b3a6edc793c2d07f877eb4b47085829` / `20260821-135408-pcm-pacing-asr-tail`；生产 Agent/Bridge healthy、restart=0；真实板 `/dev/cu.usbmodem101` 会话 `a9ea0f30-5a37-4c75-baa7-65a03e19b76f`、`stream_epoch=922` 中 generation 1/2/3 均出现 `First playable downlink frame`，串口 speaking 持续约 3s/22s/30.5s；该范围未见 `transport_rejected` 或 Edge `downlink_queue_full`。 |
| ASR finalize 尾部容差 | complete | `media_session_turns` finalize watermark 判定 | 同一候选已启用 | production runtime + real-device turns | 允许 task-finished 后最多 24,000 samples（约 1.5s）尾部偏差，超过仍 fail closed；本地 99 个 `media_session` 单测、Agent 全量相关门禁通过；真实 epoch 922 已产生 generation 1/2/3。 |
| 用户说完后的 AI 回复主流程 | complete | Direct v2 上行/下行链路已贯通 | 同一候选已启用 | **narrow real-device verified（2026-08-21）** | 用户反馈“说完后 AI 能说了”；串口与服务器同代证据显示三代回复均完成 `first_frame_sent`、`provider_completed`、`emitted_audio=True`，设备进入并退出 speaking。当前只确认回复能连续播放，不宣称回复全文完整、Exact DAC、Playback End 或 Actual Heard。 |
| 播放期用户打断 | partial | `PlaybackInputGuard` / generation preemption 已有代码路径 | 当前候选已启用 | **false（用户现场反馈）** | 用户反馈“AI 说话我无法打断”。本轮先不扩大主流程范围；后续需按当前 speech epoch 取证上行打断、generation preempted、设备停止播放和迟到 PCM 丢弃的同代时间线，再修 Router/播放抢占控制面。 |
| Persona 规则：不披露模型身份 | partial | 当前回复生成链路未形成强制的输出策略门禁 | 当前候选已启用 | **false（用户现场反馈）** | 用户反馈此前设置“不要说自己是什么模型”，但本轮回复自称 DeepSeek。记录为后续 Persona/策略约束整改；需要补权威规则注入、最终文本/音频前的可观测拒绝或改写，以及回归用例。本问题不阻塞本轮“能回复”主流程。 |

- [x] **主流程窄验收（2026-08-21）**：真实板卡取得 generation 1/2/3 首个可播放帧并连续进入 speaking，用户确认“说完后 AI 能说了”；本项只覆盖单向回复可播放，不改变 `direct_real_device_verified=false` 或 `full_duplex_verified=false`。
- [ ] **后续优化，不阻塞主流程**：播放中不可打断；Persona/策略规则泄露模型身份。两项均需单独补齐代码、接线、启用和真实设备验证证据后再更新 T1–T14 或全双工结论。

本节结论：PCM burst 丢失和约 1.14s ASR 尾部错位已完成当前候选收口，真实板卡的“说完后 AI 能连续说”主流程已窄验收通过；播放期抢占、Actual Heard/Playback End 回执和 Persona 模型身份约束仍未通过，继续保持全双工与完整真实声音闭环未验收。

### 2026-08-21 ReplyDelivery / ReplyDeliveryLedger 首个纵向切片（生产已启用，待真机话轮验收）

- [x] **code + wired + local verified（2026-08-21）**：新增 `services/agent/src/voice_core/reply_delivery.py`。`ReplyDeliveryKey` 完整绑定 `session_epoch + turn_id + generation_id + tool_epoch`，同一 key 只保留一个幂等终态；事件序列区分 `first_frame_sent`、`provider_completed`、`actual_heard`、`playback_ended`、`preempted`、`transport_rejected`、`error`、`skipped`、`no_audio`。Media session output dispatch/stream 与 Playback Ledger 已接线，日志和 `voice_reply_delivery_event_total` 使用稳定 delivery id，不记录文本或 PCM。
- [x] **local verified（2026-08-21）**：新增 ledger 单测；Media session 聚焦回归覆盖首帧→Provider 完成→精确 Playback/Actual Heard、approximate device ACK 不提升 Actual Heard、重复/迟到终态不改写。聚焦 pytest、Ruff、strict MyPy（401 个源码文件）与 `git diff --check` 通过。
- [x] **code + wired + local verified（2026-08-21）**：media-v1 新增 `session_epoch` 字段并贯通 Python Voice Core、Go Media Edge、WebRTC DataChannel、设备 WSS JSON 和 Linux 设备客户端；`GenerationController`、Media Edge `Fence` 与设备会话按 `session_epoch → turn_id → generation_id → tool_epoch` 单调排序。旧客户端在 epoch=0 仍兼容，非零 epoch 缺字段的回执按完整 fence fail closed。证据：Agent 聚焦单测、Media Edge `go test ./...`、Ruff、strict MyPy、`git diff --check` 于 2026-08-21 通过。
- [x] **enabled + production runtime verified（2026-08-21，零真机会话范围）**：提交/tag `caf70271222c43047c5d661052f3b717348cc209` / `20260821-121418-reply-delivery-projection` 已协调切换 Control API、Agent 与 Voice Core Media Bridge；三容器均 `healthy`、restart count=0，分别运行 `sha256:4c0d8966…` 与 `sha256:9b6260e5…`。生产使用独立 `MEDIA_REPLY_DELIVERY_TOKEN` 与 Agent-only Fernet spool key，env 均为 `root:root 0600`；内部端点错误 token=401、首次写入=200/inserted、重复写入=200/not inserted、精确读取=200，合成事件随后删除。LiveKit、Qwen、Doubao、FunASR×6、DeepSeek、InterruptSemantic 与 readiness 均通过，相关日志错误计数 0；证据目录 `/opt/memoria/direct-canaries/20260821-121418-reply-delivery-projection/`。
- [x] **code + wired + local verified（2026-08-21）**：新增 `reply-delivery-v1` text/PCM-free 投影生成器与有界非阻塞 `ReplyDeliveryReporter`，由 `scripts/run_media_bridge.py` 接入 `MediaVoiceCoreRegistry`；HTTP 失败进入 Fernet 加密 spool，启动/显式 replay 依次重放；Control API 新增独立 `/v1/internal/media-runtime/reply-delivery` POST/GET、稳定 `event_id`/完整 delivery fence 校验、`media_reply_delivery_events` 幂等落盘、7 天观测保留清理及账号删除时按 session 清理。该路径不复用 `/session-events`，也不进入主人历史/记忆。
- [x] **local verified（2026-08-21）**：投影器、Reporter Mock HTTP、网络 503→加密 spool→202 重放、Control API 端到端 POST/重复投递/精确 GET/fence mismatch 409、已有 media bridge/config/media session 回归、Ruff、strict MyPy 与 `git diff --check` 通过。
- [ ] **real-device verified**：生产候选已启用，但截至切流复核 `media_reply_delivery_events=0`、加密 spool 不存在，说明尚无候选真机会话而非功能失败。下一步由用户本人对板说话，取得同一 delivery id 的真实首帧、Playback/Actual Heard、抢占/拒绝终态；在证据形成前不改变 `direct_real_device_verified=false` 或 T1–T14。

### 2026-08-21 TTS 首帧 / 首帧前抢占基准切片（生产已启用，待真机样本）

- [x] **code + wired + local verified（2026-08-21）**：`MediaOutputStreamMixin` 在普通回复或 `tts_source` 输出迭代器开始消费时记录单调时钟，在首个 PCM 完成 Edge 接受且 Playback Ledger 注册后结束，写入 `voice_latency_seconds{stage="tts_first_frame"}`；预录 `pcm_s16le` 不计入 TTS 首帧。首帧前的 `preempted` 另记 `voice_first_frame_preempted_total`，不依赖 `emitted_audio` 布尔值。
- [x] **code + wired + local verified（2026-08-21）**：`MetricsRegistry.media_slo_snapshot` 产出 `tts_first_frame_p95_ms`，Agent SLO reporter 与 Control API 只允许该聚合字段透传；尚未把它加入自动回滚判定，避免在真实样本不足时伪造门禁通过。
- [x] **local verified（2026-08-21）**：覆盖 normal reply 首帧延迟样本、首帧前抢占终态/计数、SLO snapshot 与 Prometheus stage 解析；目标测试、Ruff、strict MyPy 和 `git diff --check` 通过。
- [x] **enabled + production runtime verified（2026-08-21，零真机会话范围）**：首帧与首帧前抢占埋点已随 `20260821-121418-reply-delivery-projection` 在 Agent/Bridge 启用，镜像/Provider/readiness/日志门禁通过。尚未得到真实板卡首帧、Playback ACK、Exact DAC/Actual Heard 的同代时间线；缓存命中率和真实样本仍待补齐，不能用零会话健康或本地延迟样本替代。

### 2026-08-20 FunASR wire trace 诊断埋点 + ASR 静默丢弃可见性（排查生产零 result-generated，已切流待真机取证）

- [x] **verified（排除法收窄，2026-08-20）**：下行 fence 修复后真机仍无声；新会话（epoch 911）pcm-tap 音频能量健康（语音段 RMS 160–184）、`media_asr_boundary` 11 条全 success、finalize 闭环，但 FunASR 零 `result-generated`/零 partial/尾超时丢弃。四层容器内探针（实时回放/burst/adapter 层/150 次高频轮转）同容器同代码同音频同边界全部识别成功，排除 provider、帧节奏、adapter 映射与限流累积；唯一未观测环节是生产 WS 链路本身。
- [x] **code + wired + enabled + production runtime verified（诊断埋点）**：源提交/tag `65507bede7f6842db3e659b280774db9fdfcda2b` / `20260820-144144-asr-ws-trace-diagnostics`。`FUNASR_WS_TRACE=true` 开启 provider WebSocket 全量控制消息/服务端事件限频 trace（1s 窗口最多 20 条，超出计数汇总）；ASR 决策拒绝三阶段落日志（final 为 WARNING）；ingress stale stream-epoch 丢弃与 adapter 驱逐上下文丢弃不再静默；ASR 尾超时丢弃日志补 partial 在场证据。纯观测，不改门禁/判定/fail-closed 行为；本地门禁：聚焦单测（含 ws trace 限频/env 解析/stale final 拒绝日志新用例）、ruff、strict mypy、模块预算全过。
- [x] **切流（2026-08-20）**：Agent 与 Voice Core Media Bridge 运行 `memoria-agent:20260820-144144-asr-ws-trace-diagnostics`（load 后 image ID `sha256:e6d5876aa379…`，manifest config SHA-256 `bf7abcf3cb0c…` 与本地候选一致，OCI revision 与源提交一致），均 healthy；bridge env `FUNASR_WS_TRACE=true` 生效、pcm-tap 保持；回滚点 `rollback-20260820-144144-asr-ws-trace-diagnostics-pre-agent/-pre-bridge` 已冻结（指向 downlink-fence-session-epoch 镜像 `sha256:237d3ca72337…`）。制品两版本策略已执行：清理 20260812-173008 至 20260820-122047 旧镜像与旧回滚 tag，回收 2.9GB（磁盘 41%→33%）。
- [ ] **verified（待真机说话取证）**：用户对板说话后分析 bridge 日志 `funasr_ws_trace rx/tx` 与 `media ASR result rejected`，区分服务端零回复 vs 本地静默丢弃，然后实施修复。

### 2026-08-20 下行 PCM fence 补携 session_epoch（修复阻塞③ transport_rejected，已切流待真机出声验证）

- [x] **verified（埋点取证，2026-08-20）**：用户对板说话后生产日志四个 turn 全部命中 `reason=fence_mismatch_or_generation_rejected`；turn/gen/tool_epoch 与 sequence/队列均正常，唯一分歧：权威 controller fence `session_epoch=1` vs 帧重建 fence 恒为 epoch=0。根因：`PCMFrame` 无 session_epoch 字段，完整 fence 比对在会话 epoch>0 后必败（本地测试全 epoch=0 故全过）。
- [x] **code + wired + enabled + production runtime verified（原始修复，2026-08-20）**：源提交/tag `2fdc1e721daee6c750aa3852e7235ef8f5380fac` / `20260820-125356-downlink-fence-session-epoch`。`PCMFrame` 新增 `session_epoch` 字段（默认 0），输出流以运行时权威 fence 盖戳，`accept_downlink` 按完整 fence 比对；诊断日志同时打印双侧 tool_epoch/session_epoch。保留换 subject/profile 后旧帧不得越界的 fail-closed 语义；该原始门禁位于 gRPC 序列化前，因此当时无需改 proto/Go media edge。本地门禁：聚焦单测 160 例（含 epoch=1 放行/epoch=0 拒绝回归用例）、ruff、strict mypy、模块预算全过。
- [x] **code + wired + local verified（完整跨传输补齐，2026-08-21）**：media-v1 `GenerationControl/AssistantAudioFrame/PlaybackProgress`、Go `Fence`、WebRTC DataChannel、设备 WSS JSON 与 Linux 设备客户端补携 `session_epoch`；旧 epoch 回执不会被当前 epoch 回填接受。生产 enabled/真实设备 TTS 出声仍未验证，本项不改变 `direct_real_device_verified=false`。
- [x] **切流（2026-08-20）**：Agent 与 Voice Core Media Bridge 运行 `memoria-agent:20260820-125356-downlink-fence-session-epoch`（load 后 image ID `sha256:237d3ca72337…`，OCI revision 与源提交一致），均 healthy，容器内 grep 确认两处修复在位；回滚点 `rollback-20260820-125356-downlink-fence-session-epoch-pre-agent/-pre-bridge` 已冻结（指向诊断埋点镜像）。证据回执 `CUTOVER_RESULT.txt` SHA-256 `2938ca406a859d6552b197dc95523873dc6e05744a7cb71e69460937f1a5ce2e`、`POST_CUTOVER_STATE.txt` `9189fddad8847603442204a952462d136e51e9a0127d732e1932e3f27bd150c6`。
- [ ] **verified（待真机 TTS 出声）**：用户本人对板说话，预期零 `transport_rejected`、`emitted_audio=True`、首次下行出声；若出声则推进 T1–T14 验收。

### 2026-08-20 下行拒绝路径诊断埋点候选（已切流并完成取证）

- [x] **code + wired + enabled + production runtime verified（诊断埋点）**：源提交/tag `bc383a030b0fa27c3605e545ef4a837e69381582` / `20260820-122047-downlink-reject-diagnostics`。`accept_downlink` 七个拒绝分支打 5s 限频 WARNING（含 generation gate/fence/sequence 状态快照）；`emit_generation` 四个失败分支打 `media generation control rejected`；`media_session_commit.py` 检查 `GENERATION_ACTION_START` 返回值，未接受时打 `media generation START not accepted`。不改变任何门禁判定/fence/权限/fail-closed 行为；本地聚焦单测（含 2 个新增用例）、ruff、strict mypy、模块预算全过。
- [x] **切流（2026-08-20）**：Agent 与 Voice Core Media Bridge 运行 `memoria-agent:20260820-122047-downlink-reject-diagnostics`（load 后 image ID `sha256:79003b31ab50…`，manifest config SHA-256 `a94adfe063e4…` 与本地候选一致，OCI revision 与源提交一致），均 healthy；回滚点 `rollback-20260820-122047-downlink-reject-diagnostics-pre-agent/-pre-bridge` 已冻结（指向 ingress-overflow-fix 镜像 `sha256:7d16a2cf07eb…`）。证据回执 `CUTOVER_RESULT.txt` SHA-256 `8d6d902c575cfcff8be444c1a6ebbb6a602ce3984adb75c01490e9f33bc9d7a9`、`POST_CUTOVER_STATE.txt` `1a2e8a971fb207dbd80a7f1893d511f1ac2a3c04a1bc723d00da4003b5daccc6`。
- [x] **verified（真机会话取证完成，2026-08-20）**：四个 turn 全部 `reason=fence_mismatch_or_generation_rejected`，其余状态均正常，唯一分歧为 controller fence session_epoch=1 vs 帧重建 epoch=0，直接定位阻塞③根因（见上节修复）。

### 2026-08-20 固件 ES8388 mic gain 底噪整改（24dB→12dB，真机声学验证通过）

- [x] **code + wired + enabled + verified（固件侧，含声学）**：overlay 板级文件 `GetAudioCodec()` 在 ES8388 构造后调用 `SetInputGain(12.0f)`（upstream 默认 24dB 在 `EnableInput` 时写入，构造后设置有效）；overlay hash `b0d53334…`。本地门禁：ESP-IDF 构建 EXIT=0（merged SHA-256 `92d48e14…77e8eb68`、app `5a4f3296…5cf4d74d`）、`check-overlay.sh` 通过、`firmware/esp32/tests` 78 个全过；`flash.sh` 写 `/dev/cu.usbmodem101` EXIT=0 五段写后 Hash verified，刷前/刷后身份区一致 `b7a717fa…4846`（备份 `artifacts/backups/pre-current-candidate-20260820/`），串口确认 `Set input gain to 12.0` 与 Wi-Fi 连接。源码已随提交 `ee04810` 入库。
- [x] **verified（阻塞①消除，2026-08-20）**：用户本人对板说话，会话 `1e44323b`（epoch 908，85.2s）pcm-tap WAV SHA-256 `9ed58e54…b23b`：底噪中位 RMS 100–150 → 24（77/85 窗口 <40），语音峰值 RMS 158；`media_asr_boundary` 8 条全 success、VAD 正常闭合、零 `superseded`。此前“VAD 永不关闭、回复永远被抢占”已消除。
- [ ] **新阻塞③：下行 `transport_rejected`（举一反三）**：turn 1–3 回复均在第一帧 TTS PCM 即 `status=aborted reason=transport_rejected emitted_audio=False`；`accept_downlink` 门禁拒绝但无打断/取消事件，本地 `test_media_session.py`（94 用例）通过，属生产状态分歧；怀疑 `GENERATION_ACTION_START` 未被消费或 fence 不一致，且 `media_session_commit.py` 未检查 START 返回值。已定位并修复：埋点取证证明根因是下行 PCM fence 丢失 session_epoch（见顶部两节），修复候选已切流，待真机 TTS 出声验证。
- [ ] **阻塞② speaker authority**：维持原判（生产全部账号 `subject_category='unknown'`，`speaker_enrollment` 只允许 `adult`），需产品流程或规则表评审。
- [ ] **Direct real-device / full-duplex verified**：本轮有固件刷写、身份保持与上行声学验证证据，但无 TTS 出声/下行播放；`direct_real_device_verified=false`、`full_duplex_verified=false`，T1–T14 保持 `0 pass / 14 blocked / 0 failed`。

### 2026-08-20 Media ingress 静默丢帧诊断与修复（PCM tap → 根因 → overflow 修复切流验证）

- [x] **code + wired + enabled + production runtime verified（诊断 tap）**：源提交/tag `ce14747592c245e2b98f9a4260190f1ecbb1b1bf` / `20260819-185500-ingress-pcm-tap`。新增 env 门控 PCM 诊断 tap（`MEDIA_PCM_TAP_DIR` / `MEDIA_PCM_TAP_MAX_BYTES`，默认 10MB 封顶 WAV，每 session/stream-epoch 一个，严格 fail-open），只记录准入 ASR Provider 的原始帧。生产 bridge 启用 `MEDIA_PCM_TAP_DIR=/data/diagnostics/pcm-tap`。
- [x] **verified（根因定位，2026-08-20）**：tap 抓回会话 `73805bf2`（epoch 907，327.7s）上行 WAV：用户真实语音段 RMS 200–470、300–1000Hz 谱形正常，证明固件上行内容未损坏，排除 AFE/Opus/重采样损坏假设；对照修复前 boundary 证据（准入水印 20s、Provider 实收仅 960 samples），确认断裂点为 bridge ingress 泵送被 FunASR 阻塞时 20 帧队列静默丢帧（实收约准入量 3%）。
- [x] **code + wired + enabled + production runtime verified（overflow 修复）**：源提交/tag `c70577271aae3b59d74936d1753a0716549ec24c` / `20260820-112031-ingress-overflow-fix`。ingress 队列 20→256 帧（约 5.1s），溢出改为丢最旧帧保最新语音，overflow/pump-stall 打限频 WARNING；新增 overflow 行为单测并更新既有 drop 语义测试，全量门禁通过。仅切换 Agent 与 Voice Core Media Bridge，运行镜像 `memoria-agent:20260820-112031-ingress-overflow-fix`（image ID `sha256:7d16a2cf07ebe0358770f05c9776532194441cb2a7a78414f76d59b605cdb4fa`）；回滚点 `rollback-20260820-112031-ingress-overflow-fix-pre-agent/-pre-bridge` 已冻结。证据回执 `CUTOVER_RESULT.txt` SHA-256 `47d9e4f10aaf76393b0f3ef19c275fe6801d570009c83356588ad694bc84eff3`、`POST_CUTOVER_STATE.txt` `39be6d9ce7e4ee1509d771c84267445898d75b5e5943631e3691a55b1c0b1d2c`。
- 修复后生产观测（2026-08-20，会话 `73805bf2`，stream_epoch 907）：零 overflow/pump-stall 日志；每条 `media_asr_boundary` 的 `provider_pcm_samples == send_count×320`；ASR finalize 成功；话轮 1–4 成功提交（首次 Direct 上行话轮提交）。不构成 T4/T7 通过：所有回复 generation 均在 TTS 出声前被抢占（`superseded emitted_audio=False`），无下行播放。
- [x] **两个新阻塞点之一已修（2026-08-20 固件增益整改，见上节）**：① 底噪问题已由 ES8388 gain 24dB→12dB 消除（底噪 RMS 100–150→24，VAD 正常闭合）。② speaker authority `subject_capability_forbidden` 待产品流程/规则表评审；新发现③下行 `transport_rejected` 待埋点定位。另有 3 次 projection range/text mismatch 拒提交与 2 次 ASR tail timeout 丢弃，待复评。
- [ ] **Direct real-device / full-duplex verified**：本轮只有传输与会话层修复证据，没有完整问答、TTS 出声、打断或稳定性证据；`direct_real_device_verified=false`、`full_duplex_verified=false`，T1–T14 保持 `0 pass / 14 blocked / 0 failed`。

### 2026-08-19 输出 fence + FunASR 候选生产切流与首次 Direct 设备会话观测

- [x] **code + wired + enabled + production runtime verified（2026-08-19）**：源提交/tag 为 `3a3fcdc995efd86544480a5ee8c3beeb351ffd07` / `20260819-151012-output-fence-funasr`，仅切换 Agent 与 Voice Core Media Bridge，运行镜像 `memoria-agent:20260819-151012-output-fence-funasr`（image ID `sha256:589b6f4c993730be98517ffb4ef4481447106358d49f5207dce8bc217eff27ad`，OCI revision 与源提交一致）。Bridge 具名 gRPC Health `SERVING`、Edge mTLS readyz `ready`、公网 8443 未认证回顾读 401 fail closed、切流后 Traceback/ERROR 归类为 0（Bridge 日志中 80 条 traceback 均为 `media_asr_boundary` fail-closed 的 `EmptyAudio` 记录）。回滚点 `rollback-20260819-151012-output-fence-funasr-pre-agent/bridge` 已冻结（`sha256:2afb5420…`）；证据回执 `CUTOVER_RESULT.txt` SHA-256 `97f6af5237026c783fa61b79b81561b87c1e4d3f6bae24094f06bd4ef1161e67`，`POST_CUTOVER_STATE.txt` `3447155124a7c3842bb81ae698aded4e85ff8d66caad6d2d501e216e191baf5f`，manifest `c544d0ddeb0d37fd6377fe21fb383d99f72e52bda05d3d49976efb0587a19cee`。
- 首次 Direct 设备会话观测（2026-08-19）：切流后出现会话 `8d2eeae4-96a7-4a5a-bb4a-183f14081fc4`（`stream_epoch=902`，`08:28:07Z–08:55:20Z` 约 27 分钟），`media_asr_boundary` 共 223 条：143 success、80 failed（均为 `EmptyAudio` 短段低能量 fail-closed，RMS≈41–67、时长 0.04–0.1 s）。会话未产生任何 ASR final 文本、话轮提交、generation、TTS 或下行播放，不满足 T4/T7；不计入 `direct_real_device_verified`，T1–T14 保持 `0 pass / 14 blocked / 0 failed`。
- Agent 单次自愈重启归因（2026-08-19）：`16:52–16:55 CST` 服务器 ubuntu 用户为 family-h5 站点修改 nginx 配置并多次 reload（期间出现一次 8443 `bind() Address already in use` 未生效尝试），导致 Agent 到 LiveKit 的 WSS（`wss://122.51.108.140:8443`）于 `08:55:22Z` 断开；worker 重试 16 次耗尽后优雅退出（exit=0），compose restart 策略于 `08:57:33Z` 拉起并在 `08:59:14Z` 重新注册 worker（`AW_v4ybGnJrLAAW`），第二代无连接失败。该事件同时终止了上述设备会话；属外部运维瞬态，非候选缺陷。举一反三：nginx/证书类运维应避开活跃设备会话窗口，且 Agent 对 LiveKit WSS 的重试预算耗尽即退出进程，后续可评估更长退避或无限重试以避免运维瞬态升级为容器重启。
- [ ] **Direct real-device / full-duplex verified**：首次会话只有上行音频到达 Provider 的证据，没有用户说话产生的 ASR final、TTS、DAC 出声、Exact Playback、按钮打断、AEC Reference、停止词、自然抢话、双讲或 100/500 轮稳定性证据；`direct_real_device_verified=false`、`full_duplex_verified=false`，T1–T14 保持 `0 pass / 14 blocked / 0 failed`。

### 2026-08-19 当前候选 ESP32 刷写与启动/激活验证（非媒体）

- [x] **code + wired + enabled + verified（固件写入、写后校验与身份保护）**：当前候选由 `firmware/esp32/scripts/flash.sh` 写入真实板卡串口 `/dev/cu.usbmodem101`，脚本退出码为 `0`；bootloader、分区表、OTA data、应用和资源五段写后校验通过并完成硬复位。`flasher_args.json` 的写入地址只有 `0x0/0x8000/0xd000/0x20000/0x800000`，明确未写入 `0x10000..0x1ffff` 身份区。合并镜像大小 `13,577,086` bytes、SHA-256 `7d7b921a81500817fda6a8230cd5c5b5a97814855dd0a440188f3d75e2cee188`；应用镜像大小 `2,950,832` bytes、SHA-256 `6fe8e936d481433f80fd0dcd5c076f2a5f47029132e2b9e21ccb1680fe3d798d`；overlay SHA-256 `513e14ef346d49fc39e82b9bf96e2c56df0bf25bb1d2f081ee56e5707dd6a969`。刷前/刷后身份区备份均位于 `firmware/esp32/artifacts/backups/pre-current-candidate-20260819/`，SHA-256 均为 `b7a717fa399ec1390391ca381b9b86c3202035c71695a95e417a4e0f1d084846`，`cmp` 逐字节一致；刷前 Wi-Fi NVS 与 OTA data 同目录留存。
- [x] **verified（真实板卡非媒体启动范围）**：复位后串口确认 ESP32-S3 rev `0.2`、`8 MB PSRAM`、SKU `memoria-atk-dnesp32s3-v1`、MAC `a4:cb:8f:d6:09:5c`；Wi-Fi 成功连接并取得 IP，Activation Manifest v2 验签成功，状态完成 `starting -> activating -> idle`；ES8388、I2S、AFE 和单麦输入初始化成功。以上只证明当前候选的写入、身份保持和非媒体启动/激活，不证明设备媒体会话或声学输出。
- [ ] **Direct media / full-duplex verified**：本阶段没有播放电脑音频、录音文件、TTS、程序生成音频或注入真人语音。`direct_real_device_verified=false`、`full_duplex_verified=false`；Exact Playback、按钮打断、AEC Reference、停止词、自然抢话、双讲、100/500 轮稳定性及 T1–T14 仍需按“用户本人说话”的顺序独立验收。

### 2026-08-19 FunASR lazy task handoff 与输出 fence 收口（已提交，未部署）

- [x] **code + wired + local verified（已提交并推送）**：源码提交 `3a3fcdc995efd86544480a5ee8c3beeb351ffd07` 与 annotated tag `20260819-151012-output-fence-funasr` 已推送 `origin/main`。在 `FunASRSession`、Existing Voice Provider Adapter 与 Voice Core MediaAudioIngress 中完成 lazy task handoff、task/WebSocket/epoch fence、失败帧 fail-closed、Provider PCM 统计（sample count、peak、RMS、zero/clipping）及失败恢复时 backlog drain/discontinuity reset；迟到旧 task final 保留旧 task epoch 与 sample origin；同一提交还收口了 media session 输出 fence 与 output dispatch。新增/更新 FunASR session、mock/integration、Provider adapter、Media session 回归均通过。
- 验证（2026-08-19）：Ruff、strict MyPy（399 个源码文件）、模块预算、`git diff --check`、固件 overlay gate、`firmware/esp32/tests` 与以下 Agent 聚焦套件全部通过：`test_funasr_session_edges.py`、`test_funasr_mock.py`、`test_funasr_recognize_stream.py`、`test_provider_adapter.py`、`test_media_session.py`。
- [x] **enabled + production runtime verified（2026-08-19）**：已构建并仅切换 Agent 与 Voice Core Media Bridge 至 `memoria-agent:20260819-151012-output-fence-funasr`；健康门禁与证据见上一节。不继承 `20260818-152620-vad-asr-boundary` 的 runtime 证据。
- [ ] **firmware / Direct real-device / full-duplex verified**：当前候选已完成真实板卡刷写、身份区保持和非媒体启动/激活验证，但尚未完成媒体会话或声学验收；不使用电脑播放或程序注入音频。`direct_real_device_verified=false`、`full_duplex_verified=false`，T1–T14 继续按真实设备与用户本人语音独立判定。

### 2026-08-18 VAD → ASR Provider PCM 边界可观测性

- [x] **code + wired + local verified**：代码提交 `3e16ee93b0628bfc02be527c3760ea53b943f53b` 与不可移动 annotated tag `20260818-152620-vad-asr-boundary` 已推送。`FunASRSession` 只在 Provider WebSocket `send()` 成功后记录当前 task 实际接受的 PCM 起止 sample 与 send count；task rotation、重连 task、失败 replay 均按 task fence 清空或保持空证据。Voice Core 每次 VAD finalize 输出统一 `media_asr_boundary` JSON，包含 VAD start/event/voiced-end、transport watermark、Provider PCM 范围、task epoch 前后和 `success / failed / duplicate / stale / no-provider-finalize` 结果。该增量不改变 VAD 阈值、短段策略、主体权限或 fail-closed 行为。
- 验证：Provider/Adapter/Media Session 聚焦测试、Agent 全量测试、Ruff、strict MyPy（399 个源码文件）与模块预算全部通过。
- [ ] **enabled + production runtime verified**：尚未构建或部署。只允许在服务器最新环境构建 Agent image，并只切换 Agent 与 Voice Core Media Bridge；Control API、Media Edge、固件、H5、小程序和数据服务保持不变。
- [ ] **Direct real-device behavior verified**：部署后必须由真实板卡现有固件发起新鲜设备语音会话，取得包含 Provider PCM start/end/send count 的 `media_asr_boundary` 日志，并与 VAD start/event/voiced-end 和 transport watermark 对照后再决定是否修行为。当前 `direct_real_device_verified=false`、`full_duplex_verified=false`，T1–T14 为 `0 pass / 14 blocked / 0 failed`。

### 2026-08-18 Runtime Profile 主体权威与重连 fence 生产切流

本轮跨 Control API、Session Runtime、Go Media Edge 与 Python Voice Core/Agent 的主体权威收口已提交为 `fb45fad59165ec819ac5b03148cba5c537b7eb6e`，annotated tag 为不可移动的 `20260818-103228`，并按授权范围完成生产切流。这里的 `wired` 表示源码级跨运行时接线，`enabled` 表示当前候选已进入对应生产服务，`production runtime verified` 仅表示服务器健康、readiness、gRPC Health、mTLS readyz、metrics 与公网 fail-closed 路由已通过；没有构建或刷写本轮 ESP32 固件。

| 项目 | code | wired | enabled | verified | 证据日期 |
|---|---|---|---|---|---|
| 绑定归属与当前主体分离 | complete | SQLite / Control API / Session Runtime 本地接线 | 20260818-103228 目标服务已启用 | 本地回归 + 生产 runtime（零活跃会话） | 2026-08-18 |
| Direct 票据显式主体 claim | complete | Control API → Go Edge → media-v1 → Agent 本地接线 | 20260818-103228 目标服务已启用 | 本地跨语言合同 + 生产 runtime（零活跃会话） | 2026-08-18 |
| Runtime Profile `unknown_safe` 原子降级 | complete | Session Runtime → Control API / Agent 本地接线 | Control API / Agent 已启用 | 本地回归 + 生产 runtime（零活跃会话） | 2026-08-18 |
| 重连 authority fence 与失败回收 | complete | Go Edge + Python Bridge/Registry 本地接线 | Agent / Bridge / Edge 已启用 | 本地正常/拒绝/Provider reset 失败路径 + 生产 runtime | 2026-08-18 |

- `device_media_sessions.subject_id` 继续非空表示绑定/账号归属；新增 nullable `active_subject_id` 表示 Runtime Profile 当前主体。SQLite 旧行只在新增列时一次性回填，后续显式写入的 `NULL` 不会被初始化逻辑重新覆盖。
- category/age 等主体资料不完整时，Session Runtime 在签名和 `set_action_subject()` 前将主体、revision、speaker state、service mode、capabilities 与 obligations 原子降级为完整 `unknown_safe`，不保留半旧半新的私人权限。
- Direct JWT 必须显式携带 `subject_id`：已确认主体传精确 ID，unknown-safe 传空字符串；claim 缺失或 JSON `null` 在 Go Edge fail closed。空字符串只与 Python Profile 的 `active_subject_id=None` 等价，不是 wildcard；所有非空主体逐字比较。
- 同一媒体 session 的重连只允许 `stream_epoch` 增加；account、participant、device、client type、subject、binding 与 Runtime Profile version 任一变化都必须新建会话边界。Provider epoch reset 失败时同时回收旧 Registry runtime/provider 与已切换的新 Bridge epoch，并传播原错误。
- 2026-08-18 真实板卡对既有生产版本的诊断已完成：会话 `287ec335-134b-4b65-8072-f02875d19814` 的 `POST /v1/interaction/session-policy` 返回 503 `session_runtime_subject_unavailable`，随后出现 `RuntimeProfile version does not match authority` 与 Edge stream EOF；根因是已绑定主体的 category/age 权威事实不完整形成非法混合态，不是板卡、Wi-Fi 或 Device WSS 故障。该证据不表示当前候选已部署或已被真机验证。
- media-v1 当前仍使用普通 proto3 string，Python 端无法区分 gRPC `subject_id` 未发送与显式空字符串；唯一生产 Bridge 客户端 Go Edge 只会从已验证且显式带 claim 的 JWT 构造该字段，因此伪造缺字段最多降级到 `unknown_safe`，不能升级权限。后续可将协议字段 optional 化，但不阻塞本次单设备修复。
- 本地门禁通过：Control API 聚焦回归 57 项、Agent 聚焦回归 228 项、Session Runtime 纯内存 32 项、Session Runtime PostgreSQL 文件回归 43 项、固件协议源码测试文件、Go Media Edge `go test ./...` 全量、生产源码 Ruff、strict MyPy（399 个源码文件）、模块预算与 `git diff --check`。一次性 PostgreSQL 容器已删除。
- [x] **code + wired + local verified（2026-08-18）**：以上本地门禁证明跨组件合同闭合；生产源代码提交为 `fb45fad59165ec819ac5b03148cba5c537b7eb6e`。
- [x] **enabled + production runtime verified（2026-08-18，零活跃会话范围）**：`20260818-103228` 已切换 Control API（含 Session Runtime）、Agent、Voice Core Media Bridge 与 Go Media Edge；四个目标容器均 healthy、restart count=0、OCI revision 与源提交一致。切流回执为 `/opt/memoria/direct-canaries/20260818-103228/CUTOVER_RESULT.txt`（SHA-256 `37da7f571630c0585ab350464eb789196cd3075998758a3c43d611ab240b53848`），切流后状态为 `POST_CUTOVER_STATE.txt`（SHA-256 `41060c9838f1c1db0b38e52c906292f9b14bd025e13df9539dc3bd6e9fa98bc8`）。
- [ ] **firmware / Direct real-device / full-duplex verified**：本轮没有重建或刷写 ESP32 固件；板卡仅用于既有生产版本诊断，尚未用当前候选建立设备会话、播放或声学验收。`direct_real_device_verified=false`、`full_duplex_verified=false`，T1–T14 保持 `0 pass / 14 blocked / 0 failed`。

### 2026-08-18 生产只读盘点（候选切流前）

> 本节记录切流前的历史快照；切流后的当前状态以紧随其后的“生产切流与制品清理结果”、`HANDOFF.md` 和 `architecture-status.yaml` 为准。

- 目标服务当前运行且健康：Control API `memoria-control-api:20260817-171013`（`sha256:02d4d86a…`）、Agent/Voice Core Media Bridge `memoria-agent:20260817-135129`（`sha256:163c4d53…`）、Go Media Edge `memoria-media-edge:20260816-bridge-liveness-83af813`（`sha256:7111c785…`）；目标容器 restart count 均为 0。当前未启用本轮未提交候选。
- 当前运行镜像就是本次切流前的即时回滚基线；切流时需先冻结这三组 digest，再只切 Control API（含 Session Runtime）、Go Media Edge、Agent/Voice Core Media Bridge。ESP32、Nginx、H5、小程序和 PostgreSQL/Redis 不切换。
- 2026-08-18 生产主机根盘约 15 GiB 可用、88% 已用。历史 canary 镜像和归档已超过“当前版本 + 一个可运行回滚版本”的保留目标；本轮未执行清理，待单独确认可删除清单后再做维护。
- 板卡串口 `/dev/cu.usbmodem101` 监视已安全退出，板子仍在运行；当前旧版本持续 `recovering → listening → recovering`，生产 Edge/Voice Core 日志持续出现同一 session 的 `EOF` 与 Runtime Profile authority mismatch。该证据用于诊断，不计入当前候选的 Direct 真机 verified。
- [x] **production read-only inventory verified（2026-08-18）**：运行镜像、容器健康/重启、磁盘、回滚基线与板卡旧版本故障现象已刷新。
- [x] **production cutover authorized（2026-08-18 10:38 CST）**：用户已授权仅切换 Control API（含 Session Runtime）、Agent、Voice Core Media Bridge 与 Go Media Edge；ESP32 固件、Nginx、H5、小程序、PostgreSQL、Redis 均不切换。候选 release tag 固定为 `20260818-103228`。
- [x] **rollback point frozen（2026-08-18）**：三组当前运行 digest 已创建唯一 rollback tag，现行 Compose/override、四个目标容器 JSON 与磁盘状态已保存到 root-only `/opt/memoria/direct-canaries/20260818-103228/`；`PRE_CUTOVER_STATE.txt` SHA-256 为 `9b0d19a59ed60af951570eaad6648e1fb7db539fbe457a1a1aa1fa70a23b727c`，冻结过程未重启容器。
- [x] **candidate release / production cutover（2026-08-18）**：已完成构建、上传、逐服务切流与健康门禁；结果与清理回执见下一节。未切换 ESP32 固件及非授权服务，未在健康门禁前要求板卡 RESET 或语音测试。

### 2026-08-18 生产切流与制品清理结果

- [x] **code + wired + enabled + production runtime verified（2026-08-18T04:56:23Z）**：源提交/tag 为 `fb45fad59165ec819ac5b03148cba5c537b7eb6e` / `20260818-103228`。Control API 运行 `memoria-control-api:20260818-103228`（`sha256:14916cf1d00e1c10a8a8f52b5aad64eb301949f5e2319a02125484d3ed11e891`）；Agent 与 Voice Core Media Bridge 运行 `memoria-agent:20260818-103228`（`sha256:549f5f29176b6ad3361290726abecff748b804b5cb3473fd7ea6fc5e37784d65`）；Go Media Edge 运行 `memoria-media-edge:20260818-103228`（`sha256:d512fd348f9424cbeaccf091b9deeb50962dc83e5f3c598675dd2b5081697261`）。四个目标容器均为 running/healthy、restart count=0，Control API/Agent fence 保持 `20260814-231749-direct-canary`，Bridge 保持 `20260816-bridge-liveness-83af813`。
- 生产探针：公网 live/ready 均为 200；未认证回顾读与记忆确认写均为 401；`memoria.media.v1.VoiceMediaBridge=SERVING`；Media Edge mTLS `/readyz` 为 200/ready。指标为 `active_device_connections=0`、`active_media_sessions=0`、liveness healthy=1、probe successes=4060、failures=0、redial attempts/successes/failures=0/0/0。
- [x] **rollback points preserved（2026-08-18）**：Control API、Agent、Bridge、Media Edge 的即时可运行 rollback tag 与切流前 root-only 快照均保留；annotated tag `20260818-103228` 不移动。
- [x] **artifact cleanup round 1（2026-08-18T02:57:28Z）**：删除历史目录 55 个、旧 `memoria-*` 镜像标签 90 个，失败 0；Docker build cache 为 0。该轮清理后根盘为 118G 总量 / 50G 已用 / 65G 可用 / 44%。
- [x] **artifact cleanup round 2（2026-08-18T05:55:13Z–05:55:25Z）**：删除 4 个已退出诊断/一次性 provision 容器、6 个旧目录/staging 路径和 5 个未被容器引用的旧镜像标签；root-only 回执为 `/opt/memoria/direct-canaries/20260818-103228/ASSET_CLEANUP_20260818T060000Z.txt`，SHA-256 `68e0e72c4bedaf49a919bb1ede901f07eedc25453e29188a753568c6ff8582cf`。清理后 Docker 为 29 images / 12 active、14 containers / 14 active、5 volumes / 4 active、build cache=0；根盘为 118G 总量 / 44G 已用 / 70G 可用 / 39%。当前版本、紧邻回滚、legacy 运行链、数据卷及合规备份均保留。
- [x] **artifact cleanup round 3（2026-08-18T07:16:42Z）**：确认无运行或可执行回滚引用后，删除旧源码检出 `/opt/memoria/direct-canaries/20260814-231749-direct-canary`，释放 `43,723,036` bytes。root-only 回执为 `/opt/memoria/direct-canaries/20260818-103228/ASSET_CLEANUP_20260818T071642Z_ROUND3.txt`，SHA-256 `327061dd42e369cf2d93cb45b6174f89b122892231d1d82bcd8604fae144c9bc`；删除后路径不存在、当前 runtime 与紧邻 rollback 保留、14/14 容器 active，根盘为 118G 总量 / 45G 已用 / 69G 可用 / 40%。回执中的 22 images 按唯一 image ID 统计，不覆盖第二轮 29 个 tag/list 项口径。
- [x] **VAD → ASR Provider PCM boundary observability enabled + production runtime verified（2026-08-18T09:37:29Z）**：候选提交/tag 为 `3e16ee93b0628bfc02be527c3760ea53b943f53b` / `20260818-152620-vad-asr-boundary`，仅切换 Agent 与 Voice Core Media Bridge，运行镜像 `memoria-agent:20260818-152620-vad-asr-boundary`（image ID `sha256:2afb5420f7d90459d1d94f708e97a1ba98588f31b7a144bb1a722aac6bb4c769`）。`media_asr_boundary` 脱敏聚合共 645 条：638 success、7 failed；7 条失败均为 `EmptyAudio`，另有 1 条独立 Provider timeout，不归因于边界观测。stream epoch 为 893/894，finalize reason 全为 `vad_end`，638 条观察到 rotation，7 条未观察到。生产容器与探针健康，channel generation=2、active device connections=1、active media sessions=0、downlink frames=0；这不是播放或真机全双工证据。Qwen realtime-search、Doubao、FunASR Provider smoke 通过（FunASR 6 次）。证据与 SHA-256：`POST_CUTOVER_STATE.txt` `ea9d953e969711f1d4043c6016b8202a3d8bba9f6ee06fe82207eccdce7578f5`，`CUTOVER_RESULT.txt` `c41355fb662574863ec20c0d73cbb7f36b86885bc4b5e0e1b51783ecb58f916d`，`PROVIDER_SMOKE_RESULT.txt` `65f85c4d6a23188f679ea74418abbbffadaa41f578f45f610a4802ac504cc1fd`，`MEDIA_ASR_BOUNDARY_RESULT.txt` `ea32078314c3f43bbd02e2776e156a5a22bef9eb1f3f8ca4f01beabe18dda7e6`，manifest `9ae753278568bd8496af1e4d87f123751df1c3113ef9ad151a32b4103231be07`。
- [x] **obsolete one-shot control-script cleanup（2026-08-18T09:27:15Z）**：确认历史目录中的 `rollback-control-api.sh` 已引用失效旧镜像，且 `cutover-control-api.sh` 唯一调用该脚本后，已将两份失效脚本一并删除。root-only 回执 `/opt/memoria/direct-canaries/20260818-152620-vad-asr-boundary/ASSET_CLEANUP_20260818T092715Z_OBSOLETE_CONTROL_SCRIPTS.txt`，SHA-256 `481c7fb40a1e00e221f7c478c744a34734fad45b329267744cd34e8bd19a7426`；历史 Compose/证据目录与当前 `rollback-20260818-103228-pre-runtime-profile*` 回滚链保留。
- [ ] **Direct real-device / full-duplex verified**：服务器运行证据不替代当前候选固件刷写、真实设备音频会话、Exact DAC watermark、AEC Reference、Double-talk、精确 Actual Heard、100/500 轮稳定性或 T14 微信双端证据；`direct_real_device_verified=false`、`full_duplex_verified=false`，T1–T14 为 `0 pass / 14 blocked / 0 failed`。

### 2026-08-17 PR-19 Control API 生产 canary

代码提交 `24784a90f3e6d3cfb6638d13b1d432edbc4f8d17` 与 annotated tag `20260817-171013` 已推送；本次只切换 Control API。小程序 PR-19 页面仍未发布，Agent、Bridge、Media Edge、H5、ESP32 固件、Nginx 与数据服务均未切换。运行时继续使用 `20260814-231749-direct-canary` fence，以兼容现有 Agent heartbeat。

| 项目 | code | wired | enabled | verified | 证据日期 |
|---|---|---|---|---|---|
| `GET /v1/archive/conversation-review` 窄投影 | complete | Control API 路由、Archive/Catalog 与主体能力门禁；小程序页面本地接线 | Control API 候选已启用；小程序未发布 | Control API production runtime + 公网未认证 401 fail closed；小程序 local only | 2026-08-17 |
| 记忆候选确认分区 | complete | 既有 review 写路径复用 `conversation_review` capability | Control API 候选已启用；小程序未发布 | local adult/minor/unknown/missing 矩阵；未执行生产认证业务写入 | 2026-08-17 |

- 第一次切换于 `2026-08-17T09:42:39Z` 开始，候选 healthy 后单次 readiness 返回 503，脚本于 `09:43:14Z` 自动恢复旧镜像并验证 healthy、restart count 0、readiness 200。该次未保留 503 body，因此不把它单独归因为候选代码故障。
- readiness 加固为每 2 秒重试、最多 30 次后，第二次切换于 `2026-08-17T09:51:03Z–09:51:28Z` 成功。前两次响应明确为 `smokes=passed`、12 项 core ready、`agent.status=missing`；第 3 次收到同代际 Agent heartbeat 后变为 200/ready，确认短暂 503 是新进程等待心跳的启动窗口。
- 当前 Control API 镜像为 `memoria-control-api:20260817-171013`，image ID `sha256:02d4d86a349c2dd255f44fe0229c93e5d1a82337bde6cb81a418e22f0bec40e2`；health healthy、restart count 0，挂载、端口与 OCI metadata 均通过。公网 8443 live/ready、根 H5、SPA、WMS 通过，新回顾 GET 与 review POST 路由未认证均返回 401，internal 仍为 404；`2026-08-17T10:03:45Z` 延迟复核时成功切流后的 `Traceback / ERROR / 5xx` 仍为 `0 / 0 / 0`，11 个旁路容器 ID 未变化。
- [x] **Control API code + wired + enabled + production runtime verified（2026-08-17）**：证据文件 `/opt/memoria/direct-canaries/20260817-171013/CUTOVER_RESULT.txt`，SHA-256 `a64b8d8897db818d26abe07344fe3b2bba8a6ab679e13a4ec02d7f88bdca1399`。此处 verified 是服务器运行、readiness 和公开 fail-closed 路由证据，不是认证用户业务流程或真实设备 Actual Heard 证据。
- [x] **rollback path exercised（2026-08-17，Control API only）**：第一次自动回滚证据 `CUTOVER_ATTEMPT1.txt` 的 SHA-256 为 `0b98695f2197411ac684159eaa4a778a4de06f2a8dd84e04c451ba9eaacabcfc`；旧镜像即时恢复成功。
- [ ] **小程序 production enabled / verified**：PR-19 页面目前仅为 `code + wired + local verified`，尚未发布体验版或正式版，也没有微信 iOS/Android 真实账号验收。
- [ ] **Direct real-device / full-duplex verified**：本轮没有刷板、没有设备会话、没有 Exact DAC/AEC/Double-talk/精确 Actual Heard 证据；`direct_real_device_verified=false`、`full_duplex_verified=false`，T1–T14 保持 `0 pass / 14 blocked / 0 failed`。

### 2026-08-17 Agent/Bridge 生产 canary

代码提交 `91f179069055720795fadafc1839045c1cc61f31` 与发布 tag `20260817-135129` 已推送；固定 amd64 工件已上传并完成生产切流。实际只启用 Agent 与 Voice Core Media Bridge 候选，Control API、Go Media Edge、ESP32 固件、小程序和 H5 未切换；未刷写 ESP32，也未触发设备或电脑播放。因此下表逐项区分本地跨组件门禁、Agent/Bridge 生产启用和零活跃会话服务器验证，`direct_real_device_verified` 与 `full_duplex_verified` 继续保持 `false`。

| 项目 | code | wired | enabled | verified | 证据日期 |
|---|---|---|---|---|---|
| Speaker Authority 403 主体能力矩阵 | complete | local Agent ↔ Control API；生产 Agent 沿用现有 Control | Agent 处理已启用；Control 候选未切换 | local matrix；真实策略拒绝未触发 | 2026-08-17 |
| generation 委派输出所有权 | complete | Voice Core ↔ Agent output path | Agent/Bridge 候选已启用 | local + production runtime zero-session | 2026-08-17 |
| VAD endpoint/tail、ASR final 缺失、prepare retry、重连 epoch、Playback/Projection fence | complete | Agent/Bridge 候选接线 | Agent/Bridge 已启用 | local + production runtime zero-session | 2026-08-17 |
| Python / Go / ESP32 空音频帧 fail-closed | complete | local contract paths | Python Agent/Bridge 候选已启用；Go/ESP32 未切换 | local cross-language；无真实空帧会话 | 2026-08-17 |
| Runtime Profile 低基数拒绝原因指标 | complete | Agent/Voice Core metrics path | Agent/Bridge 候选已启用 | local；零会话未产生业务样本 | 2026-08-17 |
| Go 异步容量/邮箱指标测试稳定性 | complete | local Go test path | Media Edge 保留 2026-08-16 镜像 | local only | 2026-08-17 |
| `jsonschema` 可复现 dev 依赖 | complete | `pyproject.toml` + `uv.lock` | dev only | local dev sync | 2026-08-17 |

本轮具体收口：

- Speaker Authority 对 `minor_forbidden`、`subject_capability_forbidden`、`subject_category_unavailable` 三类稳定策略拒绝统一降级为 `authority_policy_denied + uncertain`；未知 403、5xx、网络异常仍保持 transport failure，不把策略拒绝升级为主人权限。Control API 成人、未成年人、主体资料缺失矩阵均有测试。
- generation 委派新增 `PENDING / OWNED / RELEASED / COMPLETED` 输出 claim。初始决策超时、委派失败、空结果、异常、取消和迟到 generation 均只能释放一次本地 fallback；已拥有或已完成的委派不会再触发普通回复。
- Media Turn 继续以同一话轮 fence 管理 VAD endpoint/tail、缺失 ASR final、Provider prepare 瞬态重试、输出超时、重连 epoch 与 Playback/Projection；重试保留原 sample range，只有新 VAD 能显式 supersede。
- Python、Go、ESP32 对零 `frame_samples`、空 payload 与长度不匹配均 fail closed；没有把空帧当作有效音频或推进 sample/generation。
- Runtime Profile 拒绝指标只允许固定低基数 reason 标签；授权语义没有放宽，`unknown_safe + chat` 仍不能进入私人能力。
- `pyproject.toml` dev extra 增加并锁定 `jsonschema`；`uv sync --frozen --extra dev` 已验证。`--all-extras` 仍受既有 macOS ARM 无可用 `vosk==0.3.45` 包影响，不将该失败写成代码回归。
- 本轮本地门禁：Agent 1558、Control API 586；Speaker/Router/Runtime Profile 聚焦 165；Voice Core 聚焦 126；Control Speaker API 4；固件源码、Device v2 合同、架构状态与验收器 251；strict MyPy 399 个源码文件；Ruff、Go 全量、模块预算与 `git diff --check` 通过。Go 聚焦异步指标测试连续 20 次通过。
- 生产第一次切流于 `2026-08-17T06:20:54Z` 因公网 readiness 的既有 `smokes:not_run` 503 自动回滚；候选容器健康、具名 gRPC Health、Edge redial 与 Provider smoke 均通过，未定位到候选代码故障。完成 LiveKit、Provider、`verify_env` 和 readiness mark 后，第二次切流自 `06:25:41Z` 成功，Edge 于 `06:26:12Z` 无重启自动重拨。
- `2026-08-17T06:34:16Z` 延迟复核：Agent/Bridge 候选 image ID 为 `sha256:163c4d534fa06a4b796a5217fcaead32ea97162a108bef1c4c6237f94238cb4e`，OCI revision 与发布提交一致；Agent、Bridge、Edge 均 healthy、restart count 0，Bridge 具名 Health 为 SERVING，Edge mTLS readyz 为 200，redial attempts/successes 为 3/3，active device connections/media sessions 为 0/0，切流边界后的目标错误计数全部为 0。证据文件为 `/opt/memoria/direct-canaries/20260817-135129/CUTOVER_RESULT.txt`。
- GitHub Actions run `32003128393`（`2026-08-17T06:48:28Z–06:50:19Z`）未通过：H5 与小程序通过；Python 的 11 个同名测试模块 collection 冲突、Media Edge 既有 GolangCI-Lint、Media Edge image 的 9 个 HIGH/0 个 CRITICAL Trivy 结果失败。代码提交 run `31999508100`、父提交 run `31966438089` 和 2026-08-11 run `31464320509` 已有相同签名，因此本次部署不记为 CI 全绿。
- 2026-08-17T06:56:47Z 公网只读复核：8443 域名/IP 的 WMS 与业务探针通过；443 真实 IP 在无 SNI 时可返回 WMS 200，但域名 SNI 在 ClientHello 后 EOF（`SSL_ERROR_SYSCALL`/`unexpected eof`），不把标准 443 域名 WMS 写成通过。该既有 443 边界未修改，正式生产入口仍为 8443。
- PR-16 本轮本地收口（2026-08-17）：Tool cancel 的 code 已完成，wired 到 `DuplexRuntime` 真实 interrupt/preemption/identity-epoch rotation 的 `TaskManager.cancel_cancellable`，Agent/Bridge canary 已 enabled；`test_cancelled_late_tool_result_cannot_form_new_generation_output` 验证 cancelled + generation/task/context fence + one-shot output gate 拒绝迟到结果。heard text cutoff 的 code 已完成并 wired 到 provider timed transcript/Playback Ledger，Agent/Bridge canary 已 enabled；`test_existing_provider_adapter_cuts_unplayed_timed_text_on_interrupt` 与 media-session interruption/playback-ledger 回归仅保留已生成/已确认范围。此处 verified 仅为 local verified；生产运行证据仍是 zero-session，Exact DAC watermark、真实设备 Actual Heard、AEC、双讲和 T1–T14 未通过。

- [x] **code + wired + enabled + production runtime verified（2026-08-17，Agent/Bridge 零活跃会话范围）**：发布 `20260817-135129` 已只切换 Agent 与 Voice Core Media Bridge；Control release tag、Media Edge、runtime/H5 软链和其余服务保持原代际。LiveKit、FunASR、Qwen Realtime Search、DeepSeek、Doubao、Interrupt Semantic、`verify_env`、readiness 与公网 8443/IP 探针均通过；标准域名 443 的 SNI WMS 仍因既有 ClientHello EOF 未验收。该项只证明候选代码在生产 Agent/Bridge 运行、分层 release fence 可用和零会话热替换稳定，不证明新功能已由真实设备话轮触发。
- [x] **rollback path exercised（2026-08-17）**：第一次切流遇到发布前既有 readiness smoke 状态未刷新后自动恢复冻结 Agent/Bridge 镜像；刷新 smoke 后第二次切流通过。回滚快照与两个即时 rollback image 均保留，该证据证明运维回滚路径可执行，不替代 Direct 真机、声学或数据恢复演练。

- [x] **code + local verified（2026-08-14）**：小程序实时麦克风、实时 TTS、媒体 WSS、LiveKit 房间、手机声纹入口已移除，首页收敛为设备/摘要 Dashboard，并有静态防回归门。
- [x] **code + local verified（2026-08-14）**：Device Media v2、Go Device WSS/Auth、一次性 JTI、单连接租约、Redis 原子共享状态、Generation/Playback Fence、Edge ↔ Python Voice Core `media-v1` 桥及 Direct/legacy 路由隔离已实现并通过相应本地门禁。
- [x] **enabled + real-device verified（2026-08-14，legacy v1）**：Agent-only canary 修复每话段 FunASR task 轮换与确定性日期回答；同一真实 ESP32 连续完成“你好你好”和“今天星期几”两轮，用户确认均实际听清。该证据不外推为 Direct、AEC、双讲或完整 T1–T14 通过。
- [x] **wired（2026-08-14，Direct 单设备 canary 配置）**：Direct 精确 allowlist、独立 Device WSS 路由、Redis mTLS、Control ↔ Edge mTLS、Edge ↔ Voice Core mTLS 及 legacy 回滚路径已在候选 bundle 和配置门禁中闭合；当前工作机没有生产 `.env`/运行容器，`enabled` 状态未在本轮重新核验。
- [x] **code + local verified（2026-08-14，Direct v2）**：Voice Core 长生命周期 stream 已与有界握手 timeout context 解耦；v2 同时声明 `[16000, 24000]` 时优先协商目标板原生 `24 kHz`，Direct 下行帧和 sample clock 测试同步为 24 kHz。
- [x] **code + local verified（2026-08-15，Core event sequence）**：修复 Voice Core 在生产者入队时预分配事件序号、随后被优先级队列重排而导致 Edge 报 `stale core event sequence` 的问题；Generation/Transcript/State/Client/Shadow/Effect 现在统一在实际发送边界分配 wire sequence，16 个聚焦单测、Ruff 与 `git diff --check` 通过。
- [x] **wired + enabled（2026-08-15，Direct 服务器 canary）**：服务器候选 `memoria-agent:20260815-110524-event-sequence-canary` 已只重建 Agent 与 Voice Core；两个运行容器内 `grpc_bridge.py` SHA-256 均为 `1dd38c58f3e0fcc9c66140136331a06b45e3370da22c4f81555c9535a2dd5f62`。Control、Media Edge、LiveKit、PostgreSQL 容器 ID 未变化，切换后未再观察到 `stale core event sequence`；重启瞬间一次 `Cancelling all calls` 属旧 gRPC 流回收。
- [x] **code + local verified（2026-08-14，ESP32 JSON 边界解析）**：固件 `uint32/uint64` JSON 字段现在拒绝非数字、NaN、Infinity、小数、负数和越界值；`uint64` 使用 `2^64` 独占上界后才转换，源码门禁、overlay 检查和 ESP-IDF 6.0.2 clean build 均通过。
- [x] **code + local verified（2026-08-16，Media Turn 生命周期加固）**：Direct Voice Core 已把音频入口串行排空、VAD endpoint/tail、ASR final 缺失、Provider turn prepare 瞬态失败重试、输出生成超时、重连 epoch 与 Playback/Projection fence 收敛到同一话轮生命周期；重试保留原 sample range，只有新 VAD 才能显式 supersede，耗尽时 fail closed 丢弃而不合并下一话轮。Agent 全量测试、`test_media_session.py` 78 项、`test_speech_timeline.py` 5 项、硬件验收器 157 项、Ruff、strict MyPy（399 个源码文件）、模块预算和 `git diff --check` 均通过。
- [x] **wired + enabled + production runtime verified（2026-08-16，Media Turn retry canary）**：候选 `memoria-agent:20260816-214225-media-turn-retry-canary`（revision `9be3d18ed7f8ec1702cc52d674c813a549f48f71`，image ID `sha256:467b431bc80c5d6de00ffa2c578e20f0843e523ad971808654ea77bdf7d26acd`）于 `2026-08-16 22:19:13–22:20:01 CST` 切换 Agent 与 Media Bridge。两个容器 17/17 运行文件哈希一致，继承的生产 `media_agent_factory.py` SHA-256 均为 `c4c6004cbbd68665354743d51699faf801568266e7684c169538c47922307410`；Control、Media Edge、LiveKit、PostgreSQL、两个 Redis、MinIO、Device Gateway、Mini Program Gateway、Speaker Model 共 10 个保留容器 ID 未变化。延迟复核时三个关键容器均 healthy、Edge `/readyz` 为 ready，切换后 Traceback、stale sequence、ASR tail timeout、prepare retry exhausted、output timeout、ERROR、closed client、speaker 403 均为 0。
- [x] **rollback path exercised（2026-08-16）**：第一次切换于 `22:09:45 CST` 开始，Agent/Bridge healthy 但 Edge readiness 持续 503，约 `22:10:55 CST` 自动回滚到冻结镜像 `memoria-agent:rollback-20260816-214225-pre-media-turn-retry`（image ID `sha256:0453fa47c495e913ccede09ac3ab0d73caf46c4363c018e23019a51cddcada44`）并恢复 healthy。诊断确认旧 Edge 进程的长生命周期 gRPC channel 在 Bridge 容器 IP 变化后没有自动重解析/重拨；`22:14:59 CST` 重启同一个 Edge 容器后恢复，第二次切换和回滚流程因此纳入“目标 healthy 后同 ID 重启 Edge 完成重拨”。这只证明当前运维回滚可执行，不证明 Edge 已具备热替换自愈。
- [x] **code + wired + enabled + production runtime + production chaos verified（2026-08-16，零活跃会话范围）**：Python MediaBridgeGrpcServer 的标准 gRPC Health 只在 memoria.media.v1.VoiceMediaBridge 为 SERVING 时放行；Go Media Edge 的启动、运行期 readiness 与 replacement candidate 都以 Health RPC 为准，transport READY 本身不再放行。候选 revision 83af81318083b1977521a1fbdca8d9bbe51927bd 已只重建 Voice Core Bridge 与 Go Media Edge；Bridge 具名 Health 为 SERVING，Edge 私有 mTLS readyz 为 200，liveness probe success 由 6 增至 9、failures 为 0，设备连接与媒体会话均为 0。受保护演练中仅停止 Bridge，Edge readyz 变为 503、liveness 变为 0；恢复后 readyz 为 200、liveness 为 1、redial attempts/successes 均由 0 增至 1、channel generation 由 1 增至 2，未重放会话。随后按 Edge 先回退、Bridge 后回退并反向恢复候选的顺序完成双向回滚验证。它证明服务器零会话情况下的 fail-closed 与自动重拨，不证明 Direct 真机、AEC、双讲、DAC 或 Actual Heard。
- [x] **code + local verified（2026-08-16，废弃路径清理）**：小程序实时媒体、手机声纹入口及对应测试已物理删除；本轮再删除与现行 ESP32-first 架构冲突的独立旧进度文档 `docs/voice-architecture-progress.md`，并清理历史发布/ADR 与 `RealtimeFacade` 中把小程序媒体 Gateway 当作现行路径的表述。Python Device Gateway、Mini Program Gateway、legacy Nginx、H5 LiveKit 和 `services/legacy` 仍承担生产兼容或回滚职责，明确保留。
- [x] **code + local verified（2026-08-16T16:14:08Z，当前未刷板固件候选）**：从锁定 upstream `e8d8a4010788afd60f0c8aa3b2e3d0a7bb8f02e5` 完成 ESP-IDF 6.0.2 clean build、merge-bin 与 overlay gate。内容加相对路径的 overlay SHA-256 为 `513e14ef346d49fc39e82b9bf96e2c56df0bf25bb1d2f081ee56e5707dd6a969`；Memoria 应用 `2,950,832` bytes、SHA-256 `6fe8e936d481433f80fd0dcd5c076f2a5f47029132e2b9e21ccb1680fe3d798d`，应用分区余 `1,177,936` bytes（29%）；合并镜像 `13,577,086` bytes、SHA-256 `7d7b921a81500817fda6a8230cd5c5b5a97814855dd0a440188f3d75e2cee188`。写入表只含 `0x0/0x8000/0xd000/0x20000/0x800000`，不含 `0x10000` 身份区。该候选没有刷板，故 `identity preserved`、boot/activation、Direct 真机与全双工均保持未验证；2026-08-14 的身份区和 legacy 两轮可听证据只属于历史实板候选。
- [x] **production enabled + production chaos verified（2026-08-16，Edge Bridge 热替换）**：候选 supervisor 已在生产启用，并在 Bridge 停止/恢复时以应用层 Health fail closed、重建 channel 后自动恢复；恢复后 Edge mTLS readyz 为 200，liveness=1，redial attempts/successes=1，channel generation=2。已完成候选与即时回滚 Edge/Bridge 的双向编排回滚，期间 active_device_connections=0、active_media_sessions=0，非目标 Agent、Control API、Mini Program Gateway、Device Media Gateway 的启动时间未变化。告警送达、带活跃会话的恢复、Bridge IP 变化与 Edge 重启的完整矩阵仍需独立验证；不得外推为真机或全双工验收。
- [x] **enabled + production runtime verified（2026-08-16，Direct Device State Redis）**：当前生产 Direct 单 Edge 已配置独立 Redis URL 与 Redis mTLS 四件套，Media Edge healthy；生产 Direct 模式缺少 Redis 会 fail-closed 启动。该证据只证明单 Edge 共享权威已启用，不证明真实票据重放、跨实例接管、Redis HA、故障转移或混沌。
- [ ] **Direct real-device verified**：截至 `2026-08-17 14:34 CST`，生产 Edge 指标仍为 `active_device_connections=0`、`active_media_sessions=0`，没有本轮新的 VAD/uplink/FunASR final/LLM/TTS/playback 与用户 Actual Heard 闭环，结构化 T4–T7 回执仍未形成；不得把 healthy、readiness、错误计数为 0 或服务器重拨成功写成真机 verified。
- [ ] **full-duplex verified**：AEC Reference、Double-talk、自然抢话、Backchannel、停止词、Actual Heard 精确水位、100/500 轮稳定性及 T14 微信独立性尚未通过，不得宣传全双工。

发布留存纪律：服务器上每类上传包、构建归档和镜像只保留最近两个可用版本（当前版本 + 一个可运行回滚版本）；新版本核验后删除更早制品并复核磁盘占用。

---

## 0. 一页决策摘要

### 0.1 产品边界立即冻结

从本次整改开始，产品职责固定为：

| 终端/系统 | 正式职责 | 不再承担的职责 |
|---|---|---|
| 微信小程序 | 扫码、BLE/SoftAP 配网、设备 Claim/Binding/Activation、用户与家庭信息、Soul/Persona、权限与隐私、设备设置、会话记录、总结、周报、告警、OTA 状态、诊断 | 不采集实时麦克风、不播放实时 TTS、不建立实时语音 WSS、不加入 LiveKit 房间、不参与 VAD/打断/Generation |
| ESP32-S3 机器人 | 麦克风采集、扬声器播放、屏幕状态、AEC/NS/VAD/KWS、实时 WSS/Opus、播放回执、物理打断、断网重连 | 不持有 LLM/ASR/TTS 永久密钥，不决定用户数据权限，不在本地保存完整人格和长期记忆 |
| Control API | 账号、设备、绑定、主体、Soul/Persona、Policy、Runtime Profile、会话令牌、设置、总结与设备控制 | 不搬运实时音频 |
| Hardware Media Edge | 设备 WSS 终结、设备票据验证、Opus/PCM、Sample Clock、背压、重连 Epoch、Generation Gate、播放回执转发 | 不做 Persona、记忆、业务授权和 LLM 决策 |
| Python Voice Core / Agent | ASR、话轮协调、打断语义、LLM、TTS、Generation、实际已听文本、Policy/Memory/Persona 编排 | 不依赖小程序 UI 事件才能输出音频 |
| H5 | 内部回归、算法对照、诊断和开发参考；是否长期保留由产品另行决定 | 不再是硬件产品的运行依赖 |

### 0.2 最终目标链路

```text
微信小程序（控制面）
  ├─ 扫码 / 配网 / Claim / Binding / Activation
  ├─ Soul / Persona / 主体 / 权限 / 隐私
  ├─ 设备设置 / OTA / 诊断
  └─ 会话记录 / 总结 / 周报 / 风险提醒
                  │ HTTPS
                  ▼
            Memoria Control API
                  │
                  ├──────── Runtime Profile / 短期媒体票据 ────────┐
                  │                                               │
                  ▼                                               ▼
ESP32-S3 ── WSS + Opus + 控制事件 ──> Go Hardware Media Edge ── gRPC media-v1 ──> Python Voice Core
  ▲                                    │                                 │
  └──── Opus + generation/flush ───────┘                                 ├─ ASR
                                                                         ├─ Interruption Policy
                                                                         ├─ LLM
                                                                         ├─ TTS
                                                                         ├─ Policy / Memory / Persona
                                                                         └─ Playback Ledger / Actual Heard
```

### 0.3 核心技术决策

1. **停止让 ESP32 复用 `MiniProgramLiveKitBridge`。**
2. **不要再新建第二套 AI 后端。**直接复用仓库已有的 `media-v1`、Go `media_edge` 与 Python Voice Core gRPC seam。
3. Go Media Edge 先只承担媒体权威；会话与交互决策继续保持 `PYTHON_AUTHORITATIVE`，不需要同时推动 Go 成为对话权威。
4. 设备线协议继续使用轻量的 **WSS + 版本化 JSON 控制帧 + `MemoriaAudioFrameV1` 二进制 Opus 帧**；Edge 与 Voice Core 之间使用已有 Protobuf/gRPC。
5. 第一目标不是宣传“绝对全双工”，而是完成**可靠可打断**：按钮硬停、离线停止词、AEC 后语音抢话、语义区分附和与真正打断。
6. 没有经过 AEC 双讲测试，不得在产品文案中声称全双工。
7. 当前板可继续使用。现有源码证明 ES8388 同时具有输入/输出 I2S 引脚，但当前固件尚未形成经验证、对齐到扬声器真实播放的 AEC Reference；这是一项需要实测的工程门，而不是简单打开一个配置项。

### 0.4 当前建议状态

```text
当前兼容路径：Hardware Compatibility Demo
整改目标状态：Hardware Realtime Voice Candidate
公开宣称全双工前：必须通过 AEC / Double-talk / 远场 / 儿童声学矩阵
```

---

## 1. 当前源码事实与问题边界

### 1.1 已经存在且应保留的能力

当前仓库已经具备：

- ESP32 独立设备身份、Ed25519、Activation Manifest、一次性 media challenge/session；
- `MemoriaAudioFrameV1`，包含 `stream_epoch`、`sequence`、`sample_start`、`frame_samples`、`generation_id`；
- 上行 Opus 16 kHz/20 ms、下行 Opus 24 kHz/20 ms；
- 设备 VAD、播放开始/进度/结束回执；
- 实板已经到达设备 WSS、麦克风上行、FunASR final 和话轮提交；
- `packages/proto/memoria/media/v1/media.proto` 已定义完整的 Media ↔ Voice Core 契约；
- Go `services/media_edge` 已有 Opus、Sample Clock、Generation Gate、Reconnect Epoch、gRPC Voice Core Bridge、指标和会话 Actor；
- Python Voice Core 已具备 Generation、Speech Timeline、Playback Ledger、Stop/KWS/VAD 等基础对象；
- H5 端已经验证过可打断的语义与 Generation Fence，这些**语义规则可以提取复用**。

因此本次整改不是从零重做，而是：

```text
保留：设备身份、协议不变量、业务域、Voice Core、Provider、记忆与 Persona
移除：硬件必须经过小程序媒体桥和小程序 UI 状态的耦合
```

### 1.2 当前硬件链路的结构性错配

当前代码中的事实：

```python
# services/device_media_gateway/session.py
"""Device session adapter: Memoria protocol ↔ existing Mini Program bridge."""
```

设备网关当前：

- 导入并实例化 `MiniProgramLiveKitBridge`；
- 将设备票据投影成“小程序用户会话”形状；
- `DeviceMediaGatewaySettings` 继承 `MiniProgramGatewaySettings`；
- 将设备 VAD 转为 LiveKit Data 事件；
- 从小程序 UI 事件反推硬件端状态；
- 因内部 Agent `generation_id=0` 与设备“0 表示无播放代次”冲突，做 `N → N+1` 映射；
- 设备真实音频、LiveKit、AgentSession、小程序桥分别持有部分状态。

这条路径适合快速证明“硬件能进入现有语音系统”，但不适合成为正式硬件产品架构。

### 1.3 小程序当前仍是实时语音客户端

当前小程序首页仍：

- 引入 `MiniProgramMediaSession`；
- 请求 `scope.record`；
- 有 `startVoice()`、连接、监听、思考、说话状态；
- 使用 `media-gateway.js`、`media-protocol.js`、`pcm-player.js`、`transcript-events.js`；
- README 仍描述 `RecorderManager → PCM/WSS → MiniProgramMediaGateway → LiveKit Agent`。

这与新的产品定位冲突。整改后不应只是“界面隐藏语音按钮”，而应从代码、权限、网络和验收四个层面切断实时语音能力。

---

## 2. 新产品定义

### 2.1 小程序不是“机器人遥控麦克风”

小程序正式定义为：

> **机器人控制台、家庭账户入口和长期信息查看器。**

它负责以下七类任务：

1. **设备启用**：扫码、BLE/SoftAP、Wi-Fi、Claim、Binding、Activation；
2. **人物初始化**：给孩子、给自己、给父母、家庭共享；
3. **Soul 与 Persona**：机器人名称、角色、沟通风格、主动程度、教学风格；
4. **权限与隐私**：记忆范围、家长可见范围、说话人登记、声音复刻单独授权；
5. **设备管理**：在线状态、音量、屏幕亮度、勿扰、夜间、网络、解绑、转赠、OTA；
6. **内容查看**：会话列表、摘要、人生时间线、学习进度、情绪趋势、周报；
7. **运维诊断**：固件版本、最近在线、媒体质量、错误码、诊断上报。

### 2.2 ESP32 是唯一面向用户的语音终端

初始化完成后：

- 小程序可以关闭；
- 手机可以离开局域网；
- ESP32 可独立获取短期媒体票据；
- ESP32 可直接发起、保持和恢复语音会话；
- 小程序不作为媒体中转；
- 小程序掉线不影响机器人对话；
- 小程序修改设置后通过版本化 Runtime Profile 生效。

### 2.3 首页信息架构建议

小程序主导航建议改为：

```text
首页        设备        回顾        我的
```

首页不再展示“开始语音/文字对话”，而展示：

- 当前机器人在线状态；
- 当前 Soul/Persona；
- 主要使用者；
- 今日对话次数和最近一次摘要；
- 今日学习/陪伴概览；
- 需要家长处理的提醒；
- 快捷设置：勿扰、夜间、音量、学习模式；
- “机器人不在线”诊断入口。

---

## 3. 目标系统架构

### 3.1 控制面

```text
Mini Program
    │ HTTPS + Account Session
    ▼
Control API
    ├─ Identity / Subject / Guardian
    ├─ Device Fleet / Claim / Binding / Activation
    ├─ Soul / Persona / Runtime Profile
    ├─ Consent / Policy / Capability
    ├─ Conversation Index / Summary / Review
    ├─ Device Setting / OTA / Diagnostic
    └─ Media Challenge / Short-lived Device Ticket
```

控制面原则：

- 所有设置写入权威数据库；
- 每次变更产生版本；
- 设备只获取当前会话所需的最小配置；
- 敏感设置变更需要重新鉴权或监护人确认；
- 小程序不获得设备媒体长期密钥；
- ESP32 不获得用户 Access Token。

### 3.2 媒体面

```text
ESP32 Audio Capture
    ├─ Mic PCM
    ├─ Playback Reference PCM
    ├─ AEC / NS / VAD / KWS
    ├─ Opus Encoder
    └─ Sample Clock
          │ WSS
          ▼
Go Hardware Media Edge
    ├─ Device Ticket/Auth
    ├─ JSON Control Priority Lane
    ├─ Binary Audio Lane
    ├─ Opus Decode/Encode
    ├─ Sequence/Sample Validation
    ├─ Stream Epoch
    ├─ Generation Gate
    ├─ Backpressure
    └─ Playback Receipt Mapping
          │ mTLS gRPC media-v1
          ▼
Python Voice Core（权威）
    ├─ Speech Timeline
    ├─ ASR Supervisor
    ├─ Turn Coordinator
    ├─ Interruption Policy
    ├─ Generation Controller
    ├─ Playback Ledger / Actual Heard
    └─ Agent/LLM/TTS/Policy/Memory/Persona Adapter
```

### 3.3 责任边界

| 事实 | 唯一权威 |
|---|---|
| 麦克风采样位置、实际 DAC 播放水位、物理按钮、物理静音 | ESP32 |
| 网络顺序、Opus/PCM、Stream Epoch、背压、Generation Gate | Media Edge |
| 逻辑话轮、Turn ID、Generation ID、打断语义、是否取消 LLM/TTS | Voice Core |
| 用户是谁、能否读取记忆、当前 Soul、监护与权限 | Control/Policy |
| 哪段回复真正被听到 | ESP32 Playback Watermark + Voice Core Playback Ledger |
| 小程序界面状态 | 服务端投影，不参与音频控制 |

关键规则：

> UI 状态只能观察音频生命周期，不能反向决定音频是否可播放。

---

## 4. 推荐代码结构

### 4.1 Go Media Edge 增加硬件 WSS 入口

在现有 `services/media_edge` 中增加：

```text
services/media_edge/
├── device_ws_server.go
├── device_ws_router.go
├── device_ws_auth.go
├── device_ws_protocol.go
├── device_ws_session.go
├── device_ws_uplink.go
├── device_ws_downlink.go
├── device_ws_control_lane.go
├── device_ws_audio_lane.go
├── device_capabilities.go
├── device_playback_ledger.go
├── device_backpressure.go
├── device_metrics.go
└── device_ws_*_test.go
```

复用现有：

```text
bridge.go
opus_decoder.go
opus_encoder.go
session.go
session_generation.go
session_downlink.go
live_session_actor*.go
media_vad.go
JWT / JWKS / mTLS / Prometheus
```

### 4.2 Python Voice Core 收敛硬件无关语义

建议目标结构：

```text
services/voice_core/
├── transport_contracts.py
├── media_bridge_server.py
├── media_session.py
├── speech_timeline.py
├── asr_supervisor.py
├── turn_coordinator.py
├── interruption/
│   ├── policy.py
│   ├── evidence.py
│   ├── backchannel.py
│   ├── hard_stop.py
│   └── thresholds.py
├── generation/
│   ├── controller.py
│   ├── fence.py
│   └── cancellation.py
├── playback/
│   ├── ledger.py
│   ├── actual_heard.py
│   └── receipts.py
├── agent_adapter.py
├── provider_adapter.py
└── tests/
```

H5 已实现的打断经验应提取为**传输无关规则**，而不是调用 H5 或 LiveKit UI 代码。

### 4.3 当前 Python `device_media_gateway` 的处理

短期保留为回滚兼容路径，但标记：

```text
DEVICE_MEDIA_RUNTIME=livekit_compat | direct_voice_core
```

规则：

- 默认研发环境可切换；
- 新路径通过门禁后生产默认 `direct_voice_core`；
- `livekit_compat` 只作回滚；
- 禁止继续向 `MiniProgramBridgeAdapter` 添加业务功能；
- 新协议字段必须先进入公共契约，不在两个 Gateway 中分别定义；
- 最终删除 `gateway_claims(device → mini program user session)` 与 `N+1` Generation 映射。

### 4.4 ESP32 固件结构

```text
firmware/esp32/overlay/files/main/memoria/
├── identity/
├── activation/
├── provisioning/
├── runtime_profile/
├── media/
│   ├── media_client.cc
│   ├── media_protocol.cc
│   ├── media_session.cc
│   ├── uplink_encoder.cc
│   ├── downlink_player.cc
│   ├── control_priority_queue.cc
│   ├── sample_clock.cc
│   ├── playback_watermark.cc
│   └── reconnect_epoch.cc
├── audio_frontend/
│   ├── capture_pipeline.cc
│   ├── playback_reference.cc
│   ├── afe_pipeline.cc
│   ├── aec_calibration.cc
│   ├── vad_observer.cc
│   ├── stop_keyword.cc
│   └── barge_in_detector.cc
├── interruption/
│   ├── local_duck.cc
│   ├── local_flush.cc
│   └── interrupt_event.cc
├── ui/
├── ota/
└── diagnostics/
```

原则：不要继续把所有逻辑塞回上游 `Application` 大状态机；通过 overlay 保持可重建，但 Memoria 领域逻辑要有独立模块。

### 4.5 小程序删除或退出生产依赖的代码

以下模块不应再进入正式产品链：

```text
apps/miniprogram/utils/media-gateway.js
apps/miniprogram/utils/media-protocol.js
apps/miniprogram/utils/pcm-player.js
apps/miniprogram/utils/transcript-events.js（若只服务实时流）
首页中的 MiniProgramMediaSession
首页中的 scope.record 授权
startVoice / startText 实时会话路径
实时语音 WSS 域名与相关配置
```

处理方式：

1. 第一 PR 先通过 feature flag 关闭；
2. 小程序真机确认无调用后删除；
3. 只保留会话记录 API 的普通 HTTP 查询；
4. CI 增加静态门禁，禁止 `wx.getRecorderManager`、`scope.record`、媒体 Gateway URL 重新进入生产包。

---

## 5. 小程序整改清单

### 5.1 页面与能力

#### A. 首页 Dashboard

- [x] **code + wired + local verified（2026-08-16）**：删除实时语音和实时文字会话入口；静态门禁覆盖生产页面与工具目录。
- [x] **code + wired + local verified（2026-08-16）**：删除麦克风权限申请、`RecorderManager` 与手机声纹录取入口；微信生产包不再声明录音权限。
- [ ] 展示设备在线/离线、最近心跳、网络质量
- [x] **code + wired + local verified（2026-08-16）**：展示当前伙伴名称、Soul/Persona 与服务端权威主要使用者状态。
- [x] **code + wired + local verified（2026-08-16）**：只在 `memory_recall_private` 权限通过后展示最近对话摘要，不展示完整敏感原文。
- [ ] 展示学习/陪伴/情绪的低风险概览
- [ ] 展示未处理通知和设备异常

#### B. 设备启用

- [x] **code + wired + local verified（2026-08-16）**：设备二维码执行版本化 schema 校验，未知字段与不支持版本 fail closed。
- [x] **code + wired + local verified（2026-08-16）**：Bootstrap Session 过期会阻止 Claim，并保留可解释错误；跨上下文重放由 session/claim fence 拒绝。
- [ ] BLE/SoftAP 配网
- [x] **code + wired + local verified（2026-08-16）**：页面明确提示仅使用 2.4 GHz Wi-Fi。
- [x] **code + wired + local verified（2026-08-16）**：Wi-Fi 密码只进入可清零的内存缓冲；未建立经审计安全会话时禁止写入设备。
- [ ] 设备联网状态轮询
- [x] **code + wired + local verified（2026-08-16）**：Claim 由服务端保留并以 `claim_id + onboarding_session_id` 一次性绑定，绑定页禁止重新扫码或接收裸设备码。
- [ ] Binding Saga 可恢复
- [x] **code + wired + local verified（2026-08-16）**：Activation Manifest 状态仅由服务端投影，收到 `device_acknowledged` 或 `ready_for_conversation` 才显示完成。
- [x] **code + wired + local verified（2026-08-16）**：本地保存未完成 onboarding session ID，设备页提供继续办理入口并对迟到响应做 epoch/fence 拦截。

#### C. Soul/Persona

- [ ] 机器人名称
- [ ] 基础人格模板
- [ ] 语气、主动程度、幽默度、解释深度
- [ ] 学习导师风格
- [ ] 儿童/成人/适老/家庭服务模式
- [x] **code + wired + local verified（2026-08-16）**：页面明确 Soul 影响“怎么说”，Policy 决定“能不能做”。
- [x] **code + wired + local verified（2026-08-16）**：设备设置按 `expectedVersion` 更新，只接受服务端确认的新 settings/runtime profile 版本。
- [ ] 敏感能力重新确认授权

#### D. 人物与家庭

- [x] **code + wired + local verified（2026-08-18）**：主要使用者来自服务端 nullable `active_subject_id`，切换必须获得严格递增的 session epoch；非空 `subject_id` 仅表示绑定/账号归属，`active_subject_id=NULL` 表示完整 `unknown_safe`，不得作为 wildcard。
- [x] **code + wired + local verified（2026-08-16）**：绑定负载区分账号所有者、设备管理员与主要数据主体。
- [ ] 家庭成员邀请与确认
- [ ] 说话人登记状态
- [ ] 未知说话人降级策略可视化
- [ ] 儿童监护、可见范围、时长与夜间设置

#### E. 设备设置

- [x] **code + wired + local verified（2026-08-16）**：音量、屏幕亮度、勿扰、夜间设置经版本化 HTTPS API 更新。
- [x] **code + wired + local verified（2026-08-16）**：设备页展示并更新服务端允许的唤醒/会话模式。
- [x] **code + wired + local verified（2026-08-16）**：设备页只在服务端允许集合内配置打断方式。
- [ ] 物理静音状态只读展示
- [ ] 固件版本、升级、回滚状态
- [ ] 网络重配、解绑、转赠、恢复出厂说明

#### F. 回顾与总结

- [ ] 会话列表按主体和设备过滤
- [ ] 每次会话摘要
- [x] 用户实际听到的助手内容与“生成但未播放”分开（Control API `code + wired + enabled + production runtime verified`；小程序 `code + wired + local verified`、未发布；缺少精确 DAC 证据的记录明确标为 approximate，2026-08-17）
- [x] 记忆候选、已确认记忆和推断分开（Control API 候选/确认分区已生产启用，local capability 矩阵通过；小程序未发布；确认列表仅消费 confirmed claim，2026-08-17）
- [ ] 日报、周报、学习进度、人生时间线
- [ ] 原始录音默认不对普通页面开放

#### G. 诊断

- [ ] 最近连接时间
- [ ] **partial code + wired + local verified（2026-08-16）**：已展示服务端权威 Firmware、Board 与 Runtime Profile 版本；Protocol 完整版本字段仍待服务端补齐，因此本项不勾选。
- [x] **code + wired + local verified（2026-08-16）**：展示配置模式、服务端允许模式与已连接设备的实际生效模式，未连接时不伪造。
- [x] **code + wired + local verified（2026-08-16）**：展示服务端声学档案中的 AEC 验收状态、参考类型与档案版本。
- [ ] 上行丢包、下行欠载、播放 ACK、重连次数
- [ ] 一键生成脱敏诊断包

### 5.2 小程序验收标准

必须同时满足：

```text
1. 微信后台权限列表中无麦克风必选权限；
2. 冷启动、首页、回顾、设备页不会创建 RecorderManager；
3. 不连接 MiniProgramMediaGateway 和 LiveKit；
4. 小程序关闭后，机器人持续完成对话；
5. 手机切换网络后，不中断机器人媒体会话；
6. 设置变更通过版本化 Runtime Profile 在设备下一安全点生效；
7. 小程序只显示权威持久化记录，不伪装成实时音频状态机。
```

---

## 6. 设备启用与独立运行流程

### 6.1 完整用户流程

```text
ESP32 上电
  → 无 Activation 时显示动态二维码 + 备用短码
  → 小程序扫码
  → 建立 Bootstrap Session
  → BLE/SoftAP 配网
  → 设备连云并提交在线证明
  → 登录账号一次性 Claim
  → 选择给孩子/自己/父母/家庭
  → 创建主体与 Binding
  → 设置 Soul/Persona/隐私/时长
  → 可选说话人登记
  → 服务端签发 Activation Manifest
  → ESP32 验签并 ACK
  → ESP32 获取 Runtime Profile
  → ESP32 申请一次性 media challenge/ticket
  → ESP32 直连 Hardware Media Edge
  → 小程序可以退出
  → 用户直接与机器人对话
```

### 6.2 会话启动

建议：

```text
设备保持轻量在线心跳
→ 用户短按或唤醒词
→ 申请/刷新短期媒体票据
→ WSS device.hello
→ 服务器 session.accepted
→ 进入 listening
```

是否长期保持媒体 WSS，应通过真实功耗和网络测试决定：

- 插电设备：可保持长连接；
- 电池设备：使用控制长连接 + 按需媒体连接；
- 4G 设备：需考虑 NAT、心跳和流量成本。

### 6.3 Runtime Profile 同步

每个 Profile 至少包括：

```json
{
  "profile_version": 27,
  "binding_version": 8,
  "subject_id": "...",
  "service_mode": "adult_self",
  "persona_id": "...",
  "soul_version": 12,
  "policy_version": 31,
  "audio_mode": "interrupt_assist",
  "volume_limit": 72,
  "night_mode": true,
  "memory_scope": "private",
  "expires_at": "...",
  "signature": "..."
}
```

ESP32 不需要下载完整人格 Prompt，只需：

- 显示名称和表情资源版本；
- 音频模式；
- 物理交互配置；
- 允许的本地命令；
- 服务端所需的版本摘要。

完整 Persona、Memory 和 Policy 保持服务端权威。

---

## 7. 设备线协议整改

### 7.1 两层协议

#### 设备 ↔ Edge

- WSS；
- 文本帧：版本化 JSON 控制消息；
- 二进制帧：`MemoriaAudioFrameV1` + Opus；
- 控制队列优先级高于音频队列；
- 单个 WebSocket 消息不得分片；
- 限制消息大小和速率。

#### Edge ↔ Voice Core

- 复用 `memoria.media.v1.VoiceMediaBridge.Connect` 双向 gRPC；
- mTLS；
- 复用 `SessionIdentity`、`AudioFrame`、`VadEvent`、`KeywordEvent`、`PlaybackProgress`、`GenerationControl`。

### 7.2 `device.hello` 能力协商扩展

当前 capability 只有 display/microphone/speaker/device_aec/physical_button，建议升级为版本 2：

```json
{
  "type": "device.hello",
  "version": 2,
  "device_id": "dev_xxx",
  "firmware_version": "0.2.0",
  "board_profile": "memoria-atk-dnesp32s3-v1",
  "stream_epoch": 18,
  "audio": {
    "uplink_codec": "opus",
    "uplink_sample_rate": 16000,
    "downlink_sample_rates": [16000, 24000],
    "channels": 1,
    "frame_ms": 20
  },
  "capabilities": {
    "display": true,
    "microphone": true,
    "speaker": true,
    "simultaneous_capture_playback": true,
    "aec_mode": "fd_low_cost",
    "aec_reference": "software_post_gain_pre_i2s",
    "aec_reference_verified": false,
    "local_vad": true,
    "local_stop_keyword": true,
    "physical_stop_button": true,
    "playback_watermark": "exact",
    "local_duck": true,
    "barge_in_level": 1
  }
}
```

服务端根据能力选择：

```text
full_duplex_verified
interrupt_assist
half_duplex_safe
```

不能因为设备布尔值写了 `device_aec=true` 就自动开放全双工；必须同时满足服务器登记的板型声学验收版本。

### 7.3 Generation ID

最终统一：

```text
0 = 当前没有有效 Generation
1 = 第一代回复
2 = 第二代回复
...
```

禁止继续让业务日志长期存在 `Agent N ↔ Device N+1`。

迁移方式：

1. Voice Core 新会话从 1 开始；
2. 旧 H5/LiveKit Adapter 内部做兼容；
3. 新设备直连不做映射；
4. 全链路日志显示同一个 Generation；
5. Playback Receipt 必须携带 `turn_id + generation_id + tool_epoch`。

### 7.4 Sample Clock

- 上行 Sample Clock 由设备采集端产生；
- 网络重传不得改变 sample 位置；
- 丢帧由 Edge 标记 concealment，不伪装成干净语音；
- VAD end 同时上报事件位置和 `voiced_end_sample`；
- 下行播放水位按真正送入 DAC 的样本累计；
- 重连必须增加 `stream_epoch`；
- 只清空队列但不增加 Epoch 是不合格实现。

### 7.5 控制事件

#### 设备上行

```text
session.open
audio.frame
vad.start
vad.end
keyword.detected
button.stop
playback.started
playback.progress
playback.ended
playback.error
device.mute_changed
device.network_changed
device.telemetry
session.close
```

#### 服务端下行

```text
session.accepted
conversation.state
generation.started
assistant.audio.started
assistant.audio.frame
generation.pause
generation.resume
generation.cancelled
generation.completed
playback.duck
playback.flush
screen.expression
screen.subtitle
runtime_profile.invalidated
session.error
```

### 7.6 队列与背压

优先级：

```text
P0：playback.flush / generation.cancel / session.close / mute
P1：state / VAD / keyword / playback receipt
P2：下行音频
P3：subtitle / expression / telemetry
```

规则：

- P0 不得被音频阻塞；
- 下行音频缓冲目标 80–200 ms，禁止累计几秒旧 TTS；
- 缓冲超过阈值时优先丢弃旧 Generation，而不是增加播放延迟；
- 任何旧 Generation 音频到达设备时立即丢弃；
- 网络恢复后只恢复当前 Generation，不重放中断前的旧队列。

---

## 8. “接近全双工、支持打断”的实现路线

### 8.1 产品语言先统一

定义四个等级：

| 等级 | 能力 | 可对外描述 |
|---|---|---|
| L0 | 机器人说话时按键立即停止 | 支持按键打断 |
| L1 | 播放中识别本地停止词并停止 | 支持语音停止 |
| L2 | AEC 后检测用户抢话，服务端区分附和和真正打断 | 支持自然打断、接近全双工 |
| L3 | 多音量、多距离、双讲稳定，用户可在机器人说话期间持续发言 | 经验收的全双工实时对话 |

第一版公开目标建议为 **L2**，内部工程目标为 L3。

### 8.2 不要把“任何 VAD”当作打断

机器人播放时，麦克风会收到：

- 扬声器回声；
- 用户真正抢话；
- 用户“嗯、对、好”的附和；
- 电视或旁人说话；
- 移动、碰撞和风噪。

因此：

```text
VAD = 声学观察
Interrupt = Voice Core 根据多种证据做出的逻辑决策
```

### 8.3 三类打断路径

#### A. 物理硬停

```text
用户按键
→ ESP32 立即清空本地播放队列并静音
→ 发送 button.stop(expected fence)
→ Edge 原子关闭 Generation Gate
→ Voice Core 取消 TTS/LLM/Tool
→ 新 Generation Fence
```

目标：本地可听停止 P95 ≤ 150 ms，端到端确认 P95 ≤ 300 ms。

#### B. 离线停止词硬停

例如“停一下”“等等”“先别说”。

```text
AEC/AFE 输出
→ 本地 KWS
→ 高置信 hard_stop
→ 本地 flush
→ keyword.detected(hard_stop=true)
→ 同物理硬停流程
```

停止词必须使用离线模型，不能等待云端 ASR 才停止。

#### C. 自然语音抢话

```text
播放期间持续采集
→ AEC/NS
→ VAD 产生 barge_in_candidate
→ 本地先 duck 12–18 dB，不立刻全量清空
→ 上传 AEC 后音频与证据
→ Voice Core 结合持续时长、ASR prefix、说话人、回声置信度和上下文判断
   ├─ backchannel：继续播放或短暂停后恢复
   ├─ false positive：恢复音量
   └─ true interrupt：cancel generation + flush + 接受新话轮
```

在 AEC 充分验证后，可对高置信用户语音直接本地 flush，以降低停止延迟。

### 8.4 打断证据模型

```python
class InterruptionEvidence:
    session_id: str
    stream_epoch: int
    active_generation_id: int
    detected_sample: int
    source: str              # button | local_kws | local_vad | cloud_asr
    aec_mode: str
    aec_verified: bool
    vad_probability: float
    near_end_rms: float
    far_end_rms: float
    residual_echo_score: float | None
    speaker_class: str
    asr_prefix: str | None
    duration_ms: int
    device_monotonic_ms: int
```

### 8.5 附和与真正打断

建议共享 `InterruptionPolicy` 处理：

- 极短“嗯/啊/对/好”且没有继续说话：默认 backchannel；
- “等等/停/不是/你先听我说”：hard interrupt；
- 超过 300–500 ms 的连续近端语音：倾向 true interrupt；
- 儿童拖长音、笑声、哭声不能简单按成人阈值；
- 当前回复为安全警示或危机固定语时，Policy 可以禁止普通语义打断，但物理静音始终有效；
- 旁人语音在未确认说话人时可以停止公开回答，但不能读取私人上下文。

阈值只能作为初始值，必须由真机数据校准。

### 8.6 打断后的正确性

一旦确认打断：

1. 设备立即停止旧 Generation；
2. Edge 关闭旧 Gate；
3. Voice Core 取消 TTS provider 流；
4. 取消尚未完成的 LLM/tool；
5. `generation_id` 单调递增；
6. Playback Ledger 以设备最后水位计算实际听到文本；
7. 未播放内容不得进入“用户已听历史”；
8. 新用户话轮不能混入旧音频尾巴；
9. 迟到的旧帧全部拒绝；
10. 屏幕状态以新 Generation 投影。

---

## 9. AEC 与当前开发板的专项整改

### 9.1 当前事实

当前板型源码显示：

- ES8388；
- I2S `DIN` 与 `DOUT` 同时存在；
- 输入和输出当前都配置为 24 kHz；
- 当前 Device 协议上行 16 kHz、下行 24 kHz；
- 代码没有展示一个已校准的 `R` 播放参考通道进入 ESP-SR AFE；
- 当前仓库也明确把 AEC/稳定全双工作为未完成真机门禁。

结论：

> 当前硬件**可能具备实现软件 AEC 的基础**，但当前固件没有证明“麦克风信号与真正送入扬声器的参考信号在同一采样时钟下正确对齐”。不能只设置 `device_aec=true`。

### 9.2 推荐 AEC 数据路径

第一阶段为了降低复杂度，建议使用 16 kHz 全链路声学 Profile：

```text
TTS/下行 PCM 16 kHz
→ 数字音量/限幅
→ 一路送 I2S/DAC
→ 同一份 PCM 作为 Reference

Mic 16 kHz
+ Reference 16 kHz
→ ESP-SR AFE_TYPE_FD / AEC_MODE_FD_LOW_COST
→ NS/VAD/KWS/Opus
```

当 16 kHz 全双工稳定后，再升级：

```text
下行 24 kHz 播放
→ 确定性 24→16 kHz Reference Resampler
→ 与 Mic 16 kHz 对齐
→ AEC
```

这样可以避免第一轮同时解决跨采样率、时钟漂移和 AEC。

### 9.3 Reference 必须从正确位置取得

推荐位置：

```text
TTS PCM
→ 解码
→ 数字音量
→ 数字 EQ/限幅
→ [在这里复制给 AEC Reference]
→ I2S DMA
→ ES8388 DAC
→ PA/扬声器
```

约束：

- AEC Reference 之后不要再做不可知的数字音量变化；
- Codec/PA 模拟增益尽量固定；
- 用户音量优先在 Reference tap 之前数字处理；
- 若 PA 或扬声器非线性很强，需要 NLP、结构优化或下一版硬件参考路径；
- Reference 与 Mic 必须有统一样本计数和可调延时；
- 播放 underflow、flush、重连必须同步更新 Reference 时间线。

### 9.4 ESP-SR 配置建议

Bring-up 起点：

```text
AFE_TYPE_FD
AEC_MODE_FD_LOW_COST
1 Mic + 1 Reference
16 kHz / int16
VADNet enabled
NS enabled
WakeNet/停止词按产品策略启用
```

Espressif 官方资料明确将 FD 模式用于全双工对话，并要求 `M` 麦克风与 `R` 播放参考通道。具体资源占用仍需在本 ESP32-S3、现有 PSRAM、Opus 和 LVGL 同时运行时重新测量。

### 9.5 AEC 真机验收矩阵

必须覆盖：

| 场景 | 条件 |
|---|---|
| Far-end only | 机器人播放、用户不说话，VAD 不应频繁触发 |
| Near-end only | 机器人不播放，成人/儿童正常识别 |
| Double-talk | 机器人播放时用户插话，近端语音应保留 |
| 音量 | 20%、50%、80%、100% |
| 距离 | 0.3 m、1 m、2 m、3 m |
| 方位 | 正面、侧面、背面 |
| 环境 | 安静、电视、音乐、风扇、餐厅噪声 |
| 声音 | 成年男/女、儿童、老人、轻声、快速语速 |
| 内容 | 普通抢话、停止词、短附和、连续长句 |

建议指标：

- Far-end only 误打断率；
- Double-talk 近端 ASR 字错率；
- 残余回声能量/ERLE；
- AEC 后音频削弱程度；
- 停止词命中率与误触发率；
- 不同音量下的中断 P95；
- CPU、内部 RAM、PSRAM、看门狗、音频欠载。

### 9.6 AEC 失败时的产品降级

运行时必须支持：

```text
full_duplex_verified
  ↓ 声学异常/资源不足
interrupt_assist
  ↓ AEC 不可用
half_duplex_safe
```

即使降级也必须保留：

- 物理按钮立即停止；
- 本地停止词；
- 不播放旧 Generation；
- 不把回声识别成用户永久记忆；
- 小程序展示当前实际音频模式。

---

## 10. 服务端 API 与数据合同

### 10.1 建议 API

#### 设备控制面

```text
POST   /v1/device-bootstrap-sessions
POST   /v1/device-claims
POST   /v1/device-bindings
GET    /v1/device-activations/{id}
POST   /v1/device-activations/{id}/ack
GET    /v1/devices/{device_id}
PATCH  /v1/devices/{device_id}/settings
POST   /v1/devices/{device_id}/wifi-reset-intents
POST   /v1/devices/{device_id}/unbind-intents
POST   /v1/devices/{device_id}/transfer-intents
```

#### Runtime Profile

```text
GET    /v1/devices/{device_id}/runtime-profile
POST   /v1/devices/{device_id}/runtime-profile/ack
GET    /v1/devices/{device_id}/runtime-profile/changes?after_version=N
```

#### 媒体会话

```text
POST   /v1/devices/{device_id}/media-challenges
POST   /v1/devices/{device_id}/media-sessions
DELETE /v1/devices/{device_id}/media-sessions/{session_id}
```

#### 记录与总结

```text
GET    /v1/conversations?subject_id=&device_id=&cursor=
GET    /v1/conversations/{session_id}
GET    /v1/conversations/{session_id}/summary
GET    /v1/reviews/daily
GET    /v1/reviews/weekly
GET    /v1/learning/progress
GET    /v1/devices/{device_id}/diagnostics/latest
```

### 10.2 媒体票据

短期票据至少绑定：

```text
audience
type
ticket_id
device_id
client_id
account_id
binding_id
subject_id
session_id
stream_epoch
runtime_profile_version
protocol_version
issued_at
expires_at
```

要求：

- 30–300 秒有效；
- 一次性激活或单连接租约；
- 不能跨设备；
- 不能跨 Binding；
- 不能使用用户 JWT 替代；
- ESP32 不保存 Provider 密钥；
- 支持 JWKS/EdDSA 轮换。

### 10.3 设备会话数据

建议持久化：

```text
device_media_session
  session_id
  device_id
  binding_id
  subject_id
  stream_epoch
  protocol_version
  firmware_version
  board_profile
  runtime_profile_version
  audio_mode_requested
  audio_mode_effective
  aec_profile_version
  connected_at
  closed_at
  close_reason
```

### 10.4 声学能力登记

```text
device_acoustic_capability
  board_profile
  firmware_version_range
  acoustic_profile_version
  simultaneous_capture_playback
  aec_reference_type
  aec_verified
  max_barge_in_level
  tested_volume_range
  tested_distance_m
  test_report_uri
  approved_at
  revoked_at
```

服务器必须以此登记结果，而不是设备自报值，决定是否开放全双工。

---

## 11. 唯一状态机与生命周期

### 11.1 设备状态

```text
BOOTING
→ UNPROVISIONED
→ PROVISIONING
→ ACTIVATING
→ IDLE
→ CONNECTING_MEDIA
→ LISTENING
↔ USER_SPEAKING
→ FINALIZING
→ THINKING
→ ASSISTANT_SPEAKING
↔ INTERRUPT_PENDING
→ LISTENING
→ RECOVERING
→ IDLE / ERROR
```

### 11.2 Generation 状态

```text
NONE
→ STARTED
→ STREAMING
→ COMPLETED
或
→ CANCELLING
→ CANCELLED
```

每个状态变化由 Voice Core 发出，Edge 和设备只执行，不自行生成新的业务 Generation。

### 11.3 Playback 状态

```text
NOT_RECEIVED
→ BUFFERING
→ STARTED
→ PROGRESS
→ ENDED
或
→ FLUSHED / ERROR
```

服务端“完成发送”不等于设备“完成播放”。

### 11.4 Interruption 状态

```text
NO_CANDIDATE
→ CANDIDATE（本地 duck）
→ CONFIRMED（flush + cancel）
或
→ REJECTED（恢复音量）
```

---

## 12. Golden Trace 与可观测性

每次话轮必须可通过一个 `trace_id` 串起：

```text
session_id
stream_epoch
turn_id
generation_id
tool_epoch
runtime_profile_version
device sequence
capture sample range
playback sample range
```

最少记录以下时间点：

```text
device.mic_first_sample
vad.start
vad.end
edge.audio_first
asr.partial_first
asr.final
turn.committed
llm.first_token
tts.first_pcm
edge.first_downlink_opus
device.first_audio_received
device.dac_started
interrupt.detected
interrupt.local_duck
interrupt.flush
generation.cancelled
device.playback_ended
```

严禁在日志中记录：

- Wi-Fi 密码；
- 设备私钥；
- Provider Key；
- 完整原始用户转写（常规指标）；
- 声音复刻 Token；
- 未脱敏家庭信息。

### 12.1 核心指标

```text
device_media_connect_success_total
device_media_reconnect_total
device_uplink_gap_samples_total
device_downlink_queue_ms
device_playback_ack_lag_ms
stale_generation_drop_total
interrupt_candidate_total
interrupt_confirmed_total
interrupt_false_positive_total
interrupt_audible_stop_ms
aec_far_end_false_vad_total
aec_double_talk_asr_error_rate
runtime_profile_version_lag
```

---

## 13. 分阶段迁移方案

### M0：冻结产品和架构合同

输出：

- 本文 ADR；
- 小程序控制面定义；
- ESP32 一等语音终端定义；
- Generation/Sample/Playback/Interruption 权威表；
- 状态词汇和状态披露表。

退出标准：产品、架构、固件、后端、测试共同签字，后续不得再默认让小程序承载实时音频。

### M1：小程序退出实时语音

工作：

- 删除/关闭首页语音与实时文字会话；
- 删除 RecorderManager、PCM Player、媒体 WSS；
- 首页改为设备与摘要 Dashboard；
- CI 增加静态禁止规则；
- 保留设备初始化和记录查询。

退出标准：小程序全流程无麦克风权限，关闭小程序不影响机器人会话。

### M2：建立硬件直连 Media Edge

工作：

- 在 Go Media Edge 增加 `/v1/device/media` WSS；
- 迁移现有设备票据、Hello、二进制帧校验；
- Opus 上行转 PCM，通过已有 gRPC 发给 Voice Core；
- Voice Core 下行 PCM 转 Opus 回设备；
- 控制优先队列。

退出标准：固定 WAV 上行能得到 ASR；固定文本 TTS 能从设备播放；不经过 LiveKit 和 MiniProgram Bridge。

### M3：Generation、Sample Clock、Playback Ledger 闭环

工作：

- Generation 从 1 开始；
- 统一 Turn/Generation/Tool Epoch；
- exact playback watermark；
- flush/cancel；
- actual-heard 计算。

退出标准：连续打断 100 次无旧音频误播，未播放文本不进入已听历史。

### M4：按钮与本地停止词

工作：

- 物理按钮本地 flush；
- 离线停止词；
- P0 控制通道；
- Voice Core cancellation；
- 延迟指标。

退出标准：按键和停止词无需等待云端 ASR 即停止。

### M5：16 kHz AEC Bring-up

工作：

- 同步采集/播放；
- Reference tap；
- AFE FD Low Cost；
- 延迟校准；
- Far-end only / Near-end only / Double-talk；
- CPU、内存和欠载测试。

退出标准：播放期间 Far-end 回声不频繁建立用户话轮，Double-talk 近端可识别。

### M6：自然 Barge-in

工作：

- barge-in candidate；
- 本地 duck；
- early ASR；
- Backchannel 分类；
- 儿童/成人阈值；
- false positive 恢复。

退出标准：用户可在机器人说话期间自然插话，不必等待播完；“嗯/对”不会普遍错误终止。

### M7：影子比对

同一授权回放输入：

```text
旧 LiveKit Compatibility Path
新 Direct Voice Core Path
```

比对：

- VAD 区间；
- ASR final；
- Turn commit；
- Generation；
- TTS 文本；
- Playback；
- Interrupt 决策；
- Policy/Memory 行为。

退出标准：核心语义一致，差异有分类和批准。

### M8：实板 Canary

- 内部设备；
- 成人受控使用；
- 逐设备 feature flag；
- 一键回退兼容路径；
- 监控和日志完整。

### M9：生产默认切换

- `direct_voice_core` 默认；
- `livekit_compat` 只作回滚；
- 小程序媒体 Gateway 停止部署；
- 文档和 README 更新。

### M10：删除兼容耦合

- 删除 `MiniProgramBridgeAdapter`；
- 删除设备 → 小程序 Claims 投影；
- 删除 Generation N+1 映射；
- 删除不再使用的小程序媒体协议与播放器；
- 归档迁移记录。

---

## 14. 推荐 PR 清单与验收标准

### PR-00：架构状态与 ADR

- [ ] 新增本文到 `docs/architecture/`
- [x] `architecture-status.yaml` 增加 hardware target/runtime 字段（code，2026-08-14）
- [x] 状态拆为 code/wired/enabled/verified（code，2026-08-14）
- [x] 明确回滚路径（code + wired + enabled + production chaos verified：Direct 精确 allowlist、legacy 回滚、2026-08-16 失败自动回滚，以及 Edge 先回退/Bridge 后回退再反向恢复候选的双向演练均有真实生产证据；真机与完整混沌矩阵仍未完成）

验收：所有团队使用同一架构图和状态词。

### PR-01：Golden Trace

- [ ] trace/fence 字段贯穿设备、Edge、Core、Provider
- [x] 结构化指标（code + wired + enabled + production runtime verified：当前 Edge 结构化媒体/设备指标与 Voice Core channel supervisor 的 liveness、redial、generation 指标均已在生产私有 mTLS metrics 读取；2026-08-16 零会话演练验证 probe success 由 6 增至 9、failures=0）
- [x] 日志脱敏（code + local verified：票据、密钥、证书与硬件验收文本/错误证据均按边界脱敏，2026-08-16）

验收：一次实板话轮可在单一 Trace 中完整定位。

### PR-02：小程序控制面化

- [x] 删除实时语音入口和麦克风授权（code + local verified，2026-08-14）
- [x] 首页 Dashboard（code + local verified，2026-08-14）
- [x] 删除生产媒体依赖（code + local verified，2026-08-14）
- [x] 静态防回归门（local verified，2026-08-14）

验收：微信权限中不申请录音，代码无 RecorderManager。

### PR-03：Device Protocol v2

- [x] 能力协商（code + protocol-v2 real-device handshake，2026-08-14）
- [x] 音频模式（code + wired；当前实板诚实降级 `half_duplex_safe`，2026-08-14）
- [x] AEC Profile（code；实板无 Reference，声学 verified 未完成）
- [x] 完整 Playback Fence（code + local verified；DAC 精确水位未完成）
- [x] 兼容 v1（enabled + real-device verified，2026-08-14）

验收：旧固件可安全降级，新固件不被错误标记为全双工。

### PR-04：Go Device WSS/Auth

- [x] WSS endpoint（wired + enabled，Direct 单设备 canary，2026-08-14）
- [x] JWT/JWKS/一次性 Ticket（code + wired + handshake verified，2026-08-14）
- [x] 单连接租约（code + wired + enabled + production runtime verified：单 Edge Redis URL 与 mTLS 四件套已启用；真实 replay、跨实例接管、Redis HA、故障转移与混沌仍待验证，2026-08-16）
- [x] 速率/大小限制（code + local verified，2026-08-14）

验收：重放、跨设备和过期票据全部拒绝。

### PR-05：Go Uplink + gRPC

- [x] Opus decode（code + local verified，2026-08-14）
- [x] sequence/sample validation（code + local verified，2026-08-14）
- [x] loss marker（code + local verified，2026-08-14）
- [x] media-v1 Audio/VAD/KWS（code + local verified；Direct 实板 T4 待重验，2026-08-14）

验收：固定实板录音可得到一致 ASR。

### PR-06：Core Downlink + Opus

- [x] TTS PCM（code + local verified，2026-08-14）
- [x] generation-bound sender（code + local verified，2026-08-14）
- [x] control lane 优先（code + local verified，2026-08-14）
- [ ] 设备播放

验收：固定文本连续播放 100 次无乱序/爆音/旧代次。

### PR-07：Playback Ledger

- [x] started/progress/ended/error（code + local verified，2026-08-14）
- [ ] exact watermark
- [ ] actual heard
- [x] approximate 明示（code + local verified；当前板无 DAC 样本计数，2026-08-14）

验收：中途停止后只记录实际播放范围。

### PR-08：Voice Core Agent Adapter

- [x] ASR、LLM、TTS、Policy、Memory/Persona 接线（code + local verified，2026-08-14）
- [x] 不依赖 LiveKit UI topic（code + local verified，2026-08-14）
- [x] fixed speech 与普通 Generation 同合同（code + Agent canary verified；Direct T7 待验，2026-08-14）

验收：完整单轮不经过 LiveKit 仍能完成。

### PR-09：统一 Generation

- [x] 从 1 开始（code + local verified，2026-08-14）
- [x] 全链路同 ID（code + local verified，2026-08-14）
- [x] 移除新路径 N+1 映射（code + local verified，2026-08-14）

验收：日志和 Playback Receipt 不需换算。

### PR-10：硬停止

- [x] 按键本地 flush（code + local verified，2026-08-16）
- [x] Edge gate close（code + local verified，2026-08-16）
- [x] Core cancel（code + local verified，2026-08-16）
- [x] late frame drop（code + local verified，2026-08-16）

验收：P95 可听停止 ≤ 150 ms（目标），无旧音频恢复。

### PR-11：离线停止词

- [ ] 本地模型
- [ ] hard_stop event
- [ ] 误触发测试

验收：弱网/断网时也能立即停止本地播放。

### PR-12：Audio Profile 与 Full-Duplex AEC

- [x] 采集 16 kHz / 播放 24 kHz 契约（code + local verified，2026-08-14）
- [x] 24 kHz Direct downlink 协商（code + local verified；目标板原生播放率，2026-08-14）
- [ ] Reference tap
- [ ] AFE feed/fetch

验收：无 AEC 前后错采样率、时钟漂移或欠载。

### PR-13：AEC 校准与诊断

- [ ] FD Low Cost
- [ ] delay/gain calibration
- [ ] Reference/Mic 录制导出（仅测试）
- [ ] ERLE/残余回声指标

验收：Far-end only 误触发显著下降，Double-talk 可用。

### PR-14：Barge-in Candidate + Duck

- [ ] 播放中 VAD
- [ ] 本地 duck
- [ ] evidence 上报
- [ ] timeout restore

验收：候选不立即破坏全部对话，失败可恢复。

### PR-15：共享 Interruption Policy

- [ ] hard stop
- [ ] backchannel
- [ ] true interrupt
- [ ] bystander/unknown speaker
- [ ] child/adult profile

验收：H5 回放与设备回放使用同一语义测试集合。

### PR-16：打断后的 Actual Heard 与 Memory Fence

- [x] TTS cancel（code + local verified，2026-08-16）
- [x] Tool cancel（code + wired + enabled in Agent/Bridge canary + local verified，2026-08-17；`test_cancelled_late_tool_result_cannot_form_new_generation_output`）
- [x] heard text cutoff（code + wired + enabled in Agent/Bridge canary + local verified，2026-08-17；`test_existing_provider_adapter_cuts_unplayed_timed_text_on_interrupt`、media-session interruption/playback-ledger 回归）
- [x] memory/history eligibility（code + local verified：按原 generation/turn fence 绑定，访客、ambiguous、authority unavailable 与不合格话轮及其 AI 回复不得进入主人历史/自动摘要，2026-08-16）

验收：未播放内容永不被标为已听、永不误入永久记忆。

### PR-17：Runtime Profile 版本同步

- [x] Soul/Persona/Policy/Audio Mode 版本（code + local verified，2026-08-14）
- [x] 设备 ACK（code + real-device legacy verified，2026-08-14）
- [x] 过期和失效（code + local verified，2026-08-14）
- [x] 安全变更的会话边界（code + local verified，2026-08-14）
- [x] 主体 / Binding / Profile / reconnect authority fence（code + wired + enabled + local verified + production runtime verified，2026-08-18；空主体为显式 `unknown_safe` 而非 wildcard，重连只允许 `stream_epoch` 改变；生产零活跃会话证据见 `20260818-103228` 回执）

验收：小程序改设置后可追溯地生效，不出现半旧半新；本轮新增主体权威矩阵已本地通过，生产服务器层已启用并通过健康/门禁复核，Direct 真机与声学验收仍待外部门禁。

### PR-18：设备设置与诊断页面

- [x] 音量/亮度/勿扰（code + local verified；目标板亮度为二值门控，2026-08-14）
- [x] 音频模式（code + local verified，2026-08-14）
- [x] Firmware/AEC 状态（code + local verified，2026-08-14）
- [x] 脱敏诊断（code + local verified，2026-08-14）

验收：页面只展示服务器权威状态。

### PR-19：对话记录与总结页面

- [x] 会话索引（code + local verified，2026-08-16）
- [x] 摘要（code + local verified，2026-08-16）
- [x] actual heard（Control API `code + wired + enabled + production runtime verified`，小程序 `code + wired + local verified`、未发布，2026-08-17；GET `/v1/archive/conversation-review` 公网未认证 401 fail closed；小程序按 `approximate` 展示；精确 DAC/真实设备 Actual Heard 仍未验收）
- [x] 记忆候选/确认分离（Control API `code + wired + enabled`、local capability 矩阵 verified，小程序未发布，2026-08-17；候选确认走既有 review 事件，成人/未成年人允许，未知/主体资料缺失在 Archive/Catalog 读取或写入前拒绝；未执行生产认证业务写入）

验收：跨主体权限不泄漏（本地双账号与 adult/minor/unknown/missing 矩阵通过；Control API 生产路由已 fail closed，认证用户业务、小程序发布与真机验收仍待外部门禁）。

### PR-20：OTA/回滚

- [ ] A/B OTA
- [x] 签名（code + local verified，2026-08-16）
- [ ] Board/Profile 兼容
- [ ] 自动回滚

验收：坏固件不使设备永久失联。

### PR-21：容量与混沌

- [ ] 连接数
- [ ] Provider 限流
- [ ] Wi-Fi 抖动
- [ ] Edge 重启
- [ ] Core 重启
- [ ] Redis/PostgreSQL 短暂故障

验收：会话可恢复，旧 Epoch 和旧 Generation 不复活。

### PR-22：Canary 与生产切换

- [x] 逐设备 flag（code + wired + enabled：当前生产 allowlist 仅 `dev_atk_a4cb8fd6095c`，2026-08-16）
- [ ] SLO Dashboard
- [x] 自动回退（production exercised：2026-08-16 首次 Media Turn retry 切换因 Edge readiness 503 自动回到冻结镜像并恢复 healthy；当时仍需 Edge restart 才能为下一次切换重建 channel）
- [x] Release Evidence（候选 `memoria-agent:20260816-214225-media-turn-retry-canary`、运行 image ID/17 项哈希、10 个保留容器 ID、证据目录及冻结 rollback `memoria-agent:rollback-20260816-214225-pre-media-turn-retry` 已记录；Direct T4–T7、Exact DAC、AEC、双讲、Actual Heard 与 T14 待补，2026-08-16）

### PR-23：退役旧桥

- [ ] 停用设备对 MiniProgram Bridge 的依赖
- [ ] 停用小程序媒体 Gateway
- [ ] 删除过渡映射
- [ ] 更新 README/HANDOFF/部署图

---

## 15. 分团队开发清单

### 15.1 产品经理

- [ ] 确认小程序不再提供实时语音和实时文字聊天
- [ ] 定义首页、设备、回顾、我的信息架构
- [ ] 定义 L0–L3 打断能力文案
- [ ] 未通过 AEC 门禁前不宣传全双工
- [ ] 定义 Soul、Persona、Service Mode、Policy 的边界
- [ ] 定义儿童、成人、老人、家庭差异
- [ ] 定义记录和摘要可见范围
- [ ] 定义设备离线/降级提示

### 15.2 架构师

- [ ] 冻结控制面/媒体面分离
- [ ] 冻结唯一状态权威
- [ ] 评审 Device Wire ↔ media-v1 映射
- [ ] 评审 Generation/Sample/Playback Fence
- [ ] 评审直接路径和兼容回滚
- [ ] 评审 AEC 能力登记和降级
- [ ] 评审容量与故障域

### 15.3 Control API

- [ ] Runtime Profile 版本
- [ ] Device Acoustic Capability
- [ ] Media Ticket
- [ ] Device Setting/Command ACK
- [ ] Conversation/Summary API
- [ ] Diagnostics API
- [ ] 权限与 RLS
- [ ] 设置变更审计

### 15.4 Go Media Edge

- [ ] WSS Device ingress
- [ ] auth/ticket lease
- [ ] protocol parser
- [ ] Opus
- [ ] Sample Clock
- [ ] gRPC bridge
- [ ] control priority lane
- [ ] Generation Gate
- [ ] Playback Receipt
- [ ] backpressure
- [ ] metrics
- [ ] race/chaos tests

### 15.5 Python Voice Core/Agent

- [ ] transport-independent session
- [ ] ASR timeline
- [ ] turn coordinator
- [ ] interruption evidence/policy
- [ ] generation cancellation
- [ ] provider cancellation
- [ ] actual heard
- [ ] memory fence
- [ ] fixed speech contract
- [ ] Policy/Persona/Memory adapters

### 15.6 ESP32 固件

- [ ] 独立媒体客户端
- [ ] 双向并行音频任务
- [ ] Reference tap
- [ ] AFE/AEC/NS/VAD
- [ ] stop keyword
- [ ] playback watermark
- [ ] local duck/flush
- [ ] P0 control queue
- [ ] reconnect epoch
- [ ] Runtime Profile ACK
- [ ] OTA/rollback
- [ ] diagnostics
- [ ] 长时间内存稳定性

### 15.7 小程序

- [ ] 删除语音路径
- [ ] Dashboard
- [ ] onboarding
- [ ] Soul/Persona
- [ ] person/family
- [ ] device settings
- [ ] summaries/reviews
- [ ] diagnostics
- [ ] privacy/consent
- [ ] iOS/Android 微信真机验收

### 15.8 测试

- [ ] 分层测试，不只跑端到端
- [ ] 固定录音回放
- [ ] 固定 TTS 播放
- [ ] 100/500 连续话轮
- [ ] 打断矩阵
- [ ] AEC 双讲矩阵
- [ ] 儿童/成人/老人声音
- [ ] Wi-Fi/4G/弱网
- [ ] OTA/断电
- [ ] 多主体权限
- [ ] 实际已听文本

### 15.9 运维与安全

- [ ] mTLS
- [ ] JWKS/EdDSA
- [ ] Ticket 轮换
- [ ] 内外网监听隔离
- [ ] 指标/告警
- [ ] Canary/rollback
- [ ] Secret scanning
- [ ] Firmware signing
- [ ] 设备吊销
- [ ] 日志脱敏

---

## 16. 分层验收脚本

### T1：设备播放 Only

```text
固定 Opus → Edge → ESP32 → DAC → Playback ACK
```

不经过 ASR/LLM/TTS。

### T2：设备录音 Only

```text
Mic → AFE bypass → Opus → Edge → 临时 WAV
```

检查采样率、声道、爆音、丢帧、时钟。

### T3：AEC Offline

```text
保存 Mic + Reference → 离线 AEC → 对比输出
```

### T4：ASR Only

```text
ESP32 → Edge → Voice Core → FunASR final → 屏幕/日志
```

### T5：TTS Only

```text
固定文本 → TTS → Edge → ESP32
```

### T6：LLM + TTS

```text
服务端注入用户文本 → LLM → TTS → ESP32
```

### T7：完整单轮

```text
Mic → VAD → ASR → Turn → LLM → TTS → DAC
```

### T8：按钮打断

机器人播放中随机时刻按键，验证停止水位、Generation 和 Actual Heard。

### T9：停止词

不同距离、音量和噪声下说“停一下/等等”。

### T10：自然抢话

机器人播放时输入完整新问题，验证旧回复取消和新话轮建立。

### T11：Backchannel

播放中说“嗯、对、好”，验证不会普遍误取消。

### T12：回声与电视

播放机器人声音或电视人声，验证不会建立错误私人记忆。

### T13：连续稳定性

- 100 轮合入门禁；
- 500 轮夜间稳定性；
- 无 panic/watchdog；
- 堆不持续下降；
- WSS 重连不复活旧 Generation。

### T14：小程序独立性

- 初始化完成后关闭小程序；
- 清理微信后台；
- 手机换网络；
- 机器人仍能对话；
- 小程序重新打开可看到新记录和摘要。

---

## 17. SLO 与发布门槛

以下为建议工程目标，不是现状声明：

| 指标 | 初期门槛 | 目标 |
|---|---:|---:|
| 设备媒体连接成功率 | ≥ 99% | ≥ 99.9% |
| 首次 WSS 建立 P95 | ≤ 3 s | ≤ 1.5 s |
| 上行音频缺口 | < 1% 会话有明显缺口 | < 0.1% |
| 用户结束到首个 TTS 可听 P95 | ≤ 2.5 s | ≤ 1.5 s（受 Provider 影响） |
| 按钮停止可听 P95 | ≤ 250 ms | ≤ 150 ms |
| 停止词停止可听 P95 | ≤ 500 ms | ≤ 300 ms |
| 自然语音抢话停止 P95 | ≤ 700 ms | ≤ 350–500 ms |
| 旧 Generation 误播 | 0 | 0 |
| Playback End ACK 完整率 | ≥ 99.5% | ≥ 99.9% |
| 未播放文本进入已听历史 | 0 | 0 |
| Far-end only 误打断 | 需按场景设上限 | 持续下降并通过真机门 |
| 连续 500 轮崩溃/重启 | 0 | 0 |

任何出现以下情况，Release 必须 REJECT：

```text
旧 Generation 误播
旧 Epoch 复活
跨主体私人上下文泄漏
设备发完未播却标记已听
小程序关闭导致机器人对话停止
回声被写成用户永久记忆
无 AEC 证据却启用 full_duplex_verified
```

---

## 18. 主要风险与处置

| 风险 | 影响 | 处置 |
|---|---|---|
| 当前板 Reference 不对齐 | 回声触发打断、ASR 污染 | 先固定 16 kHz uplink / 24 kHz downlink 契约，再做 Reference tap、延迟校准；失败则降级或改硬件 |
| ESP32 CPU/内存不足 | AEC+Opus+LVGL 欠载 | FD Low Cost、任务核/优先级、PSRAM、降低 UI 刷新、指标实测 |
| 模拟增益导致 Reference 不一致 | AEC 残余高 | 固定模拟增益，数字音量置于 tap 前 |
| Wi-Fi 抖动 | 断续、积压 | 小缓冲、重连 Epoch、PLC、禁止积累旧音频 |
| 每个 VAD 都取消 | 机器人频繁被回声/附和打断 | Candidate/Duck/Confirm 三段式 |
| 服务端确认过慢 | 抢话延迟 | 硬停本地执行；自然打断本地 duck，Early ASR |
| 小程序和设备设置竞态 | Profile 半新半旧 | 版本化 Profile + ACK + 会话安全点生效 |
| 兼容链继续扩张 | 长期双状态机 | 冻结旧桥，只允许修复回滚问题 |
| Provider 不支持快速取消 | 旧 TTS 尾音 | 连接级取消、Generation Fence、设备 flush |
| 设备被盗/复制 | 账号和隐私风险 | 硬件身份、吊销、短票据、解绑/转赠 step-up |

---

## 19. 明确禁止事项

1. 不得让小程序继续中转设备实时音频。
2. 不得让 ESP32 长期复用 `MiniProgramLiveKitBridge`。
3. 不得以 UI `assistant_state=speaking` 作为硬件放音 Gate。
4. 不得把任何 VAD 触发都当作真正打断。
5. 不得只清空本地队列而不更新 Generation/Epoch。
6. 不得用“服务器 TTS 发送完成”代替“设备播放完成”。
7. 不得将 ASR/LLM/TTS 永久密钥写入固件。
8. 不得把完整 Persona/家庭记忆下发到设备。
9. 不得在没有双讲实测时宣传全双工。
10. 不得把小程序单测、WSS 握手或上游小智对话当作 Memoria 全链路验收。
11. 不得为了跑通而关闭 `missing_speech_epoch`、Generation Fence 或 Actual Heard。
12. 不得同时改架构、AEC、全双工和 Provider，而没有分层测试。

---

## 20. Definition of Done

本整改只有同时满足以下条件才算完成：

### 产品

- [x] 小程序定位和页面与控制面一致（code + local verified，2026-08-14）
- [x] 无实时语音入口和麦克风权限（code + local verified，2026-08-14）
- [ ] 机器人可脱离小程序独立对话
- [ ] Soul/Persona/人物/设置完整可配置
- [x] 对话记录和总结可查询（code + local verified；真实跨主体/生产数据验收仍待完成，2026-08-16）

### 架构

- [x] ESP32 不经过 MiniProgram Bridge（Direct path code + local verified；legacy 兼容路径仍保留）
- [x] Device WSS 进入 Go Media Edge（code + wired + enabled + production runtime verified；Direct 真机话轮仍待验证，2026-08-16）
- [x] Media Edge 通过 media-v1 进入 Python Voice Core（code + wired + enabled + production runtime + production chaos verified：Bridge 热替换 supervisor 已在生产以具名 gRPC Health、mTLS readyz、自动 redial 与双向回滚演练验证；验证范围为零活跃设备/媒体会话，Direct 真机和声学验收仍未完成，2026-08-16）
- [x] Python 仍是交互权威（code + local verified，2026-08-14）
- [x] 唯一 Generation/Sample/Playback 权威明确（code + local verified；exact DAC watermark 未完成，2026-08-14）
- [x] 近端语音环 + 远端重推理边界（code + wired + enabled + production runtime verified：ESP32 → Go Media Edge → Python Voice Core/Agent；不在 ESP32 引入 30B 或第二套 Voice Core；网络延迟与离线能力仍待实测，2026-08-21）

### 实时语音

- [ ] 完整单轮稳定
- [ ] 按钮硬停
- [ ] 离线停止词
- [ ] AEC 后自然抢话
- [ ] Backchannel 不普遍误打断
- [ ] Actual Heard 精确
- [ ] 旧 Generation 误播为 0
- [ ] 回复投递终态协议（**code + wired + enabled + production runtime verified，2026-08-21**：Voice Core `ReplyDeliveryLedger`、`reply-delivery-v1` 独立跨进程投影、producer-side Fernet spool/重放和 Control API 幂等落盘已生产启用；真实设备同一 delivery 的首帧/Playback/Actual Heard/抢占终态仍缺）
- [ ] TTS 首帧/打断基准（**code + wired + enabled + production runtime verified，2026-08-21**：`tts_first_frame`/`tts_first_frame_p95_ms` 与首帧前抢占计数已生产启用；真实板卡首帧、下行拒绝同代关联、Playback ACK/Actual Heard 延迟及缓存命中率仍缺）
- [ ] 自然度独立 voice profile A/B（基础 TTS 可听见、AEC、双讲和 Playback fence 通过后才执行，不作为默认能力）

### 真机

- [ ] iOS/Android 微信配网与初始化
- [ ] ESP32 500 轮稳定性
- [ ] AEC 双讲矩阵
- [ ] 儿童/成人/老人声学样本
- [ ] 远场、音量、噪声矩阵
- [ ] OTA/断电/弱网/重连

### 生产

- [ ] PostgreSQL/RLS 和托管签名密钥
- [x] mTLS/JWKS（wired + enabled + production runtime verified：当前单设备 Direct canary；证书轮换、托管签名密钥与更大范围发布仍待完成，2026-08-16）
- [x] Edge Bridge 热替换（code + wired + enabled + production runtime + production chaos verified：具名 gRPC Health、私有 mTLS readyz、自动 redial、双向回滚和零会话 fail-closed 演练均完成，2026-08-16）
- [ ] 指标、告警、Canary、回滚（单候选切换、失败自动回滚、延迟运行复核与 Edge 自动重拨零会话混沌已验证；告警送达、带活跃会话恢复、Bridge IP 变化与 Edge 重启的完整矩阵仍未完成）
- [ ] 构建供应链门禁（锁定构建脚本、代码生成器、第三方 Action、基础镜像和 Go/Python/C++/ESP-IDF/容器依赖；产出并校验 SBOM、来源证明/attestation、digest 和扫描结果；不引入 Rust）
- [ ] 容量和混沌
- [ ] Release Evidence（候选 `memoria-agent:20260816-214225-media-turn-retry-canary`、运行 image ID/文件哈希、保留容器、证据目录与冻结 rollback 已保存；Direct 真机、Exact DAC、AEC、双讲、Actual Heard、T14 证据仍缺）
- [ ] 外部法务/PIA/儿童安全门按发布人群完成

---

## 21. 最终实施意见

本项目下一阶段不应再围绕“如何把 ESP32 塞进现有手机语音架构”继续修补，而应做一次明确的产品与媒体边界收敛：

```text
小程序 = 控制面
ESP32 = 实时语音终端
Go Media Edge = 设备媒体边缘
Python Voice Core = 对话权威
Control/Policy/Memory/Persona = 业务权威
```

当前仓库已经具备大部分正确积木：设备身份、严格帧协议、Sample Clock、Generation Gate、Playback Receipt、`media-v1` 和 Voice Core seam。最有价值的工作不是重写，而是把这些积木沿正确的边界接起来。

### 21.1 2026-08-21 当前执行顺序

1. 先建立 `ReplyDelivery`/`ReplyDeliveryLedger`：一个完整 fence 只能有一个幂等最终结果，并能区分首帧发送、播放结束、Actual Heard、被抢占、传输拒绝和错误；不能继续只依赖 `emitted_audio` 或进程内诊断列表。
2. 在不替换模型的前提下，解决当前 FunASR/下行链路阻塞，取得真实板卡从 VAD、ASR final、turn commit、TTS 首帧到设备播放/Actual Heard 的同代证据。
3. 以同一 fence 贯通首帧、打断、ACK 和 Actual Heard 基准；基准未形成前，不把 provider 更换或自然度调参当作主线。
4. 基础 TTS、AEC、双讲和 Playback fence 通过后，才用独立 voice profile 做自然度 A/B；默认路径保持保守。
5. 供应链门禁可与上述工作并行补齐，但必须覆盖当前 Go、Python、C++、ESP-IDF、容器、代码生成和第三方 Action 链路；不因为 Rust 供应链事件新增 Rust。

对全双工的态度应当同时积极和严格：

- 架构从第一天允许同时采集和播放；
- 先完成本地按钮和停止词硬停；
- 再完成 16 kHz AEC Reference 与 Double-talk；
- 再开放自然语音抢话；
- 通过真机矩阵后才标记 `full_duplex_verified`。

最终产品体验应达到：

> 用户可以在机器人说话时自然开口，机器人能够快速、正确地判断是附和、停止还是新问题；旧回答立即失效，未播放内容不会污染记忆；手机小程序即使关闭，机器人仍然独立、稳定地完成整段对话。

---

## 22. 主要审查依据

### Memoria 仓库（2026-08-13 初始审查快照）

1. 项目主页与当时的小程序媒体说明：
   https://github.com/monkeyin92/memoria
2. 当前项目状态：
   https://raw.githubusercontent.com/monkeyin92/memoria/main/HANDOFF.md
3. 当前设备会话复用小程序桥：
   https://raw.githubusercontent.com/monkeyin92/memoria/main/services/device_media_gateway/session.py
4. 当前设备 Gateway 配置继承小程序配置：
   https://raw.githubusercontent.com/monkeyin92/memoria/main/services/device_media_gateway/config.py
5. 当前设备协议：
   https://raw.githubusercontent.com/monkeyin92/memoria/main/services/device_media_gateway/protocol.py
6. 当时的小程序首页实时媒体实现：
   https://raw.githubusercontent.com/monkeyin92/memoria/main/apps/miniprogram/pages/home/index.js
7. Go Media Edge：
   https://raw.githubusercontent.com/monkeyin92/memoria/main/services/media_edge/README.md
8. Media/Voice Core Protobuf：
   https://raw.githubusercontent.com/monkeyin92/memoria/main/packages/proto/memoria/media/v1/media.proto
9. Media Runtime 权威与迁移边界：
   https://raw.githubusercontent.com/monkeyin92/memoria/main/docs/adr/0029-continuous-interaction-authority-and-shadow-migration.md
10. ESP32 固件交付说明：
    https://github.com/monkeyin92/memoria/tree/main/firmware/esp32
11. 当前板型配置与音频引脚：
    https://github.com/monkeyin92/memoria/blob/main/firmware/esp32/overlay/files/main/boards/memoria/atk-dnesp32s3-v1/config.h
12. 当前板型 ES8388 初始化：
    https://github.com/monkeyin92/memoria/blob/main/firmware/esp32/overlay/files/main/boards/memoria/atk-dnesp32s3-v1/memoria_atk_dnesp32s3_v1.cc

### Espressif 官方资料

13. ESP-SR AEC：
    https://docs.espressif.com/projects/esp-sr/en/latest/esp32s3/acoustic_echo_cancellation/README.html
14. ESP-SR Audio Front-end：
    https://docs.espressif.com/projects/esp-sr/en/latest/esp32s3/audio_front_end/README.html
15. ESP-SR VADNet：
    https://docs.espressif.com/projects/esp-sr/en/latest/esp32s3/vadnet/README.html
16. ESP-GMF AI Audio：
    https://docs.espressif.com/projects/esp-gmf/en/latest/gmf-framework/gmf-elements/gmf-ai-audio.html
