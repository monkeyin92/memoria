# 20260812-163054 Release

## 状态与范围

- 基于生产版本 `20260812-154923`。实板已证明设备 AFE VAD、可靠 LiveKit 投影、Agent speech epoch
  与 FunASR final 全部生效，但真实话轮提交在 identity epoch drain 中失败：生产
  `AgentSession.interrupt()` 返回合法的已完成 `Future`，旧 `_bounded` 却直接传给
  `asyncio.create_task`，后者只接受 coroutine，并在 Python 3.12 抛出 `TypeError`。
- 修复位于统一 epoch-drain barrier：接受 Python 任意 `Awaitable`，用自有 coroutine 包装后再创建
  命名任务；coroutine、Task、Future 共用原有超时、取消、迟到结果不采纳和 fail-closed 语义。
  不在 LiveKit 调用方增加特判，也不放宽 generation、identity 或 speech fence。
- 本次只修改 Agent 源码与回归测试；不含固件、设备网关、数据库、env 或 H5 变更。运行时回滚目标
  固定为 `20260812-154923`；板上已刷固件无需再次写入。

## 发布前门禁

- 新增 Future 与 Task 两类 playback stop seam 回归，并保留 coroutine、慢 seam、永久挂起、取消和
  drain fail-closed 覆盖；相关 7 项与 Agent 全目录测试通过。
- Ruff、`identity_state.py` strict mypy、`git diff --check` 通过。
- 发布后必须重跑完整 provider/readiness 与连续 `8/8` 公网稳定性；随后用同一实板完成
  VAD start/end、FunASR final、话轮提交、LLM、Doubao 首 PCM、下行 Opus、板端播放排空。
  只有用户确认实际听到回复，才记为声学闭环完成。

## 生产结果

- 待发布。
