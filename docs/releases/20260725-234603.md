# 小程序明确语音打断与就绪门禁 Release 20260725-234603

## 发布目标

- 发布“等一下 / 停一下”在流式识别阶段提前确认的语音打断修复。
- 修正发布门禁：新 Agent 已注册但首个心跳尚未写入 Control API 时，不再把短暂的 503 误判为
  release 故障并自动回滚。

## 根因与修复

- TargetSpeakerFocus 以前只在 final transcript 后启动确认；真机短控制语会被 VAD endpoint
  延迟拖后，导致正在播放的回复几乎讲完才停止。
- Router 已确认、且具有真实 PCM 的 interim 控制语现在立即启动当前声纹校验；确认后复用唯一
  LiveKit 停止桥接。明确 shadow guest / formal guest 仍被拒绝，普通 VAD 自动打断仍等待
  endpoint。
- `refresh_readiness.sh` 在 provider smoke 标记成功后，最多等待 40 秒，直到
  `/health/ready` 同时报告当前 release 和当前 Agent heartbeat 为 ready，才宣告发布通过。

## 前一候选说明

- `20260725-232632` 的 Provider smoke、候选 server smoke、镜像验签与备份均通过；首次
  runtime 切换后，新 Agent 的第一次 heartbeat 尚未到达，立即检查 `/health/ready` 返回 503。
- 保护逻辑已自动回滚到 `20260725-221147`，未切 H5；该候选不视为已发布。本 release 包含
  等待当前 heartbeat 的修复后重新构建的同一语音功能。

## 发布前验证

- TargetSpeakerFocus、Agent production wiring、DuplexRuntime wiring 与 production Compose
  定向回归全部通过。
- Ruff、Shell 语法检查与 `git diff --check` 通过。
- 候选 runtime/H5 已完成 commit-bound 源码、四个 amd64 镜像与清单验证；候选 server smoke
  通过。

## 发布边界与回滚

- 仅 runtime 发布；H5 只参与固定工件和候选 smoke，不切 H5 软链。小程序体验版继续使用
  `0.8.45`，无需重复上传客户端包。
- 运行时回滚点为 `20260725-221147`；数据库和用户会话不随普通代码回滚。
