# 20260812-111951 Release

## 状态与范围

- 基于生产健康版本 `20260812-103158` 的设备 Path 2 策略热修。
- 真实设备已经通过 Ed25519 media challenge、media session、TURN 凭据和公网设备 WSS
  `session.ready`；进入 Agent 后，合法签名的 `unknown_safe` Runtime Profile 被旧
  Interaction Policy 错误映射为 `session_runtime_subject_unavailable`，导致会话按 503
  fail closed。
- Interaction Policy 现在只接受两种严格主体状态：已确认且主体非 unknown；或精确的
  `active_subject_id=null + speaker_state=unconfirmed + subject_category=unknown +
  service_mode=unknown_safe`。其他不一致组合仍返回 503。
- `unknown_safe` 仅开放签名 Profile 中的普通 `chat`；主人称呼、陪伴风格、私人记忆、历史、
  主人投影、学习、工具、个性化声线全部关闭。Profile 过期、绑定/epoch 不一致、签名无效或
  authority 不可用仍保持 fail closed。
- 回滚目标为 `20260812-103158`；本次无数据库 DDL、数据修订、固件或 H5 变更。

## 发布门禁

- Control Interaction API 完整测试 39 项通过。
- Agent ModePolicy/RuntimeProfile 测试 119 项及 policy/runtime wiring 定向测试通过。
- Session Runtime/readiness 定向测试、Ruff、严格 mypy 与 `git diff --check` 通过。
- 切流后必须重新用真实 ESP32 证明设备 WSS、上行语音、Agent ASR/LLM/TTS、下行播放与
  `playback.ended`；HTTP/WSS 合同通过不等于声学端到端完成。
