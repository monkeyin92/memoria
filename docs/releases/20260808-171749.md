# 20260808-171749 Release

## 状态

- 生产 runtime 与 H5 已于 2026-08-08 原子切换到 `20260808-171749`；源码 commit 为
  `9812fac155ef4f46a74d0d8dbfaf197fe9c5fa5a`，annotated tag 与 `origin/main` 均已核对。
- 直接 runtime/H5 回滚目标为 `20260807-163916`。发布前真实目标已固化在 root-only legacy
  receipt；收尾时已安全转换为 `/var/backups/memoria/rollback-20260808-171749.env`，未从切流后的
  软链反推。原 `release-state-pre-20260808-171749` 保留为独立证据。
- 微信小程序 `0.8.66` 体验版尚未创建：Node 24 已完整编译并请求微信接口，但平台拒绝当前出口
  IP `112.20.18.77`（`errCode=-10008 / invalid ip`）。需先加入代码上传 IP 白名单，再重跑真实上传；
  此操作仍只创建体验版，不提审、不正式发布。
- 被拒绝的 `20260808-161814` 未部署且不得复用或移动；本 release 使用独立 tag 与全新工件。

## 变更摘要

- 发布 Agent 自我进化控制面：三层轨迹验证、候选生命周期、generation receipt、删除 tombstone /
  read lease、独立 PostgreSQL 角色与 FORCE RLS。
- 修复账户治理测试被插入 `_fixture` 返回边界的问题，恢复真实 fixture，并用 evolution tombstone
  覆盖删除期间导出 fail-closed。
- 修复 PostgreSQL evolution 账户导出 join 的未限定字段；所有 joined activation/lifecycle 字段显式
  绑定来源表，避免 `reason` 与 candidate 同名列触发 `AmbiguousColumnError`。
- 修正候选暂存、immutable union、隔离 smoke、停 writer 与 forward-only PostgreSQL 升级顺序。
- 恢复项目内 `upload:test`。部署后诊断确认 Node 25 的无效 `global.localStorage` 与
  `miniprogram-ci@2.1.31` 的三个对称编译判断不兼容；后续工具链限制为 Node `<25`，dry-run 会在
  隔离子进程中执行真实 compile-only，并在平台写入边界前停止。

## 本地与 CI 门禁

- GitHub Actions run `31250821241`：5/5 jobs 全部成功。
- Python：`2020 passed, 3 skipped`；PostgreSQL 17/pgvector 合同实际执行；总覆盖率 `87.01%`，
  orchestration `90%`，provider protocols `97%`。
- Ruff、strict mypy（261 个源文件）、media proto reproducibility/lint、media smoke/replay/chaos/load、
  offline E2E 与 offline provider smoke：通过。
- H5：`304/304`，production build 成功；主 bundle 的 `>500 kB` 警告为既有非阻断项。
- Mini Program 发布源码：`86/86`、全部 JS syntax 通过、production dependency audit 0；发布后上传器
  回归加入 2 项，当前为 `88/88`。Node 24 compile-only 产出 82 个文件；Node 25 默认运行时在进入
  vendor 前给出明确兼容提示，不再暴露 `r.getItem is not a function`。
- `miniprogram-ci@2.1.31` 的仅开发期依赖仍报告 76 个已知 advisory（其中 41 个 critical）；依赖不进入
  小程序包，安装继续使用 `npm ci --ignore-scripts`，但发布机工具链风险仍需跟踪。

## 固定工件

- Source archive：`8cb2bf7dfee09885a6082a3f713fe3975a141f62d3062530193ec701d7138b3a`
- Images archive：`f4199ca2149b6ef2022eef722702e4937b254d8104469ed04ae565af337a35c3`
- H5 artifact：`f6aa42885ad4db2af0e5cd4e14f9dcbf2d0c886ca38ea656c21b6c60521b8af5`
- Canonical manifest：`f6470c1b07e9b19116aa3d9f5173b2efdacb23753550217989d92a163b8acd73`
- Manifest digest / evolution trusted root：
  `444144fadbb2eb5a53419b0283a0a784612d6887bf7925ac6fa7eb7822c235ea`
- Release verifier：`e949258605f7b2f8ce216dabf4cd06cbead1f7ab22a1c1defbb4c19bb4a96314`
- Linux amd64 image IDs：
  - Agent：`sha256:7bacbc4b02e8fec3505f3bbed5af698287099554674498b29882f469dfc7b91e`
  - Control API：`sha256:e456eae46635f797128461d97b5c5f5f75e0e151d5bce33cae28fa84713309b1`
  - Speaker Model：`sha256:8154507c74bc8c19f677974f0c4ef0b783ea6f0a79829117908ccd28c2e63906`
  - Mini Program Gateway：
    `sha256:2817c8d19b7d6bd1f16244f657476759256b47621f3366ee4bf0d151635fba1f`

## 发布前备份与数据库升级

- SQLite backup API 双副本 `integrity_check=ok / foreign_key_check=0`，共同 SHA-256：
  `6443bde9695697296413ed9d7486b744bc52bc52162670d158529bbf4d1577af`。
- PostgreSQL custom dump：`/var/backups/memoria/memoria-pre-20260808-171749.dump`，SHA-256：
  `209b09028fdc31be2f2d5d8763b3fe456d34a625c8c43777827c815875cb3fdf`。
- MinIO 对象清单 SHA-256：`c7e273671460428a5fa9183841134a2eae6ded9814c0aeffab3fb6d5ee6155ff`。
- PostgreSQL、Control API、Agent、Speaker Model、Gateway 的旧 env 均已保存为
  `root:root 0600`；发布记录只保存其哈希，不保存内容。
- H5 回滚 snapshot `/var/www/memoria-releases/rollback-20260808-171749` 固定旧入口/provenance
  与当前 240 个 immutable assets；root-only manifest SHA-256 为
  `4f37ea9952463ef556aaf28e241c9d5244acf6a1236a46f49a5b968702890169`，逐文件验签及
  `www-data` 可读门禁通过。
- DDL 窗口先停止 Agent、Control API 与 Gateway writer；候选 data Compose healthy 后运行
  `upgrade_evolution_postgres.sh`。生产现有 `memoria_evolution` 独立登录角色 1 个、evolution 表 8 张、
  FORCE RLS 8/8、controller policy 8/8，独立密码登录与实际查询通过。
- evolution DDL 是 additive/forward-only。代码回滚不得删除角色、表、policy 或自动恢复数据库。

## 生产验收

- Runtime 于 `2026-08-08 17:59:02 CST` 切换；四个应用容器均 healthy，OCI commit/tag/role 精确
  绑定本 release。H5 在 runtime 门禁前保持旧版本。
- 真实 smoke：LiveKit authenticated room service、QwenRealtimeSearch 强制公网检索、DeepSeek
  streaming、Doubao `PCM s16le / 24 kHz / mono` + 字级时间戳 + CancelSession/eviction + 五音色、
  FunASR interim/final/单调时间戳、InterruptSemantic 五类枚举全部通过。
- `/health/ready` 为 `ready / 20260808-171749`，Agent heartbeat 同 tag；LLM 为
  `bailian_deepseek`、TTS 为 `doubao`，`evolution_store` 在内的 core 10/10 ready。
- H5 于 `2026-08-08 18:03:20 CST` 最后切换。`index.html` SHA-256：
  `b407fb08005192129b570f5d41dc2c9b1570022caf156c7af32efd1854d5d5fb`；
  `memoria-release.json` SHA-256：
  `77ff6e1584333bc5f4c5a2ec1bc5421a534bcdeee8b36d4d9a8e753f0c6d24e1`。
  候选 immutable union 为 240 个唯一 URL，全部逐项 HTTPS 200；4776 条历史 release 资源引用均存在。
- 公网根 H5、兼容路径、SPA、live、ready、IP/域名 WMS 均为 200；internal、PocketSparks 与
  Goods Invoice 原路径均为 404。CSP 与 microphone Permissions-Policy 存在，provenance 精确绑定
  tag/commit。
- 真实浏览器在 390x844 与 667x375 均显示注册页和“怎么称呼你？”字段，无横向溢出，console
  warning/error 为 0。未创建生产账号、未请求麦克风。
- `memoria-readiness-refresh.timer` 与 `snap.certbot.renew.timer` 均 active/enabled。WMS 保持发布前的
  `inactive/enabled`，静态入口仍为 200；本 release 未改 WMS 目录、数据或启用状态。
- 四个应用容器上线后 15 分钟严重错误关键字计数均为 0。根分区剩余约 14 GiB。

## 回滚与未完成验收

- H5-only 问题先用冻结 receipt 原子切回 `20260807-163916`。Runtime/readiness 问题恢复五份旧 env
  （含 PostgreSQL）、旧 data Compose 与四应用镜像，再切旧 runtime；H5 保持旧版本直至恢复完成。
- 小程序体验版只有在微信 CI 白名单加入 `112.20.18.77` 后才能重试。上传成功只证明体验版工件创建，
  不能替代真实 iOS/Android 麦克风、扬声器/AEC、弱网、前后台和蓝牙验收。
- H5 的真实登录、登录后语音、设备声学和弱网验收仍开放；浏览器、单测、容器与 Provider smoke
  均不能代替这些结论。
