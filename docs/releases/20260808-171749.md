# 20260808-171749 Release

## 状态

- 候选源码门禁已在本地通过；生产切流前仍必须等待本 tag 的 GitHub Actions 全部成功。
- 生产基线与直接回滚目标均为 runtime/H5 `20260807-163916`。
- 被拒绝的 `20260808-161814` 未部署且不得复用；本 release 使用新的 annotated tag 和
  commit-bound 工件。

## 变更摘要

- 发布 Agent 自我进化控制面：三层轨迹验证、候选生命周期、generation receipt、删除 tombstone /
  read lease、独立 PostgreSQL 角色与 FORCE RLS。
- 修复账户治理测试被插入 `_fixture` 返回边界的问题，恢复真实 fixture，并用 evolution tombstone
  覆盖删除期间导出 fail-closed。
- 修复 PostgreSQL evolution 账户导出 join 的未限定字段；所有 joined activation/lifecycle 字段显式
  绑定来源表，避免 `reason` 与 candidate 同名列发生 `AmbiguousColumnError`。
- 修正生产 runbook 的候选暂存、immutable union、隔离 smoke 与 forward-only PostgreSQL 升级顺序。
- 恢复项目内小程序 `upload:test` 工具与 lockfile；真实上传要求 Git 工作区干净、私钥精确为 `0600`，
  并且只创建体验版，不自动提审或正式发布。

## 本地门禁

- Python：`2020 passed, 3 skipped`，PostgreSQL 17/pgvector 合同实际执行；总覆盖率 `87.01%`。
- Coverage：全仓 `>=85%`、orchestration `90%`、provider protocol `97%`。
- Ruff、strict mypy（261 个源文件）、media proto reproducibility/lint、media smoke/replay/chaos/load、
  offline E2E、offline provider smoke：通过。
- H5：`304/304`，production build 成功；主 bundle 的 `>500 kB` 警告为既有非阻断项。
- Mini Program：`86/86`、全部 JS syntax 通过、production dependency audit 0；lock 中 1103 个
  registry package 均有 integrity，`npm ci --ignore-scripts` 可复现；`0.8.66` dry-run 通过且未上传。
- 微信官方最新稳定 `miniprogram-ci@2.1.31` 的仅开发期依赖仍报告 76 个已知 advisory（其中 41 个
  critical）；它不进入小程序包，安装时禁用 lifecycle scripts，但发布机工具链风险仍需后续跟踪。
- `git diff --check`：通过。

## 部署范围

- Runtime：四个 amd64 镜像与源码、manifest、独立 verifier 全部绑定 annotated tag target。
- PostgreSQL：首次安装 `memoria_evolution` 角色、controller tables、policy 与 FORCE RLS；DDL 只允许
  走 `scripts/upgrade_evolution_postgres.sh` 的 forward-only 流程。
- H5：客户端业务源码相对生产基线无变化，但仍生成本 release provenance 包、合并历史 immutable
  assets，并在 runtime 10/10 readiness 后最后切换。
- Mini Program：上传体验版 `0.8.66`；它与 gateway 容器发布是两条独立发布面，不提审、不正式发布。

## 回滚

- H5-only 问题先原子切回发布前记录的 H5 tag。
- Runtime/readiness 问题恢复本 release 创建的 Control API、Agent、Speaker Model、Gateway env 备份，
  再原子切回 `20260807-163916` 并重跑 readiness。
- evolution DDL 是 additive/forward-only；不得为代码回滚删除角色、表或 policy，也不得自动恢复
  SQLite、PostgreSQL 或 MinIO。
- 具体命令只使用 `docs/production-deployment.md` 的完整回滚章节。

## Source identity

- Tag: `20260808-171749`
- Source commit: 以 annotated tag target 和 release manifest 中的 40 字符 commit 为唯一权威值；
  release 文档不手写可能漂移的 commit。
