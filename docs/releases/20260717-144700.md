# Release 20260717-144700

## 目标

上线三路语音 A/B（级联 / Omni Flash / Omni Plus）并补齐 P0 真人感能力：主动附和可观测、Delivery/TTS 副语言改写、interaction phase、Omni semantic_vad 可配置扫参。

## 变更摘要

- H5 主页三选一：`cascade` / `qwen_omni` / `qwen_omni_plus`，刷新持久化，通话中锁定。
- Control API 按后端分流 `qwen3.5-omni-flash-realtime` 与 `qwen3.5-omni-plus-realtime`。
- 级联默认开启 listener cues；发布 `backchannel` / `thinking_silent` / `speaking` 等交互相位。
- CosyVoice 首段可注入软笑前缀；`COSYVOICE_PARALINGUISTIC_TAGS=false` 时用自然语言「呵，」。
- Omni VAD 支持 env 扫参：`QWEN_OMNI_*` 与可选 `QWEN_OMNI_PLUS_*`。

## 本地门禁

- Ruff / mypy strict：PASS。
- Python pytest：全量 PASS。
- H5：62 passed；production build PASS。

## 生产发布

- 原子激活 runtime 后 Provider/readiness 通过，最后原子激活 H5。
- readiness 绑定：`20260717-144700 / qwen`。
- Control API 镜像：`sha256:50671164d612d72827396b654c6be988bafce8add6081d602aa3a2863578efe4`。
- Agent 镜像：`sha256:30b215b083abb15b9a24ecec7b4c06396df40271929826147a8fad9a544b7e6e`。
- 发布前 SQLite 双份快照 SHA-256：`7322d4843ec6207a12f66d5e79ccabbcda130f56fd883f17b2533257d66f4c7f`。
- 环境备份：`/var/backups/memoria/memoria.env-pre-20260717-144700`。
- H5 资源：
  - `index.html` `eba9a484fb3686d30483df011d6ccee6eced94d566de0720dce2f22a7592ae58`
  - `index-99IFBbKz.js` `395101c6155356a489fbb6961086ddb3ee9ac75a3e4a5680706048c064be3f5d`
  - `index-Bjubq2vL.css` `24ce2be43799826f11235d400c0797b938322196a2bc7a90a130f9c0a5fb59e6`
- 公网：根页 / H5 / SPA / live / ready 均为 200；ready release=`20260717-144700`；internal 404。
- 公网 JS bundle 含 `Omni Flash` / `Omni Plus` / `thinking_silent`。
- 本轮未修改 Nginx，仅 `nginx -t` 通过。

## 回滚

- 直接回滚点：runtime/H5 `20260717-123551`。
- 恢复 `/etc/memoria.env` 可用：`/var/backups/memoria/memoria.env-pre-20260717-144700`。

## 待用户验收

- 三路 A/B：级联 / Omni Flash / Omni Plus 真机听感。
- 级联听时附和（嗯/我在听）与回声是否可接受。
- Omni Plus 首句与自然度是否优于 Flash。
