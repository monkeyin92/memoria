# 实时语音可靠性热修 Release 20260724-121900

## 状态

- 预发布：准备从 runtime `20260723-223448` 热更新至本版本；生产 H5 继续保持
  `20260723-192611`，本次没有 H5 源码变更。
- 目标是修复真实浏览器会话中“说完偶尔没有回复”与“说再见时两个声音重叠”；
  不新增配置、依赖、数据库迁移或权限策略。

## 修复范围

- 当 LiveKit 的 endpoint FINAL 缺少 started/stopped 时间戳时，只有同时具备当前
  VAD epoch、该 epoch 的 PCM 与 fresh-speech 标记才作为真实语音接收；没有这些
  证据的孤立 FINAL 仍按回声保护拒绝，避免把播放回声放入聊天。
- 中断提示音只属于 `INTERRUPT_COMMAND`（如“停一下”“别说了”）。普通聊天、
  “打断后提问”与空候选不会再另发控制提示音，因此不会和正式回答的 TTS 重叠。
- “交还话权”不再提前清除播放 fence。LiveKit 迟到的 `playback_finished` 仍可用原
  generation 完成已听内容、历史资格与档案归档；下一用户话轮或下一次播放自然替换它。

## 回归与本地门禁

- 先以失败测试复现：
  - 普通“再见”在播放期错误触发“嗯，你说。”；
  - 当前 VAD + PCM、但 endpoint 缺时间戳的 FINAL 被误判
    `missing_speech_epoch`。
- 新增正常聊天不播放提示音且保留播放 fence、无时间戳有效 VAD 接收、无当前 VAD
  孤立 FINAL 继续拒绝三项回归。
- `services/agent/tests/unit/test_duplex_runtime_wiring.py`、
  `test_utterance_router.py`、`test_target_speaker_focus.py`、
  `test_speaker_authority_wiring.py` 相关回归均通过。
- Python 全量测试、`uv run ruff check .`、`uv run mypy services/agent/src --strict`
  与 `git diff --check` 均通过。

## 发布与回滚方案

- 本次只切 runtime，仍按 commit-bound 三件套（source、images、H5 artifact）生成、
  校验、候选 smoke；H5 artifact 只用于 manifest/候选验证，不切公网 H5 软链。
- 切流前新建 SQLite 双快照、PostgreSQL custom dump、三份 root-only env 与 Nginx
  配置备份。普通回滚只把 runtime 原子切回 `20260723-223448` 并刷新 readiness，
  不自动恢复数据。
- 发布完成后的固定工件、候选/生产验收、精确 source commit 与运行时切换时间将补入本文。
