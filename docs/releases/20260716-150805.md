# Release 20260716-150805

## 范围

- 同时发布 Agent runtime 与 H5；LiveKit、Nginx、证书、防火墙和公网端口均未修改。
- 用户字幕只展示 FunASR final，避免 `It's` 等会被改写的 interim 闪现。
- 保留语义分段但不再发送 `FlushSentinel`，每次助手回复使用一个连续 CosyVoice 流。
- 完整播放结束后的 800ms 内忽略带标点的短 backchannel 尾音；FunASR/endpoint 调整为 `550ms / 900ms`。

## 工件与备份

| 工件 | SHA-256 / Image ID |
| --- | --- |
| `memoria-agent:20260716-150805` | `sha256:ba3c1f2ae0a6635dbe783763a8cb05fa6f7b4a82034830d1db1821912b072ccf` |
| `memoria-control-api:20260716-150805` | `sha256:d32aef6ff28c82548eb46df35b028d67003d3a08f1ac3a955aba908aab330d85` |
| H5 `index.html` | `b6c9f9fc35433ca698cd4af82dd84e4aea065189d1c82eda561b714af35bd23e` |
| H5 主 JS | `952961c2cd400d86e3cd1eda888b0039abf40cb9524f966728b98ed6c7d16d7a` |
| `services/agent/src/agent.py` | `94630aaa1572e8674d743177be65045c9e19f9928c7863792cbe84efea0d3ddc` |
| `services/agent/src/duplex_runtime.py` | `cabb1ba07ed9140cce963e8b4b08a7aec0d5a69740d36ce4065f90d5e2e0e63d` |
| `services/agent/src/providers/funasr_stt.py` | `566846bd389a25588763a7b6ec0c027911c91a86998c3ab35099b38b98f87206` |
| SQLite 发布前快照（两份相同） | `3aad7080742d598d299fa9224ca2a8aa4c810d6a9216c99b9ad970f632456271` |
| `/etc/memoria.env` 发布前保护副本 | `9af12ba834cc8d1f560ca5013b1090c94c94351103780a9d742a1822cfd50cb5` |

两份 SQLite 快照均为 `root:root 0600`，`integrity_check=ok`，foreign-key violations 为 0；环境保护副本同为 `root:root 0600`。

## 验证

- 精确回归先稳定失败：用户 interim `It's` 被显示；一句上海介绍产生 3 个独立 TTS segment；修复后两项均通过。
- Python：207/207；H5：39/39，其中 `useVoiceSession` 19/19；Ruff、mypy strict、H5 production build 全部通过。
- 两个 runtime 镜像均为 `amd64`，Control API 与 Agent 均为 healthy；启动后 error/failed/timeout 计数为 0。
- LiveKit RoomService、FunASR、Qwen（thinking disabled）、CosyVoice 实网 smoke 全部通过；readiness 绑定 release `20260716-150805`、provider `qwen`。
- 公网根 H5、兼容路径、SPA、live、ready 均为 200；internal、PocketSparks、Goods Invoice 原路径均为 404。
- 本地 build、服务器版本目录和公网响应的 `index.html` 与主 JS SHA-256 一致。
- `7882/UDP`、LiveKit 容器、Nginx 和防火墙未修改；WMS、MySQL 与 Goods Invoice 继续 inactive/disabled。

## 回滚

- Runtime：`20260716-141337`。
- H5：`20260716-085218`。
- 回滚 runtime 前恢复 `/var/backups/memoria/memoria.env-pre-20260716-150805`，普通回滚不恢复数据库。
