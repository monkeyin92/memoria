# Release 20260717-175400

## 目标

落地规则版 **UtteranceRouter**，统一 enroll / pure interrupt / interrupt+chat / chat 的话轮意图，避免 turn-commit 与 barge-in 平行 if 再撞车。

## 变更

- 新增 `services/agent/src/orchestration/utterance_router.py`：有序规则表 + `UtteranceRoute` 策略标志。
- `DuplexRuntime.accept_user_turn` 与 `on_real_interrupt` 共用 `route_utterance`。
- 单测 `tests/unit/test_utterance_router.py`（表驱动）。
- 工作约定写入根 `AGENTS.md`（举一反三 / 优先改 Router）。

## 未变

- H5 无变更（沿用上一版静态资源）。
- 播放期 noise / backchannel / echo 仍在 `PlaybackInputGuard`。
- env / Nginx / LiveKit 无变更。

## 回滚

- Runtime / H5 上一版：`20260717-165200`。
