# 实时语音回复热修 Release 20260723-223448

## 状态

- 已发布：runtime 于 2026-07-23 23:07:37 CST 原子切换至
  `20260723-223448`；生产 H5 有意保持 `20260723-192611`，本次无 H5 源码变更。
- 上一生产 runtime/H5：`20260723-192611`。
- source commit/tag：`b58d6a2e71a9f1a8bdffb26f9638c91c9048f839` /
  `20260723-223448`。

## 修复范围

- 普通 `companion` 话轮在提交 generation 后统一绑定当前 TTS 音色快照，避免
  response provenance 因缺少实际音色而在 LLM 前 fail closed。关闭“过滤明显旁人（实验）”
  时，`uncertain / shadow_guest_candidate` 仍按现有权限边界聊天，不升级 owner、私人记忆、
  工具或主人历史资格。
- 同一 speaker epoch 复用唯一分类 task，避免迟到回调以相同确定性 event ID、不同 fence
  重发 `speaker.classified` 并触发 Archive 409。
- waiter 取消通过 `asyncio.shield` 与共享分类 task 隔离；新 VAD epoch 和 runtime close
  仍可取消旧 task，不会把取消结果冻结到下一话轮。
- 更新两个历史语音测试夹具，使其实现当前 Doubao 设计音色的完整审计字段；未新增依赖、
  配置项、数据库迁移或 H5 行为。

## 本地门禁

- PostgreSQL 17 + pgvector 正式临时环境：`1179 passed, 2 skipped, 0 failed`。
- 总覆盖率 `88.76%`，orchestration `92%`，provider protocol `92%`，均通过正式门槛。
- Ruff、148 个 strict mypy source files、compileall、`git diff --check` 通过。
- H5：19 files / 232 tests，production build 通过。
- 离线 ASR → LLM → TTS E2E 通过，旧 generation/tool epoch 音频均为 0。
- Production Compose 合同 22 tests 通过；本地 Compose 解析需要服务器 root-only env，
  在候选服务器 smoke 与切流前再次验证。
- Standards / Spec 双轴审查无剩余 P0/P1/P2；审查发现的 waiter cancellation P1 已补
  回归并复审通过。

## 发布方案

- 基础镜像：`20260723-192611`；使用增量构建生成绑定本次 commit/tag 的 Agent、
  Control API 与 Speaker Model 三镜像。
- H5 仍需构建、打包并进入 commit-bound release manifest 和隔离 server smoke，但不切换
  公网 H5 软链。
- 不旋转 capability token，不修改三份生产 env 内容，不重启 PostgreSQL、MinIO、LiveKit，
  不 reload Nginx。
- 切流前创建 SQLite 双快照、PostgreSQL custom dump 和三份 root-only env 备份；普通回滚
  只恢复 runtime `20260723-192611` 并刷新 readiness，不自动恢复数据库。

## 发布前数据与回滚

- 普通 runtime 回滚点为 `20260723-192611`；H5 没有切换，无需回切。SQLite、PostgreSQL、
  MinIO 与 LiveKit 均未在本次切换中重启或恢复。
- SQLite backup API 双快照均通过 `integrity_check=ok`、`foreign_key_check=0`，
  SHA-256 同为
  `1bab2274f821e48de6bb1aa051419dd2d07d7304b514d19f0d6842333cb55f89`：
  - `/var/lib/memoria/memoria-pre-20260723-223448.sqlite3`
  - `/var/backups/memoria/memoria-pre-20260723-223448.sqlite3`
- PostgreSQL 17 custom dump 已在独立同版本容器以 `pg_restore --list` 验证：
  `/var/backups/memoria/memoria-pre-20260723-223448.dump`，
  SHA-256 `0fa497906cfbe6057316bc1fc19339a90c36d66a6a52ea3795f472a7e88283d9`。
- 三份未旋转的生产 env 已按 `root:root 0600` 备份：
  - Control API：`93babe7fb0c1522c8ad99096ee65b3fb4046737c3c045c5f188116f6f55380f5`
  - Agent：`a5140c50f39908cc9706ebdd1855036d234a56d33109ac1ac0a7bf80a40a9317`
  - Speaker Model：`c87511651a6af7561a5990d3d67dcdfe5d0012012fc2cf9438806ec2e3ab9476`
  文件路径均为 `/var/backups/memoria/memoria-<service>.env-pre-20260723-223448`。
- 原 Nginx 配置保存于 `/var/backups/memoria/nginx-20260723-223448`；未 reload Nginx。

## 固定工件

- source archive：`b3834dd5983e6ef39d4d02921b035937177f8bf3657af32f202ee25db29a19b3`
- images archive：`7a289d45361be7a18cb60de34003a29d3520da4e0dfa8c7e38040289900ec556`
- H5 artifact：`bc5d9f81a9b720e6b3fef2e9b030f241a80caf0b24685b27db5c54956f93a566`
  （已进入 manifest/候选 smoke，公网 H5 未切换）
- release manifest：`8e72f45b8a8ac7e9fbd143f414bc070d1486d80d80e5fc89f661842ebcf9d9f8`
- release verifier：`bd2170a2e15538e426493ebd552f73d367f7b03e66ddfa49f25eea780d625842`
- Linux amd64 导入镜像 ID：
  - Agent：`sha256:04afe448ea03b3aa71801604ea91760807b6dad8be4e67d44cb8abbd59d48104`
  - Control API：`sha256:ae54e2d8456d78da5163c91547769a23fc9a65c4ed46c9de49454c1276d6298f`
  - Speaker Model：`sha256:267475fc185d308011d3f50e5a554404e71d6ce22e38c0de8f8f61d8013888f0`

## 生产验收

- 切流前，生产 Compose 解析、候选镜像 import、唯一
  `duplex-zh-agent-candidate-223448` 的 LiveKit 注册、accepted heartbeat 与
  容器内 heartbeat health 均通过；隔离 `smoke_server_deployment.sh` 亦通过。
- 切流后 Provider/readiness 全通过：LiveKit 鉴权、FunASR interim/final/词时间戳、
  Qwen streaming、Doubao `seed-tts-2.0` PCM16/24 kHz/mono/词时间戳/CancelSession，
  `verify_env` 与 readiness mark/check 均绑定 `20260723-223448`。
- 当前本机与公网 `/memoria-api/health/ready` 都返回
  `ready / 20260723-223448`，Agent ready，Qwen/Doubao ready，9/9 core checks ready；
  `agent / control-api / speaker-model` 均 healthy、restart 0。Nginx 语法检查、WMS、
  readiness 与 certbot timer 均正常，IP TLS 证书有效至 2026-07-26 21:15:49 UTC。
- 公网 H5 保持 `20260723-192611`：
  `index.html` 为
  `182d967a564c80f22a73b8b4263fd92a610472deec0b2ce7c9c2297fb6fe6bb5`，
  `memoria-release.json` 为
  `0e8e7d4f9669c18a9c1eb04e813638d3a0c5943c79a220beade655ff1c1a0fb0`；
  root、H5、`/memoria-api/health/live`、`/memoria-api/health/ready` 与 `/wms/`
  均为 200。
- 2026-07-24 11:56:34 CST 的真实浏览器 companion 测试确认
  `reject_non_owner_voice=0`。Agent 收到 speaker authority 结果且
  `target_speaker_rejected=0`，后续出现
  `turn_committed → response_plan_cached → llm_request_started →
  llm_first_content_token → tts_task_started → tts_first_audio_received`，
  浏览器上报 `first_playback`；这证明此次不再卡在 response plan 之后、LLM 之前。
- 从切流到该真实测试后的日志观察超过 12 小时：三容器持续 healthy、restart 0、archive
  spool 0；`response provenance bind failed closed`、generation voice binding rejected、
  Archive 409、archive spool/durable evidence failure、Traceback、ImportError 均为 0。
