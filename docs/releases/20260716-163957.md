# Release 20260716-163957（已回滚）

## 状态

该 Runtime 于 `2026-07-16T09:00:44Z` 短暂激活，但未通过独立发布审计，已于 `2026-07-16T09:11:50Z` 回滚到 `20260716-150805`。它不是正式版本，不得作为当前生产状态或回滚目标引用；修正版为 `20260716-171958`。

## 回滚原因

- 播放结束后 800ms 内，助手英文尾音仍可能被 FunASR 识别为 `it's` 并创建新 turn。
- 自建档案把 LiveKit `min_words` 固定为 5，会阻塞“停”“等等”“不是”等真实短打断，不符合全双工交互要求。
- 单个超长首段没有在 96 个有效字符处截断，回复预算只覆盖了多段输出。
- `guarded_user_input_total` 只有会话内 registry 分类计数和结构化日志，生产没有启动或暴露 Prometheus endpoint，原记录的表述容易误解为已对外提供指标。

## 被拒绝的工件

| 工件 | SHA-256 / Image ID |
| --- | --- |
| `memoria-agent:20260716-163957` | `sha256:fb29ea05ab171dadda306680d7c51bf814ce63685a96e75a00d338ea3bcf9bdb` |
| `memoria-control-api:20260716-163957` | `sha256:f2f65305966f0a1384b0a6e7f3ff18ac83d68d6e55dcd210e929e9067b383f6d` |

这些工件仅保留为事故追溯证据，不得重新激活。该次发布前的环境备份仍保留在 `/var/backups/memoria/memoria.env-pre-20260716-163957`。

## 后续

上述三个阻断问题均已写成确定性回归测试，在 `20260716-171958` 中修复并重新完成完整生产门禁；正式证据见 `docs/releases/20260716-171958.md`。
