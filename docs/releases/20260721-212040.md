# “仅听主人”与主人历史隔离 Release 20260721-212040

## 状态

- 已发布：runtime 于 2026-07-21 21:48 CST 切换，H5 随后按 runtime-first /
  H5-last 激活；当前二者均为 `20260721-212040`。
- 上一生产基线为 runtime/H5 `20260721-181810`。
- runtime 回滚点为 `20260721-181810`；H5 回滚点为
  `20260721-181810-rollback-union-20260721-212040`。
- 本次只执行 SQLite 幂等加列，没有 PostgreSQL schema 变更，也没有创建生产测试账号。

## 产品与安全行为

- Profile 新增 `reject_non_owner_voice`，默认 `true`。H5“我的 → 陪伴偏好”新增
  “仅听主人”开关；设置保存成功后从下一次发言生效。
- 开启时 formal/shadow guest/ambiguous 的普通话轮与播放控制均拒绝，明确“停一下”也
  不能绕过；关闭后访客可聊天和打断，但权限始终保持 non-owner。
- 策略由 Control API 按可信 `session_id → account → Profile` 解析；浏览器不向
  create-session 或 room metadata 注入策略。
- Agent 按 `(turn_id, generation_id)` 冻结 `history_eligible`，仅 formal owner /
  `shadow_owner_candidate` 的用户与对应 AI 权威终稿可进入主人历史。访客、
  ambiguous、无档案、authority 不可用和缺字段话轮均 fail-closed。
- 非 owner 的 Qwen 上下文只保留当前话轮，不读取私人记忆或工具，并禁止冒充亲属与泄露
  账户主人上下文。

## 过时项清理

- 删除零引用 `QWEN_OMNI_MODEL` alias 与 deprecated
  `isOmniInterruptCommandOnly`。
- 删除当前 Doubao Agent 不再读取的 AgentSettings CosyVoice 字段、validator 与
  split-env 旧 runtime keys；CosyVoice provider、历史预览/评估/撤销治理继续保留。
- 删除 Hydra `outputs/` 生成物并加入忽略规则；runtime source artifact 排除 legacy
  `apps/web`、env、缓存、数据与构建产物。
- 离线 ASR→LLM→TTS 门禁由 CosyVoice mock 迁移到当前 Doubao mock。
- 修复 live server smoke 的旧假设：候选 Control API/H5 使用隔离端口
  `18791/18891`、候选静态目录与 `/data` 临时存储，不再占用在线 `8791`。
- 历史 release/ADR/research、Omni 隔离 A/B、数据库迁移和 CosyVoice 治理均有审计或
  回滚价值，未删除。

## 发布前门禁

- Python 全量：`704 passed, 21 skipped`。
- 覆盖率：`82.05%`，低于仓库 `85%`；沿用既有 waiver，不能表述为覆盖率门禁通过。
- H5：12 个测试文件、`136/136`，Vite production build 通过。
- Ruff、strict mypy、`git diff --check` 与 Doubao 离线 E2E 通过。
- Candidate server smoke 通过：候选 H5、SPA fallback、API proxy、
  `reject_non_owner_voice` 默认 `true`、保存 `false` 以及 SQLite 重启持久化。
- 本地 amd64 镜像：
  - Agent：`sha256:849f8b52280bac6844c0b7f3619c4084d4408ba7a4598981786e63ae5f33f8c6`
  - Control API：`sha256:b31fe6cac3d95543e5fb7d0e822a151145e9e5970efb02d4e53df67f022da8df`
  - Speaker Model：复用上一健康 amd64 内容。

## 固定工件

- `images.tar.zst`：
  `655dff0c7268c6eb384024df02297d6d6d822ad362ee9ad5c1ddb94a1d20f39b`
- `source.tar.gz`：
  `1ba54465494678a86f53fc19f787134c1ddaf9d40ccaa46eee62c2747b376c23`
- `h5.tar.gz`：
  `18e92eb768ab448e8fd7f01d815f430e108a9244b61b897899ae7909a0991f39`
- 服务器逐项 `sha256sum -c` 与 `zstd -t` 均通过。

## 数据备份与迁移

- 切换前 SQLite 不含 `reject_non_owner_voice`；backup API 双份快照均为
  `root:root 0600`，`integrity_check=ok`、`foreign_key_check=0`：
  - `/var/lib/memoria/memoria-pre-20260721-212040.sqlite3`
  - `/var/backups/memoria/memoria-pre-20260721-212040.sqlite3`
  - 共同 SHA-256：
    `39c930603199037b05770169459fcbc702619e5b912f3ae9e5fa8741101d7fff`
- 双 env 备份为 `root:root 0600`：
  - Control API：
    `aef69832adbe9916d55da2f9ec10d4096bf47575077cae1373b29acfdb7ca502`
  - Agent：
    `f2745253f34810350ec3c2953e4b36818f1a63db80ddf6042dbc8ac0a391097a`
- 新 Control API 启动后加列为
  `INTEGER NOT NULL DEFAULT 1`；SQLite `quick_check=ok`，51/51 个既有 Profile
  均为严格模式，没有自动改成放行访客。
- 旧 runtime 会忽略新增列；常规代码回滚不恢复数据库。

## 生产验收

- 服务器 Engine 镜像：
  - Agent：`sha256:7a3079e90cf191aa8d98bf08984df2cb9312d625108c1741c84d0e906a362523`
  - Control API：`sha256:962a4ec21b2b6908196448e531f1aab209bba865ca2fd1d1a613cd524e3ecff5`
  - Speaker Model：
    `sha256:8230eea556d2c5ed5278fb176f0e6a2f71c07b1195ee2fb822aa7afa217c6d4b`
- 三应用容器均 healthy、restart 0；LiveKit、FunASR、Qwen、Doubao PCM/字时间戳/
  CancelSession smoke 全部通过。
- readiness 为 `ready / 20260721-212040`：LLM `qwen`、TTS `doubao`，
  9/9 core checks ready。
- IP 与域名的 root/live/ready 均 200；internal、PocketSparks、Goods Invoice 为 404；
  WMS `/wms/`、`/wms/api/` 为 200，`wms.service` active/enabled。
- IP 证书 SAN 含 `122.51.108.140`，域名 SNI 含 `aigcnice.com` 与
  `www.aigcnice.com`；两个续期/readiness timer active。
- H5 candidate 与 rollback-union 均包含 334 个 immutable assets；334 个候选资源经公网
  逐一返回成功。
- H5 固定文件：
  - `index.html`：
    `ab31e529cbe7354582625cc3beba46ff60aadfb86d8d5c46e53e953145ffbfdc`
  - `index-DCNMNBy9.js`：
    `04eee133f3775e6645671747164f5a63d33151e5f512459d972024e0adfe3abd`
  - `index-C6TCaVi0.css`：
    `8b2f46be52b9ebb4d85daffaa7f8015f28559dfbd95b0d7c46a8be1e1a691da0`
- 内置浏览器在 IP/域名均加载上述新 bundle；390×844 与 667×375 无横向溢出，
  图片完整，console 0 warning/error。

## 发布过程保护

- 首次 runtime 切换在 `docker compose config` 未传 tag 时触发自动保护；新容器尚未
  创建即回到 `181810`，随后旧版 LiveKit/Provider/readiness 全部复验通过。
- 修正 config 与 up 均显式传 `MEMORIA_RELEASE_TAG` 后重新切换成功。
- H5 只在 runtime、Provider、readiness、Nginx、证书和 WMS 门禁全部通过后激活。

## 回滚

runtime 回滚前先恢复双 env，然后切回：

```bash
sudo install -o root -g root -m 0600 \
  /var/backups/memoria/memoria-control-api.env-pre-20260721-212040 \
  /etc/memoria-control-api.env
sudo install -o root -g root -m 0600 \
  /var/backups/memoria/memoria-agent.env-pre-20260721-212040 \
  /etc/memoria-agent.env
sudo ln -s releases/20260721-181810 /opt/memoria/.current.rollback-20260721-212040
sudo mv -Tf /opt/memoria/.current.rollback-20260721-212040 /opt/memoria/current
cd /opt/memoria/current
sudo -E env MEMORIA_RELEASE_TAG=20260721-181810 \
  docker compose -f docker-compose.production.yml up -d --no-build --wait
sudo /opt/memoria/current/scripts/refresh_readiness.sh </dev/null
```

H5 单独回滚：

```bash
sudo ln -s \
  memoria-releases/20260721-181810-rollback-union-20260721-212040 \
  /var/www/.memoria-h5.rollback-20260721-212040
sudo mv -Tf /var/www/.memoria-h5.rollback-20260721-212040 \
  /var/www/memoria-h5
```

## 未关闭风险

- “仅听主人”仍需真实设备覆盖主人/孩子、短句“停一下”、耳机/扬声器、噪声与弱网。
- 既有访客历史未回溯删除；数据修复需另走有备份、可审计流程。
- 滋滋声尚缺同 generation 原始 PCM、远端 MediaStream 和设备外录，不能从现有日志
  归责豆包或 WebRTC。
- 括号舞台指令朗读已定因，但本 release 未实现括号过滤或确定性笑声映射。
- 当前 release 来自本地 dirty worktree（`HEAD=411b2ea`），未创建 Git commit/tag；
  复现以本节固定 artifact SHA-256 为准。
