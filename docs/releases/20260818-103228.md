# Memoria Runtime Release 20260818-103228

## 发布目标

- 修复主体 category/age 权威事实不完整时形成 `confirmed + unknown` 非法混合态的问题，统一降级为完整 `unknown_safe`。
- 将绑定归属 `subject_id` 与当前 Runtime Profile 主体 `active_subject_id` 分离，并让 Direct JWT 显式携带空主体 fence。
- 冻结设备媒体重连的 account/device/client/subject/binding/profile authority，仅允许 `stream_epoch` 递增。
- 在 Provider epoch reset 失败时同时清理 Registry 与新 Bridge epoch，避免半切换 runtime 残留。

## 授权范围

- 切换：Control API（含 Session Runtime）、Agent、Voice Core Media Bridge、Go Media Edge。
- 不切换：ESP32 固件、Nginx、H5、小程序、PostgreSQL、Redis、MinIO、LiveKit、Speaker Model 与两个兼容 Gateway。
- Control API 启动时仅执行向后兼容的 SQLite nullable `active_subject_id` 加列及旧行一次性回填；不执行 PostgreSQL migration。

## 发布前门禁

- Control API 聚焦回归：57 项。
- Agent/Voice Core 聚焦回归：228 项。
- Session Runtime 纯内存：32 项；隔离 PostgreSQL 文件回归：43 项。
- Go Media Edge `go test ./...`、Ruff、strict MyPy（399 个源码文件）、模块预算、`git diff --check` 与架构 YAML 解析通过。
- 一次性本地 PostgreSQL 容器已删除。

## 预切流回滚点

- Control API：`sha256:02d4d86a349c2dd255f44fe0229c93e5d1a82337bde6cb81a418e22f0bec40e2`。
- Agent/Bridge：`sha256:163c4d534fa06a4b796a5217fcaead32ea97162a108bef1c4c6237f94238cb4e`。
- Media Edge：`sha256:7111c7855a3ab100d06b0b3a83a37c6e615a5c81cbff9416e00675817295b3c6`。
- root-only 证据目录：`/opt/memoria/direct-canaries/20260818-103228/`。
- `PRE_CUTOVER_STATE.txt` SHA-256：`9b0d19a59ed60af951570eaad6648e1fb7db539fbe457a1a1aa1fa70a23b727c`。
- `PRE_CUTOVER_CONTAINERS.json` SHA-256：`614f66de5fe8f7e0ffae12dc7c89b04a9e609a6a9297ac7fa3f02f444107192c`。

## 部署与生产只读复核

- 源提交与 annotated tag：`fb45fad59165ec819ac5b03148cba5c537b7eb6e` / `20260818-103228`；tag 不移动。切换范围严格为 Control API（含 Session Runtime）、Agent、Voice Core Media Bridge、Go Media Edge。
- Control API：`memoria-control-api:20260818-103228`，image digest `sha256:14916cf1d00e1c10a8a8f52b5aad64eb301949f5e2319a02125484d3ed11e891`。
- Agent / Voice Core Media Bridge：`memoria-agent:20260818-103228`，image digest `sha256:549f5f29176b6ad3361290726abecff748b804b5cb3473fd7ea6fc5e37784d65`。
- Go Media Edge：`memoria-media-edge:20260818-103228`，image digest `sha256:d512fd348f9424cbeaccf091b9deeb50962dc83e5f3c598675dd2b5081697261`。
- 观察时间 `2026-08-18T04:56:23Z`：四个目标容器均 running/healthy、restart count=0、OCI revision 为源提交。Control API/Agent runtime fence 为 `20260814-231749-direct-canary`，Bridge fence 为 `20260816-bridge-liveness-83af813`。
- 探针通过：公网 live/ready=200；未认证 archive read/write=401；`memoria.media.v1.VoiceMediaBridge=SERVING`；Media Edge mTLS `/readyz`=200/ready。切流后 `active_device_connections=0`、`active_media_sessions=0`、liveness healthy=1、probe successes=4060、failures=0、redial attempts/successes/failures=0/0/0。
- 切流后状态证据：`/opt/memoria/direct-canaries/20260818-103228/POST_CUTOVER_STATE.txt`，SHA-256 `41060c9838f1c1db0b38e52c906292f9b14bd025e13df9539dc3bd6e9fa98bc8`；切流回执：`/opt/memoria/direct-canaries/20260818-103228/CUTOVER_RESULT.txt`，SHA-256 `37da7f571630c0585ab350464b789196cd3075998758a3c43d611ab240b53848`。
- 层级：`code=true`、`wired=true`、`enabled=true`、`production_runtime_verified=true`（仅服务器健康/readiness/gRPC Health/mTLS readyz/metrics/fail-closed 路由与零活跃会话范围）。这不等同于 Direct 真机或全双工验收。

## 制品清理

- 清理完成时间：`2026-08-18T02:57:28Z`。
- 删除历史目录 55 个、旧 `memoria-*` 镜像标签 90 个，失败 0；Docker build cache=0。
- 清理后磁盘：118G total / 50G used / 65G available / 44%；Docker images 34/13 active，containers 18/14 active，local volumes 5/4 active。
- 未执行 broad `docker system prune`，未删除命名卷、数据库、Redis、MinIO、LiveKit 或合规备份；当前版本与即时可运行 rollback 点保留。

## 验收边界

- 当前候选没有刷写 ESP32、没有候选设备音频会话、没有 Exact DAC watermark、AEC Reference、Double-talk、精确 Actual Heard、100/500 轮稳定性或 T14 微信 iOS/Android 证据。
- 保持 `direct_real_device_verified=false`、`full_duplex_verified=false`，T1–T14 为 `0 pass / 14 blocked / 0 failed`。
- 服务器健康只记录 `enabled / production runtime verified`，不能替代真实板卡语音闭环；后续真机测试需在当前候选固件刷写并形成同一 generation 的设备、服务、播放回执与用户 Actual Heard 证据后再推进。
