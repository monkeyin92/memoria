# 小程序播放期显式打断竞态热修 Release 20260725-211256

## 发布目标

- 消除“停一下”已被 Router 判定为明确控制语后，先恢复 LiveKit 自动打断阈值、再异步停止
  播放的竞态。
- 让播放期明确控制语始终先走受控的停止回调；自动阈值只在没有可用停止回调时才作为回退。

## 根因与修复

- `DuplexRuntime` 曾在播放期接受候选输入后先将 `min_words` 恢复为基础值。LiveKit 自动
  打断可以在异步控制回调之前抢先触发，使实际行为依赖事件调度顺序，无法证明“停一下”走了
  Router 的确定控制路径。
- 现在先请求已接线的 LiveKit 停止；回调成功挂起后保持播放保护阈值，`on_real_interrupt`
  完成后统一恢复基础阈值。没有停止回调时才恢复原阈值继续走既有回退。
- 回归测试固定“停止回调观测到 `min_words=1000`”以及“迟到的 playback completion 不得把
  截断的已听文本覆盖为完整旧回复”。

## 发布前验证

- Agent production wiring、TargetSpeakerFocus、PlaybackInputGuard、UtteranceRouter 与
  小程序网关相关测试通过；Ruff、strict mypy 与 `git diff --check` 通过。
- 小程序逻辑测试 15/15 通过；本轮没有小程序源码变更。
- 前一 runtime `20260725-203826` 的公网合成小程序会话已验证媒体票据、下行播放、播放中
  “停一下”、generation `audio_reset` 和 `assistant_state: interrupted` 全链路可达；
  本次发布后复测 generation reset、状态变更和阈值顺序。LiveKit 的
  `playback_finished(interrupted=true)` 对受控停止和自动停止共用生命周期原因，不能以该
  原因字符串单独判定回调来源。

## 发布边界

- 仅 runtime 热修；H5 源码与小程序包源码均未改变。H5 仍只参与候选 smoke/工件校验，
  不切换 H5 软链。
- 本次上传新的微信体验版以关联本次后端热修，不提交审核或正式发布。
- 合成上行与开发者工具不能替代真机扬声器回声消除、物理麦克风、弱网、前后台和蓝牙验收。

## 发布结果

- source/tag：`6030a83498915d0f0b3497c5f6ab8fe6907cd15f / 20260725-211256`；
  runtime 于 2026-07-25 21:39 CST 原子切换，H5 继续保持
  `20260723-192611`。
- 固定工件 SHA-256：source
  `4f9d29852e0d3f66c52b7a135470a8fae0e6d294cf8550f8445caf39f7293ad2`，
  images `210a9bdb077d8fe0635da3329bc5b3bc0de6c27caa6c74fb67dd696974813540`，
  H5 `1de7e687e297bb66836572bb89f83cc1612508eb7017a011d031ce34a858a4a7`，
  manifest `553b8ddd796f11dbd22b8c20aebb360d2ffebabcfa81575c8c239f4bf2852a8c`，
  verifier `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101`。
- 候选 H5 immutable asset union（183 个文件）、隔离 server smoke、SQLite/PostgreSQL
  备份校验、四个 root-only 环境回滚点、四个 runtime 容器健康、LiveKit/FunASR/Qwen/
  Doubao provider smoke、Nginx 与 WMS 均通过；公网 readiness 为
  `ready / 20260725-211256`，非法媒体票据仍关闭为 `4401`。
- 公网匿名小程序合成会话在 Agent 下行播放中发送“停一下”后，收到 generation 2 的
  `audio_reset`（约 2171 ms）和 `assistant_state: interrupted`（约 2173 ms）。回归测试
  额外证明停止回调执行时 `min_words` 仍为 1000，迟到的 playback completion 不会覆盖
  已听到的截断文本。
- 微信开发者工具 CLI 已上传体验版 `0.8.43`（“修复语音打断、实时展示与默认表情”），
  包体为 `581.5 KB / 595418 bytes`；未提交审核或正式发布。
