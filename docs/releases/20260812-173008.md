# 20260812-173008 Release

## 状态与范围

- 基于生产版本 `20260812-163054`。同一实板已证明设备 VAD、FunASR final、话轮提交和 Future
  epoch-drain 修复真实生效，但签名 `unknown_safe` 普通对话在 LLM 前被 generation voice binding
  拒绝，设备没有收到回答。
- 根因不是 TTS provider 或说话人识别失败，而是控制面不一致：ModePolicy 明确授权 unknown-safe
  公开聊天，音色证据又按隐私要求去掉 companion profile ID；旧 designed-voice 校验和 local-safe
  response plan 却没有表达“匿名公共输出”契约。
- 新增单一 generation output policy，同时约束 generation voice、response plan、上下文和 provenance：
  仅当 mode=`unknown_safe`、conversation=true，且 private memory/persona/tools/history/learning/personal
  voice 全关闭时，才允许匿名使用目录中批准的 `seed-tts-2.0` speaker；LLM 只接收当前用户话轮，
  不接收旧聊天、私有上下文或工具。任何身份引用、未知 speaker hash、个人音色或敏感能力均拒绝。
- companion/self-preview/legacy 继续要求原有精确 profile/model/speaker SHA-256 合同；archive 未开放。
  本次不含固件、设备网关、数据库、env 或 H5 变更，回滚目标固定为 `20260812-163054`。

## 发布前门禁

- 新增真实 audio turn 回归：`uncertain + authority_unavailable` 的 unknown-safe 话轮跨过 commit、音色绑定、
  本地响应计划与 LLM；断言旧私密问题不进入上下文、工具为空、provenance 无 profile/source refs。
- 新增负向矩阵：带 profile ID、未知 speaker hash、personal voice、authority unavailable，以及虽名为
  unknown-safe 但携带 history 能力的策略全部 fail closed；原 companion/fallback 测试改用权威目录哈希。
- Agent 全目录 `1463` 项通过；定向 `182` 项通过。Ruff、相关源码 strict mypy、模块预算与
  `git diff --check` 通过；`agent.py=3410`、`duplex_runtime.py=4717`，预算均收紧未提高。
- 发布后必须重跑完整 provider/readiness 与连续 `8/8` 公网稳定性；随后用同一实板完成 VAD、
  FunASR final、response plan、LLM、Doubao 首 PCM、下行 Opus、板端播放排空。只有用户实际确认
  听到回答，才记为声学闭环完成。

## 生产结果

- 待发布。
