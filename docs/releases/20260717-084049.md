# Release 20260717-084049

## 范围

- 修复 2026-07-17 08:15–08:17 CST 真人 A/B 中级联同一物理讲话被重复提交、后半段持续自说的问题。
- 保持 `ENDPOINTING_MIN_DELAY_S=1.50`、`ENDPOINTING_MAX_DELAY_S=2.20`、`FALSE_INTERRUPTION_TIMEOUT_S=1.70` 与 FunASR 550ms 不变；本轮用 speech anchor 与 pre-EOU 门禁修根因，不继续增加等待。
- 级联在用户 turn/助手播放后将 LiveKit `min_words` 锁为 1000；只有新的真实 speaking epoch 且有效 transcript 才重开。带 LiveKit metrics 但 speaking anchors 全空的话轮 fail-closed，显式文本路径不受影响。
- FunASR 按同一 task 的 `sentence_id` 丢弃重复 FINAL；用户 final 字幕只在话轮门禁通过后发布，孤儿片段不再污染页面和消息库。
- 级联接收并记录白名单 WebRTC inbound audio 数值；两条链路补充总采样数、静音 concealment、jitter-buffer emitted count 及加减速采样数，支持计算比例和平均值。
- Omni 保持现有 WebRTC、semantic VAD 与取消隔离，仅把默认回复收紧为一到两句。
- LiveKit 生产模板将日志级别收紧为 `warn`，避免 INFO 启动日志继续记录完整 SDP 或 API key；发布时单独备份并更新生产 LiveKit 配置。

## 根因证据

- Omni 会话 `d183ba29-6667-4e48-aa6c-ff548cf9c4a0` 的 4 次 speech stop 均只对应 1 次 response created，stop → created 为 68–139ms，没有重复生成。
- 级联会话 `623cc6cf-d04e-474a-ad06-46b9182b60b2` 仅有 2 次真实 speaking epoch，却提交 6 个 turn；其中 4 个 turn 的 `started_speaking_at`、`stopped_speaking_at`、`transcription_delay`、`end_of_turn_delay` 全为空。
- LiveKit 同时记录 `transcript arrives after turn has been committed`；第一轮两条用户 final 的脱敏哈希完全一致。
- 级联首个有效 turn 的 transcription/EOU delay 为 1.72s，第二个有效 turn 的 transcription delay 为 0.91s、EOU delay 为 2.20s。设备为 iPhone Safari，经 8443/TCP，接通初段发生多次 ICE pair 切换。
- Omni 零丢包但累计 concealment 很高；旧遥测缺少归一化分母，因此本轮补字段而不据此开放公网 UDP。

## 回归反馈循环

修复前以下三项稳定失败，修复后全部通过：

```text
test_one_physical_speech_epoch_cannot_commit_more_than_once
test_entrypoint_routes_control_playback_and_ui_events
test_funasr_recognize_stream_deduplicates_final_sentence_id
```

覆盖同一 speech epoch 多次 completion、误触 speaking 但空 anchors、真正下一轮讲话、pre-EOU `min_words` 锁及同 sentence ID 重复 FINAL。

## 本地门禁

- Ruff：PASS。
- mypy strict：46 个 source files，PASS。
- Python 完整 pytest：243 passed。
- H5：57 passed；production build PASS。
- Web：28 passed；TypeScript/production build PASS。
- LiveKit `v1.13.3` 接受 `logging.level: warn` 配置：PASS。

## 生产发布

- `2026-07-17 08:55:28 CST` 原子激活 runtime，`08:56:33 CST` 在全部 readiness 门禁通过后原子激活 H5；两条软链最终均指向 `20260717-084049`。
- Control API 镜像 `sha256:72c15f265c9f3ebd6bd4777f58d82cf9857e46ac320f177f07a9eeb043026bc0`，Agent 镜像 `sha256:e3b63ac80af1773bf9ff9f2a0ef15795c69bec8017a1af58c735431e5734f533`；均为 Linux/AMD64，两个容器均 healthy。
- LiveKit `1.13.3` 生产配置已设为 `logging.level: warn`，权限保持 `root:root 0600`；重建后 RoomService 真实鉴权 smoke PASS，未再读取或输出原始 INFO 日志。
- LiveKit、FunASR、Qwen、CosyVoice 实网 smoke 全部 PASS，readiness 精确绑定 `20260717-084049 / qwen`；切换后 Agent 与 Control API 近十分钟错误关键词计数均为 0。
- 两份 SQLite 一致快照位于 `/var/lib/memoria/memoria-pre-20260717-084049.sqlite3` 与 `/var/backups/memoria/memoria-pre-20260717-084049.sqlite3`，均为 `root:root 0600`，SHA-256 均为 `1ef7a69f2130eb1aa6713bc1440ce3bb799817c20e031d552d0eedbeefa131b1`，`integrity_check=ok`、foreign-key violations 为 0。环境、LiveKit 配置与 Compose 的发布前备份均位于 root-only `/var/backups/memoria`。
- H5 `index.html`、主 JS、主 CSS SHA-256 分别为 `704e6d6d1ae794e3b1d46ae0d0ff1b983a8ac9a93bb7d5578ecd627cbdf7ffa1`、`9df7fb28dc26e60a5d44375823dd61de88fc5fceccf685886193a55cbefb9a74`、`9275210d0de69bc654f521c1863f119edaeadf7dde1782376671fd54be99dc87`，本地、服务器目录与公网响应三方一致。
- 公网根页、兼容入口、SPA、API live/ready 均为 200，internal、PocketSparks 与 Goods Invoice 旧路径仍为 404；`nginx -t` PASS，本次未 reload Nginx。
- 内置浏览器验证两个模型选项可切换且刷新保持，console warning/error 为 0。本轮没有获取浏览器麦克风授权，因此新版真人首声、抢话、级联单轮性与音质仍由用户在同一 iPhone 上 A/B 复测。
- 直接回滚点：runtime `20260717-003211`，H5 `20260717-001826`；LiveKit 配置可使用 `/var/backups/memoria/livekit.yaml-pre-20260717-084049` 独立回滚。
