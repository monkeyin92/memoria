# 小程序短命令 KWS 与 AEC 诊断 Release 20260727-204609

## 发布状态

- source commit / annotated tag：
  `d2d35ba52181e18068d8df074f3e515f406b4710 / 20260727-204609`。
- 后端 runtime 于 `2026-07-27 21:07:40 CST` 开始原子切换，
  `21:10:43 CST` 完成全部生产门禁。
- 生产 H5 保持 `20260723-192611`，没有切换 H5 软链。
- 微信小程序体验版保持 `0.8.51`；本轮没有客户端改动，因此没有重复上传、
  提交审核或正式发布。

## 发布内容

- Agent 增加可选 Vosk 受限词表识别，只消费可信小程序 AEC 后 PCM，且只在助手播放期、
  当前 VAD speech epoch 与至少 `80 ms` PCM witness 下运行。
- KWS 不消费 partial；VAD 结束后的完整结果必须精确等于纯控制词、不含 `[unk]`，
  平均置信度不低于 `0.65`，随后仍经过 `UtteranceRouter`、
  `TargetSpeakerFocus`、playback epoch 与 generation fence。
- 已固定真机失败回归：FunASR final 错成“他。”但 KWS 命中“停一下”时，旧播放只停止
  一次，错误 final 不进入 chat/history。
- Gateway 增加精确 session 的 AEC 前后 WAV 采样，默认 `5 秒`、最大 `15 秒`，
  默认关闭，目录/文件权限为 `0700/0600`。
- 架构与许可决策见
  `docs/adr/0023-keyword-control-evidence-and-bounded-aec-capture.md`；
  模型研究见 `docs/research/2026-07-27-vosk-chinese-kws.md`。

## 模型与工件

- Vosk 模型：`vosk-model-small-cn-0.22`，宿主机路径
  `/var/lib/memoria-agent/models/vosk-model-small-cn-0.22`。
- 模型归档 SHA-256：
  `3af8b0e7e0f835ae9d414ce5df580237a3cfb08d586c9fbbb0f7ff29ad5b14ba`。
- 模型未进入仓库、Docker 镜像或 release 三件套；生产目录/文件为
  `65532:65532 / 0750 / 0640`。

| 工件 | SHA-256 |
|---|---|
| source | `f219f942838f7e963551aa548e71fc2fd2aef064e76e5e6e2d1efda7298625b8` |
| images | `d32542e51cbed9ac00c398bb15bf5d2b03a82903806b658558a97c8e04004753` |
| H5 candidate | `70c4571555d0b1f4174d8be2c421bb00d4f8d4e5e76eca92317fe24e18f4c82d` |
| manifest | `8a38875e79b3b813cc1b132d76eca4ee2b63499c8216225e3794322d2d84042e` |
| verifier | `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101` |

服务器导入后的镜像 ID：

| 角色 | 镜像 ID |
|---|---|
| Agent | `sha256:623c089d2b8cdee05eeafbc406c98608a8f4e965367d318b32dea69087c16185` |
| Control API | `sha256:e22aff2eae401e1e118e4df475d9882ac0207c0fe1f6ac929b0cf1edcffc01d3` |
| Speaker Model | `sha256:41829921aa317943d290ffd21db1439ced8122cc237a97b48b753ff88739aef1` |
| Mini Program Gateway | `sha256:2288e1e00afe306ff86bae5fbf884adf9d2b4ec71d9c21df35b5b772c47365d2` |

Docker Desktop 与 Linux Docker Engine 的顶层 image ID 不同；本次以镜像归档 SHA、
manifest 与 release verifier 的 imported-image labels 校验作为一致性证据。

## 发布前门禁

- Ruff：通过。
- strict mypy：160 个 source files 通过。
- Python：`1295 passed, 27 skipped`。
- H5：`232/232`，production build 通过。
- 微信小程序：`46/46`，全部 JavaScript syntax check 通过。
- `scripts/run_e2e.py --profile offline`：通过。
- 关键覆盖率：Agent orchestration `92%`，provider protocols `92%`。
- 全 `services` 覆盖率仍为 `81.54% < 85%`，属于既有门槛缺口；未降低门槛。
- Linux/amd64 成品 Agent 镜像实际加载模型：
  “停一下”命中，“等一下我想问个问题”拒绝。

## 候选与生产验收

- 服务器重新核验 verifier、manifest、三份 sidecar 与模型 SHA；release verifier 在
  `docker load` 前后均通过。
- 四份候选 env 为 `root:root 0600`；KWS 开启，模型路径和置信度精确匹配；
  AEC capture session ID 为空，诊断保持关闭。
- 候选 Agent、Control API、Gateway 与 Speaker Model env 均通过镜像内验证；
  候选 Agent 使用正式模型成功构造 `VoskKeywordSpotter`。
- 隔离 server smoke 通过候选 H5、SPA、API、owner-only 默认值与 SQLite restart。
- `agent / control-api / speaker-model / miniprogram-gateway` 四容器均为
  `healthy`、restart 0。
- LiveKit、FunASR interim/final/字时间戳、Qwen streaming、Doubao
  `24 kHz / mono / s16le`、字时间戳、CancelSession/连接驱逐，以及
  InterruptSemantic 五类样本全部通过。
- readiness 为 `ready / 20260727-204609`，9/9 core checks 与 Agent heartbeat
  精确绑定本 release。
- 生产 Agent one-off 使用 `/etc/memoria-agent.env` 与宿主机只读模型成功构造
  `VoskKeywordSpotter`；四容器最近十分钟关键错误匹配均为 0。
- Nginx 配置通过；WMS、readiness timer 与 certbot timer 均为 active/enabled。
- 公网根 H5、兼容 H5、SPA、API live/ready 与 WMS 为 200；
  internal、PocketSparks 与 Goods Invoice 原路径为 404。
- 公网 H5 `index.html` SHA-256 为
  `182d967a564c80f22a73b8b4263fd92a610472deec0b2ce7c9c2297fb6fe6bb5`，
  与服务器 `20260723-192611` 完全一致。
- 小程序 WSS 成功升级；缺少 ticket 的 hello 按协议关闭为 `4401`。
- IP 证书 SAN 包含 `122.51.108.140`，有效至 `2026-08-03 01:40:00 UTC`；
  域名证书 SAN 包含 `aigcnice.com` 与 `www.aigcnice.com`，有效至
  `2026-10-18 03:59:59 UTC`。

## 备份与回滚

- 直接回滚 runtime：`20260727-170628`。
- 固定 H5：`20260723-192611`。
- 回滚证据：
  `/var/backups/memoria/runtime-switch-20260727-204609-from-20260727-170628-20260727-210538/`。
- SQLite backup API 创建双副本，完整性为 `ok`、外键违规为 0，SHA-256：
  `3ffd8c2e199ee2b829f72b6b2d2165e8e6e5e9f6e291bee3dad06802fd7db72b`。
- PostgreSQL custom dump 已由同版本 `pg_restore --list` 验证，SHA-256：
  `17e46eae072bb185844549d774e37b722726878e58a7bdd3456bdcf3471ff5e3`。
- 四份 env 备份 SHA-256：

| env | SHA-256 |
|---|---|
| Control API | `3e4667fb2f44b8ec54c9c4586adade0c0d3aee7583b722d4e7d0dd6a6dc1a5a4` |
| Agent | `2acccb6ebdf1ebd7dc06a1dcfbf02657cb7a501991fd614109c4ae03d2a16bfb` |
| Speaker Model | `6377ed699f2af0e407bd69550522da5b7d39b067079f232c821f29d49d0bd9fa` |
| Mini Program Gateway | `9058fe75e5cf9fcee24a95e0024093c734a2db6c0b473c0e3a3db1c6935e8988` |

- runtime 回滚先恢复四份 env，再原子切回 `20260727-170628` 并重跑 readiness；
  不自动恢复 SQLite、PostgreSQL 或 MinIO。

## 选择性清理

- 生产删除 `2.2 GiB` incoming、未激活的 `204609` H5 候选、临时候选 env、
  旧 runtime `20260727-120448` 与其四个 Docker tag，并移除退出的 MinIO provision
  容器。
- 生产仅保留 runtime/source/image `20260727-204609` 与直接回滚
  `20260727-170628`；H5 仅保留 `20260723-192611`。
- 服务器根分区由约 `34 GiB` 已用降至约 `31 GiB` 已用，可用约 `83 GiB`。
- 本机删除临时 release worktree、约 `2.1 GiB` 工件目录、`120448` 四个旧 tag 与
  `memoria-agent:kws-vosk-smoke`；Data 卷约 `294 GiB` 可用。
- 未运行全局 Docker prune；未删除数据库、卷、模型、备份、其他项目镜像、
  跨项目构建缓存或用户未跟踪素材。

## 已知边界与下一步

- 生产模型加载、合成录音和 WSS 协议验收不能替代真实手机外放声学闭环；
  当前仍不能宣称 iOS/Android、听筒、蓝牙、弱网下高质量全双工已完成。
- 下一次真机测试前先取得目标 session ID，只为该 session 设置
  `MINIPROGRAM_GATEWAY_AEC_CAPTURE_SESSION_ID`；测试结束后立即导出 pre/post WAV、
  恢复空值并重建 Gateway。
- 真机重点覆盖“等一下”“停一下”、控制词后跟内容、助手原声/噪声误触发、
  访客声音、迟到结果、下一段播放和音频路由切换。
