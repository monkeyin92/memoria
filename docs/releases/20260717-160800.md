# Release 20260717-160800

## 目标

在级联已稳定的基础上继续优化响应与登记成功率，并强化 P1 duck-first 恢复。

## 变更

- 声纹登记：`SPEAKER_ENROLL_SPEECH_MS=2500`（原 3500）；超时 12s；有语音时更快 poll；用户说完接近阈值时 early finalize。
- 首句 TTS：首段 wait 280→180ms，hard 450→320ms。
- 端点：min_delay 1.50→**1.30s**，max 2.20→2.00s；打断 min_duration 0.40s；假打断恢复 1.50s。
- P1：duck 后用户停说即 restore gain（不再只靠 timeout）。

## 生产

- readiness：`20260717-160800 / qwen`。
- env：`SPEAKER_ENROLL_*`、`ENDPOINTING_*`、`FALSE_INTERRUPTION_TIMEOUT_S`、`INTERRUPTION_MIN_DURATION_S` 已更新。

## 回滚

- 上一版：`20260717-155200`。
