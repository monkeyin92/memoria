# Memoria Runtime Release 20260817-171013

## 发布目标

- 为小程序提供受主体能力门禁保护的对话回顾窄投影。
- 将 Actual Heard、记忆候选和已确认记忆分成互斥分区，避免把生成但未播放的内容或未确认候选直接当作主人记忆。
- 收口候选记忆确认路径，并补齐跨账号及 adult/minor/unknown/missing 主体矩阵。

## 发布范围

- Control API：新增 GET /v1/archive/conversation-review，并为记忆确认写入复用 conversation_review capability 门禁。
- 微信小程序：新增 Memory Review 页面投影、确认后权威重拉、auth epoch/游客清理保护及对应测试。
- Agent：仅补充 PR-16 Tool cancel / heard text cutoff 回归测试；不需要切换 Agent/Bridge 运行代际。
- H5、ESP32 固件、Media Edge、Voice Core Bridge 和设备身份区不切换、不刷写、不触发设备语音。

## 发布前门禁

- Control API archive 全文件测试通过；相关主体/能力矩阵测试通过。
- Agent Tool cancel、provider timed-text cutoff 回归通过。
- 小程序全量测试 150/150 通过。
- Ruff、strict MyPy（399 个源码文件）、模块预算和 git diff --check 通过。

## 验收边界

本 release 的 Control API 已达到 `code / wired / enabled / production runtime verified`；小程序页面仍只达到 `code / wired / local verified`，没有发布体验版或正式版。这里的 production runtime verified 只证明候选容器、readiness、公开路由和 fail-closed 边界，不证明有认证用户已经在小程序完成对话回顾业务操作。

不得把本次控制面部署写成精确 DAC、真实设备 Actual Heard、AEC、Double-talk、T1–T14 或 `full_duplex_verified` 通过。

## 提交与制品

- 源码提交：`24784a90f3e6d3cfb6638d13b1d432edbc4f8d17`，已推送 `origin/main`。
- annotated tag：`20260817-171013`，已推送且不再移动。
- 服务器 incoming：`/opt/memoria/incoming/20260817-171013`；release manifest SHA-256 为 `a24619062746060977a60afac96c7180ca10f8c7a828194fd024770ef5f36eb2`，verifier SHA-256 为 `5468ae7d71f431598826fc8681905a42a2ec161abc49756768f74d8f37c2d085`。
- Control API 镜像：`memoria-control-api:20260817-171013`，image ID `sha256:02d4d86a349c2dd255f44fe0229c93e5d1a82337bde6cb81a418e22f0bec40e2`；OCI revision/version/role/arch 为 `24784a90f3e6d3cfb6638d13b1d432edbc4f8d17 / 20260817-171013 / control-api / amd64`。

## 切换与回滚

- 为保持现有 Agent/Control 分层 canary fence，候选镜像继续运行 `MEMORIA_RELEASE_TAG=20260814-231749-direct-canary`；镜像版本与运行代际没有混用。
- 第一次切换于 `2026-08-17T09:42:39Z` 开始。候选容器已 healthy，但脚本只执行一次 readiness，收到 503 后于 `09:43:14Z` 自动回滚。旧镜像 `memoria-control-api:20260814-231749-direct-canary` / `sha256:2558c09f8d42b8a64e0bbdd8aab42328d90155e5cff20d8abfef9eda8647c976` 恢复 healthy、restart count 0、readiness 200。该次没有保留 503 body，因此不把它单独归因为代码故障或 Agent 心跳。
- 切换脚本随后改为每 2 秒重试、最多 30 次，并只记录脱敏 readiness 摘要。第二次切换于 `2026-08-17T09:51:03Z–09:51:28Z` 成功：前两次 503 均为 `smokes=passed`、12 项 core ready、`agent.status=missing`；第 3 次收到 release tag 匹配的 Agent heartbeat 后返回 200/ready。该证据确认新进程的短暂 503 是等待下一次心跳的启动窗口。
- 第一次自动回滚证据：`/opt/memoria/direct-canaries/20260817-171013/CUTOVER_ATTEMPT1.txt`，SHA-256 `0b98695f2197411ac684159eaa4a778a4de06f2a8dd84e04c451ba9eaacabcfc`。
- 最终切换证据：`/opt/memoria/direct-canaries/20260817-171013/CUTOVER_RESULT.txt`，SHA-256 `a64b8d8897db818d26abe07344fe3b2bba8a6ab679e13a4ec02d7f88bdca1399`。

## 生产验收

- 实际 Control API 容器为 `1efe4040348cdf22d5e980c1daa15bbe2d8ad901236d3320ee4a2d85ed0e7eaf`，Docker health healthy、restart count 0。
- `/data -> /var/lib/memoria` 为读写挂载；`/etc/memoria-media-runtime` 为只读挂载；服务只发布到 `127.0.0.1:8791`。
- 本机与公网 8443 的 live/ready 均成功；readiness 和 Agent heartbeat 的 release tag 均为 `20260814-231749-direct-canary`，12 项 core check 全部 ready。
- 未认证 `GET /memoria-api/v1/archive/conversation-review` 与 `POST /memoria-api/v1/archive/memories/{claim_id}/review` 均返回 401，证明候选读写路由已进入公网并 fail closed；`/memoria-api/internal/` 继续返回 404。
- 公网根 H5、兼容 H5、SPA 与 WMS 均返回 200。生产 runtime/H5 软链继续指向 `/opt/memoria/releases/20260812-173008` 与 `/var/www/memoria-releases/20260808-171749`。
- Agent、Voice Core Bridge、Media Edge、Speaker Model、两个 Gateway、PostgreSQL、两个 Redis、MinIO 与 LiveKit 共 11 个旁路容器 ID 未变化且状态正常。`2026-08-17T10:03:45Z` 延迟复核时 Control API 仍为 healthy、restart count 0，候选 readiness 成功后的日志窗口 `Traceback / ERROR / 5xx = 0 / 0 / 0`。
- 本轮没有运行数据库迁移，没有重建数据服务，没有切换 H5，没有发布小程序，没有刷写 ESP32，也没有触发设备或电脑播放。

## 回滚

- 部署前已冻结当前 Control API 容器、镜像、runtime fence、旁路容器 ID 和 root-only 回滚脚本。
- 若 Control API readiness、容器健康、挂载、端口、OCI metadata、公开路由或旁路容器门禁失败，`/opt/memoria/direct-canaries/20260817-171013/rollback-control-api.sh` 会只恢复旧 Control API 镜像；第一次切换已实际验证该路径。
- 本 release 不执行破坏性数据库迁移，不自动回滚 PostgreSQL、SQLite 或 MinIO 数据。

## 状态

- 提交与 tag 推送：完成。
- Control API 生产切流：完成，`code / wired / enabled / production runtime verified`。
- 小程序：`code / wired / local verified`，未发布体验版或正式版。
- `direct_real_device_verified=false`；`full_duplex_verified=false`；T1–T14 = `0 pass / 14 blocked / 0 failed`。
- 根盘在验收时约 15 GiB 可用、88% 使用；保留当前候选、即时可运行回滚镜像和两次切换证据，不执行 broad Docker prune。
