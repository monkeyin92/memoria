# 小程序语义打断候选 Release 20260727-120448

## 发布结果

- source commit / annotated tag：
  `db07f8a11fb08ee9282a4530ebf2394d50280104 / 20260727-120448`。
- runtime 于 2026-07-27 12:23:59 CST 原子激活；H5 继续固定
  `20260723-192611`，没有切换 H5 公网软链。
- 四个 runtime 容器均为 `healthy`、restart 0；readiness 精确返回
  `ready / 20260727-120448`，9/9 core checks、Agent、LiveKit、FunASR、Qwen 与
  Doubao 正常。
- 真实 Provider gate 通过：
  `provider_smoke_test PASS: FunASR, Qwen, Doubao, InterruptSemantic`；
  `qwen-flash` 的真实污染、控制+内容、引用追问、明确问题与控制+回声五类样本全部匹配。
- 服务器从本地已校验 source/images/H5 manifest 二次校验并导入，隔离
  `smoke_server_deployment.sh` 通过；公网根路径、H5、API live/ready 与 WMS 均为 200。
- root-only 回滚证据：
  `/var/backups/memoria/runtime-switch-20260727-120448-from-20260726-233337-20260727-122225/`。
  其中 SQLite 双副本哈希一致、PostgreSQL custom dump 可被 `pg_restore --list` 读取，
  四份旧 env、前后状态与脱敏日志均为 `0600`。
- 微信体验版 `0.8.50` 已于 2026-07-27 15:16:19 CST 通过 `wechatide upload`
  上传成功，包体 `603,490` 字节；未提交审核或正式发布。
- 为启用专用上传链路，本机微信开发者工具已更新为 Nightly `2.02.2607252`，
  agent 侧 `wechatide-skill` 已从内置版本单向同步至 `0.3.4`。

## 发布目标

- 将可信小程序播放期的首个 barge-in 下行立即静音，避免用户已经开口后仍听到
  Assistant 尾音。
- 只在当前 speech epoch 已形成 sticky `interrupt_then_chat` 的歧义 final 上，以
  `qwen-flash` 产出严格三态语义证据；`UtteranceRouter` 继续是 stop、ack、clear 与
  chat 的唯一副作用入口。
- 上传微信小程序体验版 `0.8.50`，不提交审核或正式发布。客户端源码未变；新体验版用于
  明确关联本次 runtime 候选，真机验收通过后再决定后续动作。

## 候选行为

- 可信小程序 AEC 会话的首个 VAD/barge-in 事件发布
  `assistant_audio(action=duck, gain=0.0)`；H5 与不可信链路保持原来的 `gain=0.25`。
- 高置信纯控制、resume、登记和精确重放仍优先走确定性 Router，不调用模型。
- 仅歧义 sticky `interrupt_then_chat` final 调用模型，输入严格限制为 final、首个 sticky
  文本与打断时冻结的 Assistant 文本；不传会话历史、记忆、工具、权限或 Persona。
- `CONTROL_ONLY` 只确认一次“嗯，你说。”并保留原回答供“继续”；`HAS_USER_CONTENT`
  保持真实问题；`UNSURE`、超时、异常、非法返回与迟到结果均 fail closed 并请用户重说。

## 发布前门禁

- Python：`1269 passed, 27 skipped`。
- Ruff、`mypy services --strict`、`git diff --check` 通过。
- H5：`232/232`；小程序：`32/32`。
- 本机未配置 `DASHSCOPE_API_KEY`；真实 `qwen-flash` 五样本 smoke 必须在候选生产
  Agent 容器执行，且 Provider 成功行必须精确包含：
  `provider_smoke_test PASS: FunASR, Qwen, Doubao, InterruptSemantic`。

## 发布顺序与回滚

- 只切四个 runtime 容器，H5 固定为 `20260723-192611`。
- 切换前备份当前 `20260726-233337` runtime、SQLite/PostgreSQL 与四份 root-only service env；
  验证失败自动恢复当前 runtime 与对应 env。
- 真机验收重点：AI 播放约 0.6 秒后依次说“等一下”、“停一下，你叫什么名字？”以及
  引用 Assistant 原话的追问；检查 0–700 ms 静音、确认仅一次、真实问题不丢失且“继续”
  恢复原回答。
