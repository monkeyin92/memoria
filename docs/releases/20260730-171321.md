# 语音打断与主人声纹 Release 20260730-171321

## 目标

- 修复 H5 播放期短纯控制词需要重复多次才停止，以及旧 speech epoch 迟到终稿串入下一话轮的问题。
- 将自建 endpoint 窗口恢复为 `1.50 / 2.20` 秒，降低自然句中停顿被提前提交的概率。
- 在 H5 与小程序“我的”新增可重复的主人声纹录取，用自然、轻声、带笑和稳重四种状态建立独立原型。

## 发布前门禁

- Python 全量测试通过（`1499 passed, 29 skipped`）；Ruff 与 strict mypy `177 source files` 通过。
- H5 `244/244`、production build 通过；小程序 `82/82` 通过。
- 发布上传与生产 compose/env 定向门禁 `45/45` 通过；`git diff --check` 通过。
- H5 已在隔离本地 API 下完成 `375×812` 与 `812×375` 的主人声纹流程检查，无横向溢出，
  可见录音按钮不小于 `44×44`，console warning/error 为空。

## 生产范围

- runtime 四镜像、H5 静态 release 与小程序开发测试版使用同一份源码语义。
- 声纹多原型由 Control API 权威保存与匹配，因此不能只发布客户端；runtime 必须同时切换。
- 小程序只上传开发测试版 `0.8.63`，不提审、不正式发布。

## 安全与回滚

- 声纹登记仍只创建 `shadow` 档案，不绕过 anti-spoof、200 条评估与 FAR/FRR/EER 门禁；旧单中心模板继续兼容，无数据库迁移。
- runtime 直接回滚点为 `20260730-092236`，H5 直接回滚点为 `20260730-153343`。切换前创建
  SQLite、PostgreSQL 与四份 env 的 root-only 备份；runtime/H5 分别通过原子软链切换。
- runtime 回滚不自动恢复数据库或对象存储；只有确认数据格式不兼容时才另存当前状态后恢复快照。

## 发布结果

- source commit / annotated tag：
  `79f0cb89e1444e9fbdbb014bd66e1d5d58f5626c / 20260730-171321`；源码提交和 tag 已推送
  `origin/main`。四个服务器镜像均为 `linux/amd64`，revision/version/role 精确绑定本 commit/tag。
- 固定工件 SHA-256：source
  `8fc1d3be206a711e52fd899ad0dae26b8bf2d1ff1cc2edd2c658eefaf261807e`；images
  `208de12f02e52cf0e762c5dedb42d378551ab7b3d89d8440693aa7bf3207c658`；H5
  `0163200c389e482bd38a74b38b289694e7c46d943abc8227e669ed2087d4aed9`；manifest
  `b04db5a10f222de5b98776b1795cfc7ed57e65172f488aeeff295149272309d2`；verifier
  `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101`。增量上传实际发送约
  `30.09 MB`，复用约 `2.39 GB` basis，服务器复验通过。
- runtime 于 `2026-07-30T09:23:46Z` 完成切换，H5 于 `2026-07-30T09:25:17Z` 原子切换。
  四容器均 healthy、restart=0、严重错误日志为 0；真实 LiveKit、Qwen、FunASR、豆包五音色和
  Interrupt Semantic 门禁通过，readiness 绑定本 tag，9/9 core ready。
- 公网 8443 根 H5、兼容 H5、SPA、API live/ready 与 WMS 为 200，internal、PocketSparks、
  Goods Invoice 为 404；WSS 无效 header ticket 按协议关闭为 `4401`。Nginx 和域名 TLS SAN
  通过，WMS 保持 `enabled / inactive`。候选合并历史 immutable 资源后完成 1,038 次公网检查。
- 线上 index/主 JS/主 CSS SHA-256 分别为
  `1dd5dbb951262009aa764fc5ce8ea6f755f6d5151bb613813517821f731ed190`、
  `d26757b2bd746798212bc13e0f4b317fcc661b43803ce3ddc139dcfe7ed77c92`、
  `8ffa8c83f223041c921779f4fe9a480c11f6d0bf4055a5a3deb3d97b393d02ee`，与本机工件一致。
- root-only 回滚目录：
  `/var/backups/memoria/runtime-switch-20260730-171321-from-20260730-092236-20260730T092241Z/`；
  SQLite / PostgreSQL 备份 SHA-256 分别为
  `70eae33c0ff8d8768a14c110f2b013316e1bde9b20a5b77e7d0c88b5407aa3f6` /
  `0a7f275600eaa71f3c109b22206367ec65a38b8610b8c8ffa54e917b92086239`，四份 env 与数据备份均为
  `root:root 0600`。
- 小程序 `0.8.63` 首先按项目 `upload:test` 预检；Node 24 完整编译 32 个代码文件，但
  `miniprogram-ci` 实际上传因当前公网 IP 不在微信 CI 白名单而被拒。经用户确认后由已登录的
  微信开发者工具上传成功，任务
  `confirmation_upload_082e65b6-5a92-4560-a3a5-143ee334f477` 返回
  `success / execution_success`，包体 `653,554` 字节；本次只生成体验版，未提审、未正式发布。

## 未验证边界

- 自动化和桌面浏览器不能证明真实手机上的四种音色、噪声、FAR/FRR、尾句完整性或打断听感；
  体验版上传后仍需按 iPhone/Android 设备矩阵实测。
