# DeepSeek InterruptSemantic 延迟门限修复 Release 20260802-142257

## 根因与修复

- 百炼 DeepSeek-v4-flash 主链、Doubao、FunASR 均通过真实 smoke；首次
  `InterruptSemantic` 使用历史 Qwen 的 `0.6s` 门限时超时。
- 服务器实测分类请求约 `0.77–0.88s`；统一将默认与生产候选门限调整为 `1.2s`，
  仍受配置上限 `2.0s` 约束，超时/非法输出继续 fail-closed 为 `UNSURE`。

## 发布范围

- 仅 runtime 配置与 Agent 镜像；H5、小程序、数据库数据不变。
- 上一候选 `20260802-135917` 因 readiness 未通过已回滚至
  `20260731-114616`，本 release 重新走完整 artifact、备份、隔离 smoke 与 readiness。

## 发布前门禁

- 相关单测、Ruff、strict mypy、compileall、`git diff --check` 通过。
- 生产重新执行真实 DeepSeek、InterruptSemantic、FunASR、Doubao 与 LiveKit smoke；
  任一失败不得写入 readiness。

## 工件、部署与验收

- source commit / annotated tag：`43bd6152df7cf3ccb8585f4780ac3a7cea8dc9a8 /
  20260802-142257`。source、images、H5、manifest、verifier SHA-256 分别为
  `0be304e90ca38171d4996f6b76f740d3ea3f88b32139694ff3fc14b7576df581`、
  `72bd48d478cc62750c6b7f2590b198be64b3930d19801e8dead96a5368421a93`、
  `434798a9735b53afdd1f25ee1616eeec2f7e3bba70c3a2e886ca28de59853a74`、
  `b014f7c94a4116e095e8d789017070d178d79bd4053600b9f8f5f5167dc2d5e0`、
  `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101`。
- runtime 于 `2026-08-02T14:30Z` 原子切换，H5 随后切换；四个 runtime 容器
  `healthy / restart=0`，无近 10 分钟 Traceback、ModuleNotFoundError 或 ImportError。
- 真实 smoke：LiveKit PASS；Doubao 五音色、PCM/字幕/取消 PASS；FunASR 六段 PASS；
  DeepSeek streaming PASS；InterruptSemantic `deepseek-v4-flash` 五类 PASS；
  readiness 绑定 `20260802-142257 (bailian_deepseek)`，9/9 core ready。
- 公网 H5、SPA fallback、live/ready 均 HTTP 200，internal readiness 路径为 404，
  Nginx `-t` 通过。未上传、提审或正式发布小程序。

## 备份与回滚

- 直接 runtime 回滚点为 `20260731-114616`，H5 回滚点为 `20260730-233824`。
  root-only 备份目录为
  `/var/backups/memoria/runtime-switch-20260802-142257-from-20260731-114616-20260802T143013Z/`。
- SQLite 双副本 SHA-256 为
  `deaf81a0250886ac40dcff48a02437dbed50aef5bc0d455f0f1f1bb56847663f`；
  PostgreSQL 17 custom dump SHA-256 为
  `f48d70882ad37ca644a91ff39d200d0c984852ae69860aebe19de43903409a51`，
  `pg_restore --list` 为 420 条目。四份 env 均以 `root:root 0600` 保留。
- PostgreSQL WAL archive 的既有同机权限问题仍未扩展处理；普通 runtime 回滚不恢复数据库或
  MinIO。真实设备双讲、真人声音/Persona 盲测与 200 条录音矩阵仍待单独验收。
