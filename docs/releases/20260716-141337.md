# Release 20260716-141337

## 范围

- 仅发布 Agent/Control API runtime；H5 继续使用 `20260716-085218`，LiveKit、Nginx 和公网端口配置未改。
- 修复 AI 播报期间被单字、短噪声或迟到 ASR 结果误打断，继而自行提交新 turn、重复回答和 pause/resume 卡顿的问题。
- 自建档案使用 LiveKit 原生 `min_words=2`，并把 FunASR/endpoint/interruption 参数调整为 `400ms / 550ms / 450ms`。

## 工件与备份

| 工件 | SHA-256 / Image ID |
| --- | --- |
| `memoria-agent:20260716-141337` | `sha256:f95d554b46cfc9ec7053cd456f43f24bca7e7fbd95c7a036292bde2c5f21b970` |
| `memoria-control-api:20260716-141337` | `sha256:109de0ab8ff55874b6d9f19545a4ada86306bc55144ad04ea08ffc5b40fb8524` |
| `services/agent/src/agent.py` | `ac345da4ce6337edae058d176366eb682c2b7b35f1b3625d622a994a0deb4f93` |
| `services/agent/src/providers/funasr_stt.py` | `dada3d8b93fbb5e1e7ce77389e24cf620be4795bf1ccdbb46bd47aeea88df3e5` |
| SQLite 发布前快照（两份相同） | `8673558baa2ff3b647cce27d7d3b8879402dfa3893c2f87a1fc30a6534c2d61f` |
| `/etc/memoria.env` 发布前保护副本 | `d6b293ffc39a67547b8c9aadb0ab988cc82518692924f30bfccd69a855544b45` |

SQLite 两份快照均为 `root:root 0600`，`integrity_check=ok`，foreign-key violations 为 0；环境保护副本同为 `root:root 0600`。

## 生产参数

```dotenv
FUNASR_MAX_SENTENCE_SILENCE_MS=400
ENDPOINTING_MIN_DELAY_S=0.55
ENDPOINTING_MAX_DELAY_S=2.20
ENDPOINTING_ALPHA=0.85
INTERRUPTION_MIN_DURATION_S=0.45
FALSE_INTERRUPTION_TIMEOUT_S=0.80
```

自建档案的 runtime 配置另固定为 `interruption.mode=vad`、`interruption.min_words=2`。`livekit_cloud` 档案在未显式覆盖环境变量时保留原默认值，避免旁路回归。

## 验证

- 修复前最小回归命令稳定得到 2 个失败：endpoint `0.30 != 0.55`、FunASR silence `650 != 400`；修复后同命令 2/2 通过。
- 全量 Python：206/206；Agent：176/176；Ruff：PASS；mypy strict：PASS。
- 镜像架构：两个 runtime 镜像均为 `amd64`；容器均为 `healthy`。
- readiness：LiveKit RoomService、FunASR、Qwen（thinking disabled）、CosyVoice 全部 PASS；公网 ready 绑定 release `20260716-141337`、provider `qwen`。
- 公网：根 H5、SPA fallback、live、ready 均为 200；internal、PocketSparks、Goods Invoice 原路径均为 404；内置浏览器首页可渲染且 console warning/error 为 0。
- 公网真实语音房间 `voice-0be606b6-321c-4032-bcd7-ce18900dd0c6`：客户端通过 `8443/TCP`，Agent 通过容器内部 UDP；欢迎语播放中注入约 0.5 秒单字“嗯”未提交新 turn，随后“你好。”只提交 `turn_id=1 / generation_id=1` 并只生成一次回复，远端音轨已实际接收。
- 上述会话日志：`late_stt=0`、`playback_restart=0`、`provider_failure=0`、`turn_committed=1`。
- `7882/UDP` 仍只有原 LiveKit 本地监听，本次未修改 LiveKit Compose、Nginx、防火墙或云安全组。

## 回滚

- Runtime：`20260716-085218`。
- H5 已保持在 `20260716-085218`，无需回滚静态软链。
- 环境保护副本：`/var/backups/memoria/memoria.env-pre-20260716-141337`。
- SQLite 保护副本：`/var/backups/memoria/memoria-pre-20260716-141337.sqlite3`；本次无数据格式变更，普通 runtime 回滚不应恢复数据库。
