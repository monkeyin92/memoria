# 已废弃候选 20260721-112628

## 状态

- **本候选已废弃，未部署，禁止上传或复用。** 后续审查补入生产 WSS、真实取消
  smoke、clone 激活 409、历史预览 registry、鉴权互斥和 TTS shutdown；发布改用
  全新 tag `20260721-123203`。
- SSH 已改为 `ssh memoria-prod` 专用 Ed25519 密钥免密登录，不再是阻塞。

## 变更

- 级联 TTS 主链改为豆包 `seed-tts-2.0` 双向流式 WebSocket：直接消费 LLM
  增量短语，正常 session 复用连接，取消时发送 `CancelSession` 后关闭并淘汰连接。
- 输出固定为 PCM s16le、24 kHz、mono、字级时间戳；字幕按完整 PCM 对齐后才进入
  LiveKit。`context_texts` 因真实探针出现超过 1.4 秒偏移而暂时禁用。
- 五个机器人按批准 registry 使用豆包原生音色，并提供版本化真实试听：
  - 星澜：阳光青年 2.0；
  - 桃喜：甜美桃子 2.0；
  - 绵绵：温柔小雅 2.0；
  - 阿序：高冷沉稳 2.0；
  - 玄墨：深夜播客 2.0。
- 历史 CosyVoice clone profile 保留查看、评估与撤销，但不再进入播放主链；会话
  按所选机器人回退对应的批准豆包音色。
- readiness、provider smoke、生产 env 拆分、镜像构建和 H5 音色入口同步切换豆包。
  WebSocket 鉴权仅允许 API Key，或 APP ID + Access Token；Secret Key 不使用也不部署。

## 审查修复

- 修复 `scaled` 字幕先以原始时间戳进入 LiveKit 的问题；回归锁定最终字幕末尾与
  实际 PCM 时长一致。
- 首包与总超时从首个非空 `TaskRequest` 成功发送后开始，不再把 LLM 首 token
  等待误算成豆包超时；空输入立即失败。
- 取消 helper 返回前等待连接淘汰与池补位完成，避免下一话轮额外等待 300 ms 再
  临时建连；保留的 CosyVoice 历史 helper 同步修复同型竞态。

## 本地验证

- Python：`594 passed, 21 skipped`；Ruff lint、strict mypy（112 个源文件）、离线
  E2E 与 `git diff --check` 通过。
- 关键覆盖率：编排 92%、provider protocol 91%。仓库总覆盖率为 **80.86%**，未达到
  既有 `fail-under=85`；这是发布前已存在的全仓缺口，本候选不把它伪报为通过。
- `ruff format --check .` 暴露 78 个既有历史文件格式债务；本次改动涉及的 35 个
  Python 文件均通过 format check，没有为本次发布机械改写无关文件。
- H5：12 个测试文件、`125/125` 通过，production build 通过。五个试听文件均为
  PCM s16le、24 kHz、mono、16-bit WAV。
- 真实豆包探针：`seed-tts-2.0`、阳光青年 2.0、PCM/24 kHz/mono 与字级时间戳
  PASS；凭据仅经隐藏 stdin 临时注入，未落盘。Secret Key 未使用。
- 两轴审查后的豆包与历史 CosyVoice integration 共 21 项通过；镜像运行时导入与
  豆包 registry/readiness 模型检查通过。

## 候选工件

- 本地目录：`/tmp/memoria-release.20260721-112628`
- 三张镜像均为 `linux/amd64`：
  - Agent：`sha256:f7ff92a00eee3b773add8bab76cf967f58c6e92197c6ad30674b0f39103894c2`
  - Control API：`sha256:eb29f715abd0c096ffa7ef81ebbfd0dc9ad474889e98bfab6272f8a79cd491a0`
  - Speaker model：`sha256:f7cd145dc3262a19a8fd05e85907cb6046c5d8136f6eb35d0e13507bb3490668`
- SHA-256：
  - `images.tar.zst`：`2bbf317ecca7c39dcd801de8e5db6332420628d081b224aec70b5da31ed70f4e`
  - `source.tar.gz`：`2e0f9ff380b553282e05b826f15dc9a762c98ac76e0acabc233e4231b33e034a`
  - `h5-dist.tar.gz`：`2cd5b4bfee9d2c9d5583d633e510fbb9dd63aee4979bb6a94ab55cef8c1cfd34`
- 源码归档已确认包含豆包适配器、registry、ADR 和五个试听 WAV，且不包含用户的
  `outputs/`；Git diff 与镜像 history 的明文 secret 候选数均为 0。

## 待生产执行

1. 先只读记录 `/opt/memoria/current` 与 `/var/www/memoria-h5` 当前目标。
2. 上传并复核三份候选工件；生成 root-only Agent env，只加入 APP ID + Access
   Token，不写 Secret Key，并保留旧 env 与 SQLite 双份回滚快照。
3. runtime 先切换，强制运行 LiveKit、FunASR、Qwen、Doubao smoke/readiness；失败
   立即回滚 `20260720-225534`。
4. runtime 全绿后最后切 H5，保留历史 immutable assets；公网验证 root、live、ready、
   五个试听 WAV 和 WMS，再做浏览器试听与 console 检查。
