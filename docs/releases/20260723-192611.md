# 硅基生命 S1–S9 Release 20260723-192611

## 状态

- 已发布：runtime 于 2026-07-23 19:47:57 CST 原子切换，H5 于
  2026-07-23 19:51:26 CST 在 Provider/readiness、Nginx 与公网门禁通过后最后切换。
- 当前 runtime/H5：`20260723-192611`；annotated tag 唯一解析到
  `0d13d2b22e1d0160529bfd61d2eb1d586ba18d29`。
- 上一生产 runtime/H5：`20260721-224804`。
- 前一候选 `20260723-190001` 因 Agent 镜像漏打包
  `services.archive` 在 runtime 激活时失败，已自动回滚且从未切换 H5；该 tag 不复用。
- 发布顺序固定为：数据与迁移预演 → 隔离候选 → Agent 实镜像 heartbeat →
  runtime-first → Provider/readiness → Nginx/TLS → H5-last → 公网与浏览器验收 →
  15 分钟观察。

## 发布范围

- S1：Agent 新鲜 heartbeat、短 access/旋转 refresh、旧身份受限迁移、消息幂等、
  对象 keyring、事件合同、CSP 与 commit-bound 发布门禁。
- S2：服务端 `companion / archive / self_preview / legacy` 四模式，以及
  “轻量稳定陪伴者 + 从主人证据空白成长的 Digital Self”。
- S3–S6：不可变 DigitalSelfVersion、成长地图、认知/决策/关系模型和唯一
  DigitalSelfResponsePlanner。
- S7：owner-only Self Preview、来源证明、“不像我”纠正和 holdout Fidelity Evaluation。
- S8：manifest v3 版本绑定个人音色、设计音色安全回退和 generation-bound provenance。
- S9：LegacyGrant、exact frozen core、approved relationship、allowlist、独立关系外壳、
  每轮重验、最小审计和 H5 传承管理面。
- 发布修复：全量与增量 Agent 镜像统一复制完整 `services/`，避免源码测试通过但生产镜像
  缺少跨模块运行时 import；静态发布合同和真实候选容器 import/heartbeat 作为硬门禁。

## 本地门禁

- PostgreSQL 17 + pgvector 全量：1175 passed、3 skipped、0 failed。
- 总覆盖率 88.13%，orchestration 92%，protocol 92%，通过 85% 正式门槛。
- H5：19 files / 232 tests；production build 通过。
- Ruff、148 个 strict mypy source files、Shell/JSON、Compose、`git diff --check` 通过。
- 生产 Compose 发布合同 22 tests 通过；Agent 全量/增量镜像都要求 `COPY services ./services`。
- 离线 ASR→LLM→TTS E2E 通过；过期 generation/tool epoch 音频均为 0。
- In-app browser：
  - 390×844 与 667×375 均可进入“数字心智与声音 → 传承模式”；
  - 显示“基于冻结资料生成的数字分身，不是本人”；
  - 授权人/接收人视角可切换，无水平溢出，console error/warn 为 0；
  - 仅使用隔离本地账号和空授权状态，未采集真实声音。

## 生产只读预检与迁移预演

- runtime/H5 均为 `20260721-224804`；Agent、Control API、Speaker Model、PostgreSQL、
  MinIO 均 healthy；LiveKit 正常运行。
- 根分区约 96 GiB 可用；内存约 4.1 GiB 可用；旧 amd64 三镜像均存在。
- `wms.service` active/enabled；公网根 H5、API live/ready 与 `/wms/` 为 200，
  `/memoria-api/internal/` 为 404。
- 双服务 env、Speaker Model/PostgreSQL/MinIO env 均为 `root:root 0600`。
- `scripts/prepare_production_upgrade_env.py` 已从现有 root-only env 生成新的候选文件，
  补齐独立 message-idempotency、heartbeat、voice-cleanup、interaction-policy、
  response-plan 等能力 token；未输出 token 值。
- 生产 SQLite 一致快照与 PostgreSQL custom dump 已恢复到隔离目标；候选 Control API
  已验证 SQLite 认证/预览/Legacy 会话字段，以及 PostgreSQL 5 张 Legacy FORCE-RLS 表。
  恢复库已清理。恢复时必须保留 table owner；`--no-owner` 会让应用迁移失去权限。
- IP 证书当前有效至 2026-07-26 21:15:49 UTC；certbot timer active/enabled。

## 冻结边界与已知限制

- Legacy 只允许 manifest 中明确 `family/public` 的范围：
  Memory 使用 `sensitive_domain`，Persona 因没有 scope 暂不授权，
  Cognitive/Decision/Relationship 使用 `sharing_scope`；private/unknown 均拒绝。
- 个人声音代码能力将部署，但没有真实本人样本、synth-ready provider 映射、
  本人盲测或真机听感证据时保持 fail closed，不能宣称“已复刻并应用本人声音”。
- 死亡认证、遗嘱执行、多执行人、争议冻结、异地副本、PITR、KMS 与分布式一致性
  不在本次发布范围，保留为正式商用前待办。

## 发布前数据与回滚

- 真实回滚点为 runtime/H5 `20260721-224804`。
- SQLite backup API 双快照均通过 `integrity_check=ok`、`foreign_key_check=0`，
  SHA-256 同为
  `afe8f40eaf9c81fde1bf8d5d91769b8931b1e843e48e78b6ea6563797ad466a2`：
  - `/var/lib/memoria/memoria-pre-20260723-192611.sqlite3`
  - `/var/backups/memoria/memoria-pre-20260723-192611.sqlite3`
- PostgreSQL 17 custom dump 已用同版本容器内 `pg_restore --list` 验证：
  `/var/backups/memoria/memoria-pre-20260723-192611.dump`，
  SHA-256 为
  `c33822e5eba8e0a347ac754cba41cb8bf6014de52fea19296a6b6cf29dae9083`。
- 旧 env 均保存为 `root:root 0600`：
  - Control：
    `/var/backups/memoria/memoria-control-api.env-pre-20260723-192611`，
    SHA-256 `6437cb72bb6c20ac652898f9ff65b61f0153aa9207846c9e14daeb3cf767a25d`
  - Agent：
    `/var/backups/memoria/memoria-agent.env-pre-20260723-192611`，
    SHA-256 `f2745253f34810350ec3c2953e4b36818f1a63db80ddf6042dbc8ac0a391097a`
  - Speaker Model：
    `/var/backups/memoria/memoria-speaker-model.env-pre-20260723-192611`，
    SHA-256 `f866611e02c5d370154f0a10d5a8fed115bc3e0ccbd422f3d6384a728bd11d77`
- Nginx 原配置保存在
  `/var/backups/memoria/nginx-20260723-192611`；WMS、PostgreSQL、MinIO、
  LiveKit、旧 release 和对象数据均未删除，未执行 `docker compose down -v`。

## 固定工件

- source commit/tag：
  `0d13d2b22e1d0160529bfd61d2eb1d586ba18d29` / `20260723-192611`
- source archive SHA-256：
  `a49ea481f29e7272d244f49d96b5f3d90a9e0f4116fba8898c5d86fc3e62126c`
- release verifier SHA-256：
  `bd2170a2e15538e426493ebd552f73d367f7b03e66ddfa49f25eea780d625842`
- release manifest SHA-256：
  `0aa1149b8a0a5187cc14718fecc108581c573637d7b6d06aa9d27aa7e178eb4d`
- images archive SHA-256：
  `a4bdf2891615cd5a3dad3dbfe1df17031b35e6f1703bf1847b6afb67baf0e56c`
- H5 artifact SHA-256：
  `0f5043c0bf690991ab92d3e0ae40e196c818b690e37e6d987357bbade0b3f7be`
- Linux amd64 导入镜像 ID：
  - Agent：
    `sha256:3720ba9426740700e1557cdbf0ca71ccd15d82103033139592acadc3179d88bc`
  - Control API：
    `sha256:eb777c20e9964460549196bba4ea3f4388fbb35c2acab31cf2474ffce55cc449`
  - Speaker Model：
    `sha256:6db48ed5e4569fb43fd641b1fc2882ae50f0ac5d8e1b6f57c68484aa8668a0eb`
- 激活后的 H5：
  - `index.html` SHA-256：
    `182d967a564c80f22a73b8b4263fd92a610472deec0b2ce7c9c2297fb6fe6bb5`
  - `memoria-release.json` SHA-256：
    `0e8e7d4f9669c18a9c1eb04e813638d3a0c5943c79a220beade655ff1c1a0fb0`
  - 新旧真实 immutable assets 并集：183 个

## 生产验收

- 服务器端固定哈希、sidecar、release verifier、source/image/H5 manifest、
  Linux amd64 架构和 OCI commit/tag label 全部通过。
- `smoke_server_deployment.sh` 在隔离 SQLite 与 loopback Nginx 中通过：
  candidate H5、SPA、API、默认主人过滤、偏好持久化与 Control API 重启均正常。
- 新 env 由 `prepare_production_upgrade_env.py` 重新生成，Control/Agent/Speaker Model
  分别 80/96/1 个键；共享 capability 精确对齐、各能力 token 相互独立，
  `voice_cleanup` 保持 Control-only，全部为 `root:root 0600`。
- 隔离真实 Agent 诊断使用唯一
  `duplex-zh-agent-candidate-192611`：
  - 候选 Control API 离线本地存储 readiness 通过；
  - Agent 实际注册 LiveKit；
  - Control 接受同 release heartbeat；
  - 容器内 `services.agent.src.heartbeat --check-health` 通过；
  - 日志无 `Traceback / ModuleNotFoundError / ImportError`。
- runtime-first 激活后，LiveKit、FunASR、Qwen、Doubao PCM16/24 kHz/mono、
  字时间戳和 CancelSession/连接驱逐 smoke 全部通过；`verify_env`、readiness mark/check
  通过。最终 readiness 为 `ready / 20260723-192611`，Agent `ready`，
  LLM `qwen`、TTS `doubao`、9/9 core ready。
- Nginx：
  - `nginx -t` 与 reload 通过；
  - root/H5 均返回 CSP 与 microphone Permissions-Policy；
  - signed provider sample 路由 `access_log off`；
  - 当前 `memoria-https.conf` SHA-256
    `2822dfb922fc525437504d35d24c3424c0ece245ac70346c11d37e5b0c0d6fad`；
  - 当前 `memoria-site-common.conf` SHA-256
    `175e121c552ab17bc757a5590f0a89553fd804085cc5e141fff92af040fe0453`。
- H5-last 激活后，域名入口返回 exact 新 `index.html`，
  `memoria-release.json` 绑定 tag/commit，183 个 immutable assets 逐项 HTTPS 200。
- 公网门禁：
  - IP/域名 root、H5、任意 SPA 路径、live、ready、`/wms/` 均为 200；
  - `/memoria-api/internal/*` 与公开 speaker classify 为 404；
  - response-plan、voice session-resolution、`/auth/me` 缺凭据均为 401；
  - Control heartbeat 缺 capability token 为 401；
  - WMS 保持 active/enabled。
- TLS：
  - IP SAN `122.51.108.140`，有效期至 2026-07-26 21:15:49 UTC；
  - 域名 SAN `aigcnice.com / www.aigcnice.com`，有效期至
    2026-10-18 03:59:59 UTC；
  - `snap.certbot.renew.timer` active/enabled。
- 生产 in-app browser：
  - 创建一次性注册账号，验证“陪伴方式只影响助手工作风格，伙伴输出不会成为数字分身
    证据”的混合方案文案；
  - 进入三段声纹步骤后停止，未申请麦克风权限、未上传声音；
  - 390×844 与 667×375 均无水平溢出，console warning/error 为 0；
  - 测试账号已永久删除，重新登录返回 401，浏览器回到注册页。
- 观察窗从 Agent 2026-07-23 11:48:16 UTC 启动持续至 12:03:49 UTC：
  7 次采样均为三容器 healthy、restart 0，readiness/Agent ready，H5 200，
  WMS active/enabled，错误标记 0。
- 未冒充完成：
  - 没有真实本人声音样本、个人音色 synth-ready 映射、本人盲测或真机听感证据；
  - 没有真实家庭关系与冻结版本夹具做生产 Legacy 正向会话；
  - 200 条授权录音、AEC/弱网/重叠/回放攻击矩阵仍是正式商用前外部验收项。
