# Release 20260717-001826

## 范围

- 级联 FunASR/LiveKit endpoint 从 0.90s 调整为 1.50s，假打断恢复同步为 1.70s，吸收生产观测到的约 0.5s 迟到 FINAL，避免一次物理说话触发连续两次助手回答。
- 每次级联 turn commit 记录 `started_speaking_at`、`stopped_speaking_at`、`transcription_delay` 与 `end_of_turn_delay`，为后续按 P95/P99 下调等待值提供证据。
- Omni 在首次 `session.updated` 后只主动创建一次欢迎回答；用户开始说话时取消 pending/active response，用户说话期间迟到的 `response.created` 也立即取消。
- Omni 按远端 track/stream ID 去重；H5 始终复用一个远端 `<audio>` 并在断开时清理。
- 级联与 Omni 每 5 秒采集一次 inbound RTP 音质数值。Omni 遥测入口校验 Bearer、会话所有权与后端类型，只接受允许的事件名和数值字段，不接受文本、音频、SDP、任意 detail 或凭据。
- 不新增模型或依赖；不修改 LiveKit Server、Nginx、证书、防火墙与 UDP 策略。

## 本地门禁

- Ruff：PASS。
- mypy strict：52 个 source files，PASS。
- Python：240 passed。
- H5：57 passed，production build PASS。
- Web：28 passed，lint 与 production build PASS。

## 生产发布

- 发布前 runtime：`20260716-225754`；H5：`20260716-225754`。
- `2026-07-16T16:24:16Z` 激活 runtime；Provider/readiness 全部通过。`2026-07-16T16:28:22Z` 激活 H5。
- 浏览器验证 Omni 主动欢迎、单一远端音频元素、实际播放与 0 console error 均通过；遥测请求均为 204。
- 发布验收发现模块 INFO 被生产 Uvicorn 根日志级别过滤，因此本 tag 作为 H5 正式版本保留，runtime 由 `20260717-003211` 补齐可查询事件日志。runtime 直接回滚点为本 tag；H5 直接回滚点为 `20260716-225754`。

## 已知边界

- 1.50s 是基于本次约 0.5s 迟到 FINAL 的稳定性止血，会增加约 0.6s 端点等待；待采集真实 timing 分布后逐步下调。
- 更低延迟且严格的一次物理讲话一次提交，需要在 Provider 到 LiveKit EOU 前引入 speech epoch；不能用文本相似或业务 hook 后置去重替代。
- 卡顿或“滋滋声”的最终归因仍需用户真机产生的 jitter、丢包、concealment 和 jitter-buffer 数据。本次不凭猜测开放 7882/UDP。
