# 小程序媒体契约与中断恢复 Release 20260727-170628

## 发布状态

- 候选 tag：`20260727-170628`；source commit 在本轮代码 checkpoint 后固定。
- 本文创建时尚未切换生产 runtime，也尚未上传小程序 `0.8.51` 体验版。
- 本轮只切换四个 runtime 容器；生产 H5 继续固定 `20260723-192611`。

## 发布目标

- 统一 Agent、H5、legacy Web 的 `transcript_delta.session_id` 契约，拒绝空会话标识。
- 为小程序 PCM 链路增加跨语言 golden contract、固定 `20 ms / 960 bytes` 下行校验与
  CI 门禁。
- 修复 Socket 断开后仍录音、mic/onStart 竞态、同步发送失败错误推进 sequence、播放器
  首包 lead 和 gain 硬切问题。
- 将 `audio_reset` 提升为可靠 barrier；播放中断只抑制匹配 generation 的 AEC reference。
- 系统录音中断结束后通过 `uplink_discontinuity` 重置半帧、sequence 与 AEC 时序，并在
  同一 WSS 会话恢复录音。
- 上传微信小程序 `0.8.51` 体验版；不提交审核或正式发布。

## 发布前门禁

- Python：`1282 passed, 27 skipped`。
- Ruff、`mypy services --strict`、`git diff --check`：通过。
- H5：`232/232`，production build 通过。
- legacy Web：`28/28`，lint/build 通过。
- 小程序：`46/46`，全部 JavaScript 文件 syntax check 通过。
- `scripts/run_e2e.py --profile offline`：通过。
- 本机 Provider smoke 因缺少 `DASHSCOPE_API_KEY` 与 `DOUBAO_TTS_AUTH` 而跳过；候选必须
  在生产环境真实执行，`SKIP` 不算通过。

## 顺序、验收与回滚

- 构建基线和切换前直接回滚点为实际在线 runtime `20260727-120448`；切换时再次读取并
  固化真实软链，不依赖本文硬编码。
- 从干净 annotated tag 构建并校验 source、四个 Linux/amd64 镜像和 H5 三件套；H5 只
  作为 manifest 与隔离 smoke 的候选工件，不切换公网软链。
- 切换前创建 SQLite 一致快照、PostgreSQL custom dump、四份 root-only env 备份与状态
  证据；任一 runtime、Provider、readiness 或公网门禁失败，立即恢复旧 env 与旧 runtime。
- 后端成功标准：四容器 `healthy`、restart 0，readiness 精确绑定本 tag，9/9 core、Agent、
  LiveKit、FunASR、Qwen、Doubao 与 InterruptSemantic 真实 smoke 全部通过；公网 H5、API、
  WMS 路由保持原合同。
- 只有新后端完全通过后才上传 `0.8.51`。自动化与开发者工具上传不能代替 iOS/Android
  外放、听筒、蓝牙、系统录音中断和弱网下的真机声学验收。
