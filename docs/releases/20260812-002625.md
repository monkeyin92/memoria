# 20260812-002625 Release

## 状态与范围

- 基于 `20260811-230535` 的生产热修复；小程序、设备媒体协议、ESP32 固件和 H5 内容不变。
- 修复 PostgreSQL Identity FORCE RLS 下首次 `self_use` 绑定失败：账户本人和主主体读取必须显式携带
  owner actor，禁止依赖 SQLite 对无 actor 内部读取的开发期兼容行为。
- 回滚目标为 `20260811-230535`；本次无数据库 DDL、密钥、固件或 Nginx 结构变更。

## 发布门禁

- `test_multi_subject_api.py` 与 `test_device_onboarding_binding_integration.py` 必须通过；新增测试强制所有
  self-binding Person 读取均携带 owner actor。
- Ruff、严格 mypy、镜像 commit/tag/role provenance、线上 Control 12/12 core、Agent 心跳和供应商
  smoke 必须通过。
- 生产保留已有 claim，使用同一幂等键重试绑定；Activation 必须签发版本 2，并由真实 ESP32 以递增
  counter 3 确认。未取得设备 WSS、真实语音上下行和 playback receipt 前不得宣称板端验收完成。
