# Release 20260716-225754

## 范围

- 同时发布 Control API、Agent runtime 与 H5；现有级联继续作为默认，页面新增 `Qwen3.5-Omni-Flash` 隔离 A/B。
- 新增首声 trace、duck-first 候选打断、独立 listener cue、FunASR + Qwen3-ASR 情绪旁路和受控 CosyVoice 情绪输出。
- Omni 固定使用 `qwen3.5-omni-flash-realtime` 与 `semantic_vad`。浏览器通过 WebRTC 直连百炼媒体面，Control API 只代理 SDP 与服务端鉴权；浏览器不接收永久 Provider 凭据。
- 生产只要求已有的 `DASHSCOPE_API_KEY`。北京 Workspace ID 由服务端内置，`DASHSCOPE_WORKSPACE_ID` 仅为可选覆盖，本次保持未设置。
- LiveKit Server、Nginx、证书、防火墙、443/8443 和 7882/UDP 策略均未修改；listener cue 两个生产开关继续使用默认 `false`。

## 工件与备份

| 工件 | SHA-256 / Image ID |
| --- | --- |
| `memoria-control-api:20260716-225754` | `sha256:64f465e6225eb0bcecf67e581b4d0aba8b245390690da4608912c475d83a616f` |
| `memoria-agent:20260716-225754` | `sha256:a1adb31a972bb042ea364885e0660c46ab5f8761150c6683e2197bee6f9b9c5b` |
| H5 `index.html` | `d0c2b7d99124d8d2da0a6c23947c18594cc93e1fdb0440273d50252768d1b65d` |
| H5 主 JS | `1804e886768e2db83b71f17586ccd2bf2ea33324119153605236ed48e27c8b18` |
| H5 主 CSS | `9275210d0de69bc654f521c1863f119edaeadf7dde1782376671fd54be99dc87` |
| SQLite 发布前快照（两份相同） | `436d65aa80cd85ec0e0d922f3a75d0fb57ea5e2d891fe6d39676a7740dc91b47` |
| 线上 Memoria Nginx snippet（本次未修改） | `0673fd8805c3859eb6e1b44c155607cca326df2cc37f4b689b2a7526ae0c61e8` |
| `aginice.cn` 证书指纹 | `91:47:47:4F:0C:D0:DA:BE:56:08:19:D3:B6:70:F1:F0:20:1A:C4:6C:6A:B1:0C:0D:B0:B9:26:54:C5:5A:3F:CF` |
| 公网 IP 证书指纹 | `3A:21:A0:14:EC:3B:12:85:E9:99:F1:64:77:8E:0B:82:64:48:8C:B4:DD:26:74:F1:7F:92:DB:49:0C:5D:9F:2F` |

两份 SQLite 快照位于 `/var/lib/memoria/memoria-pre-20260716-225754.sqlite3` 与 `/var/backups/memoria/memoria-pre-20260716-225754.sqlite3`，均为 `root:root 0600`，`integrity_check=ok`，foreign-key violations 为 0。环境保护副本位于 `/var/backups/memoria/memoria.env-pre-20260716-225754`，同为 `root:root 0600`；发布记录不保存 secret 值。

## 激活时间

- `2026-07-16T15:09:00Z`：原子切换 `/opt/memoria/current`，随后重建固定 tag 的 Control API 与 Agent。
- `2026-07-16T15:15:37Z`：runtime、Provider、readiness 与生产 Omni WebRTC 全部通过后，最后原子切换 `/var/www/memoria-h5`。

## 验证

- 本地门禁：Python `239 passed`；H5 `51 passed`；Ruff、mypy strict 与 H5 production build 全部通过。
- 两张镜像均为 Linux/AMD64；Control API 与 Agent 均 healthy，发布后 10 分钟内错误关键词计数均为 0。
- LiveKit RoomService、FunASR、Qwen（thinking disabled）与 CosyVoice 实网 smoke 全部 PASS；readiness 精确绑定 `20260716-225754`、provider `qwen`，五项检查全部为 true。
- 生产公网 Omni：匿名鉴权 200、创建会话 200、SDP 交换 200 且返回 `application/sdp`；实际模型为 `qwen3.5-omni-flash-realtime`，WebRTC 状态为 connected，收到远端音轨与 `session.created`、`session.updated`。
- 新 H5 页面首次进入默认勾选“现有级联”，可切换到 `Qwen3.5-Omni-Flash`；连接成功后显示 ready，通话中禁用模型切换，结束会话正常，console warning/error 为 0。验收后已恢复级联选择并关闭测试页面。
- 公网根 H5、兼容 H5、SPA fallback、API live/ready、EchoLife health 与公网 IP 兼容入口均为 200；`internal`、PocketSparks、Goods Invoice 原路径均为 404。
- 本地 build、服务器版本目录和公网响应的 H5 `index.html` SHA-256 完全一致。
- `nginx -t` 通过，未安装或 reload 本地新增的 `/memoria-api/v1/sessions/` 专用限流块；现网通用 `/memoria-api/` 已完成真实 SDP 代理，应用层继续执行 64 KiB、会话所有权与每会话两次交换限制。
- PocketSparks 无运行容器；Goods Invoice、WMS 与 MySQL 均为 inactive/disabled。`memoria-readiness-refresh.timer` 与 `snap.certbot.renew.timer` 均为 active。

## 已知边界

- 本次证明了生产信令、远端音轨和页面生命周期，但真人语音的首声、打断、附和、AEC、情绪听感和成本对比仍由用户在同一设备上验收。
- 级联链路支持受控情绪语气；Omni 按语音语义与提示词自适应。尴尬笑、咳嗽等显式副语言动作尚未做确定性动作层，不应宣称已完整交付。
- 公网 IP 兼容证书有效至 `2026-07-22T05:31:44Z`；自动续期 timer 当前正常，需持续监控首次续期结果。

## 回滚

- Runtime 直接回滚点：`20260716-171958`。
- H5 直接回滚点：`20260716-150805`。
- 回滚前恢复 `/var/backups/memoria/memoria.env-pre-20260716-225754`，再用旧 tag 执行 Compose `up -d --no-build`。
- 普通回滚不恢复数据库；只有确认数据格式不兼容时才使用本次发布前 SQLite 快照。
