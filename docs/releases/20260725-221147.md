# 20260725-221147：小程序麦克风上行真机修复

## 发布目标

- 修复真机语音会话显示“我在听”但 Agent 未收到任何用户音频的问题。
- 小程序只在 `RecorderManager.onStart` 后进入录音态；首帧/首个 SocketTask 发送未确认时受控重启一次，仍失败则明确结束会话提示用户恢复。
- PCM 上行按 SocketTask 的成功回调串行发送，并将异步发送失败反馈给页面，避免无提示丢帧。
- 网关在内存维护每个连接的上行帧数、字节数和转入 LiveKit 的帧数；不处理或记录音频正文、
  转写或票据。

## 固定范围

- 源码提交：`b234624174b0099d0319020d99fa665f04a90eb7`。
- 运行时候选：Agent、Control API、Speaker Model、`miniprogram-gateway` 四个 amd64 镜像。
- H5 无源码改动，仅按发布门禁生成同提交的候选工件；不切换 H5 软链。
- 发布前线上回滚点：runtime `20260725-211256`。

## 发布前验证

- 小程序逻辑测试：19/19 通过。
- 小程序全部 JavaScript 已通过 `node --check`。
- 网关定向测试：7/7 通过。
- `ruff check services/miniprogram_gateway/bridge.py`、strict mypy 与 `git diff --check` 通过。
- 线上基线：2026-07-25 22:10 CST，四个 runtime 容器均 healthy，`/memoria-api/health/ready` 为
  `ready / 20260725-211256`。

## 发布结果

- 2026-07-25 22:28 CST，runtime 已原子切换为 `20260725-221147`；H5 继续保持
  `20260723-192611`。
- 本地 SHA-256：source `849a96e6fc3130bc5e885befe1d816db91813264a732be729c4fa32f9d2def69`，
  images `b8c819058342597fd212eb5701cac22afcf6ce4350218343d3e8c8cc18a3fdc8`，
  H5 `22d85b7b578bc9c5d9067d364bfeeddee4f4e54fdd3094c4903dfc853cdc4fe6`，
  manifest `34f78ec068375dc5a9030a85b0c35258ee8e41e9d91be182e43771b2762352a4`。
- 服务器已验证 verifier、manifest、三个归档、四个导入镜像和候选 smoke；SQLite
  一致性/外键检查与 PostgreSQL custom dump 列表检查通过，四份最小权限环境文件已备份。
- 新 runtime 的 LiveKit、FunASR、Qwen、Doubao、readiness 与公网 API/H5/WMS 路由均通过；
  无效小程序 ticket 被关闭为 `4401`。
- 合成 PCM 经生产小程序网关进入 Agent，观察到 `user_speaking`、转写、`turn_committed`
  与助手 speaking；这证明本次后端媒体链路可用，但不能替代真机录音验收。
- 微信开发者工具已上传体验版 `0.8.44`（`598496` bytes），描述为“修复真机麦克风上行与发送确认”。

## 真机验收门槛

- 真实 iPhone 必须出现 Agent 的 `last_user_audio`、VAD 和已提交话轮，并收到相应回复。
- 若录音启动或上行失败，小程序必须自动重试一次；仍失败时明确结束会话并显示恢复入口，不能继续显示“我在听”。
- 仅收到欢迎语、仅 Socket 建连、模拟器录音或合成 PCM 回放都不能替代该验收。

## 回滚

- 运行时恢复至 `20260725-211256`；H5 保持原软链，不自动回滚数据库或用户会话数据。
