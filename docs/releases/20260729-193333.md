# 短期上下文与情绪化语音 Release 20260729-193333

## 目标

- 发布 `20260729-185111` 已通过评审和本地门禁的短期上下文、情绪聚合、自然表达与豆包 TTS 风格控制。
- 修正首个候选的 raw-only canary：沿用生产播放链既有的 `ok/scaled` 时间戳契约，继续拒绝 `degraded`。

## 发布前验证

- 完整 Python pytest、Ruff、strict mypy、离线 E2E、H5 `242/242` 与 production build 通过。
- owner/public scope、Qwen PCM turn epoch、`SpeechPlan`/TTS generation config、迟到控制确认音和 self-preview voice rebind 均有回归测试。
- `20260729-185111` 两次风格 canary 分别在不同音色得到 `scaled`；无 `context_texts` 对照中 `calm_guide/low_magnetic` 同样为 `scaled`，没有证据表明该状态由风格上下文独有触发。

## 生产门禁

- `DOUBAO_TTS_STYLE_CONTROL_ENABLED=true` 时 required Provider smoke 必须遍历五个批准音色；每个音色均要求非空 24 kHz mono s16le PCM、单调且为 `ok/scaled` 的字级时间戳，以及 FunASR 回识别目标正文且不包含引用上文关键词。
- 任一音色出现 `degraded`、空音频、时间戳异常、回识别失败或引用上文被朗读，均不得切流。
- 仅切换 runtime；H5 保持 `20260729-113831`，小程序不上传、不提审、不发布。

## 回滚

- 切流前创建 SQLite 一致快照、PostgreSQL custom dump 与四份 root-only env 备份。
- runtime 故障先恢复 env 备份，再原子切回切换前实时读取的 runtime tag；不自动回滚数据库或 MinIO。

## 发布结果

- source commit / annotated tag：
  `c9cf96e32d0a71c8ee11651e6949c8de0e1a7205 / 20260729-193333`；`main` 与 tag 已推送到私有远端。
- source、四个 `linux/amd64` 镜像、候选 H5、manifest 与 verifier 在本机和服务器均完成
  commit-bound 复验；服务器隔离 smoke 覆盖候选 H5/SPA/API、owner-only 默认值与 SQLite restart。
- 切流前五音色 canary：`warm/bright/soft=ok`、`calm/low=scaled`；readiness canary：
  `warm/low=ok`、`bright/soft/calm=scaled`。两轮均无 `degraded`，普通音频与五段 styled PCM
  的 FunASR 回识别、引用上文排除、Qwen、LiveKit、Interrupt Semantic 和取消全部通过。
- runtime 于 `2026-07-29T12:28:16Z` 原子切换至本 tag；四个 runtime 容器均
  `healthy / restart=0`，严重错误日志为 0。readiness 绑定本 tag，9 项 core 全 ready，
  LLM 为 Qwen、TTS 为豆包；`DOUBAO_TTS_STYLE_CONTROL_ENABLED=true` 已生效。
- 公网 8443 根 H5、兼容 H5、SPA、live、ready、WMS 均为 200；internal、PocketSparks 与
  Goods Invoice 旧路径为 404。Nginx 配置、域名证书 SAN、443 loopback WMS 与 SQLite
  完整性/外键均通过。WMS 保持既有 `enabled/inactive` 资源让渡状态，静态页正常，本轮未操作它。
- H5 未切换，继续指向 `20260729-113831`；直接 runtime 回滚点为 `20260729-171002`。
  root-only 备份目录：
  `/var/backups/memoria/runtime-switch-20260729-193333-from-20260729-171002-20260729T122724Z/`。
- 固定工件 SHA-256：source `4a0a9a8f458af26b0162a3a757463d18700d531e204fc7f4a08ba12cd46ffb4d`；
  images `2705dfcc1c337e578fd2d1d8978b6b361d28641be3971fca4245a50654c9d48b`；候选 H5
  `91cf0e65c2e03baa89b8fec4bcda4eb1ef6eec9d803aa1923b0ba0dcdb074c04`；manifest
  `5d09dc3eb46b7feb32778371d08e2567238049dfc132aff246c29fd9cdadb27b`；verifier
  `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101`。
- SQLite 一致快照 SHA-256：
  `e2d0caabf6f78306ec9d475ee73f85993935b0b46ea5a693603cea4872ce90c5`；PostgreSQL custom dump
  SHA-256：`12474e257549542042a7a7ee905c8cdf572e8e283b09acbfe635b081d16f962d`。
- 本轮服务器 canary 证明协议、时间戳和可懂度门禁，不等同于真机端对自然度、方言准确度、
  情绪强度或真实笑声的主观验收；豆包指令仍是软控制，不能承诺每次都产生笑声。
