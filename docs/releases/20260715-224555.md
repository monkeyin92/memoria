# Memoria Release 20260715-224555

- Runtime 状态：ACTIVE
- 默认 LLM provider：`qwen`（百炼）
- 目标服务器：`110.42.235.198`
- H5：`https://110.42.235.198/memoria-h5/`
- Control API：`https://110.42.235.198/memoria-api/`
- 发布目录：`/opt/memoria/releases/20260715-224555`
- 当前 runtime 链接：`/opt/memoria/current -> /opt/memoria/releases/20260715-224555`

## 工件

| 工件 | ID / 校验和 | 架构或落点 |
|---|---|---|
| Control API image | `sha256:6b8a8f231d1d2372e78d4ad9e25cbaeb04116e7ac61a96c973de4b1bdfd4de5f` | `amd64` |
| Agent image | `sha256:baa7841c53a2c81b8f21255e2f5d3fbf482d87f61880f11ea035b104e923a21b` | `amd64` |
| H5 `index.html` | `待主线补全：sha256` | `/var/www/memoria-releases/20260715-224555/index.html` |
| H5 main JS | `待主线补全：文件名 + sha256` | `/var/www/memoria-releases/20260715-224555/assets/` |
| H5 main CSS | `待主线补全：文件名 + sha256` | `/var/www/memoria-releases/20260715-224555/assets/` |
| Nginx IP server | `待主线补全：线上文件 sha256` | enabled site `memoria-ip` |
| Memoria HTTPS snippet | `待主线补全：线上文件 sha256` | `/etc/nginx/snippets/memoria-https.conf` |

## Runtime 与门禁证据

- `memoria-control-api-1`：healthy，监听 `127.0.0.1:8791`。
- `memoria-agent-1`：healthy，以 `duplex-zh-agent` 注册到 LiveKit Cloud。
- `/health/ready`：`ready`，绑定 release `20260715-224555` 与 provider `qwen`。
- readiness evidence：SQLite 持久化，24 小时有效；同版本 Control API 重启后保持 ready。
- `memoria-readiness-refresh.timer`：enabled/active，每 12 小时运行；失败后按 service 策略重试。

精确门禁输出：

```text
livekit_smoke_test PASS: authenticated room-service access
provider_smoke_test PASS: FunASR, Qwen, CosyVoice
verify_env OK
readiness mark OK
readiness check OK
readiness refresh PASS: 20260715-224555 (qwen)
```

Systemd readiness service 手工执行结果：`Result=success`、`ExecMainStatus=0`。

## 自动化与设计验收

- 后端：Ruff、mypy strict、202 项 pytest 全部通过。
- H5：26 项测试与 production build 通过。
- Product Design QA 与 390×720 浏览器视觉/交互回归通过；console 无 error/warning。
- 只持久化权威字幕事件，避免分段字幕重复落库。

## 身份、安全与持久化

- H5 使用服务端签发的匿名 Bearer 身份；已存 token 通过 `/v1/auth/me` 校验，只有 401/403 才换发。
- Memory 与 session route 强制认证和资源所有权；跨用户访问返回 403/404。
- 消息、Profile、Agent Context 与 FunASR Context 在持久化或发送上游前统一做 PII 脱敏。
- SQLite 持久化消息、Qwen 每日回顾、Profile、三个偏好、会话控制和 readiness evidence。
- Control API 同版本重启后，同一 token 仍可读取 Profile、偏好和 `source=qwen` 的回顾；会话控制与 readiness evidence 保持。
- 生产关闭 `/docs`、`/redoc`、`/openapi.json`；公网 `/memoria-api/internal/` 固定返回 404。
- 永久 LiveKit、百炼和应用认证 secret 只存在服务器 secret 文件；本清单不记录任何值。

## TLS

- 证书：Let's Encrypt 公网 IP 短期证书，SAN `110.42.235.198`。
- 有效期至：2026-07-22。
- 自动续期：snap Certbot renewal timer 已启用，renewal dry-run 已通过。
- Deploy hook：先执行 `nginx -t`，成功后才 reload；具体线上安装路径待主线在下方验收表回填。
- 80 端口 ACME challenge 已从公网验证为 200。

## 公网激活验收回填

以下项目必须由执行最终 H5 安装与 Nginx reload 的主线填写；在填写前，不得把本节当作公网激活证据。

| 项目 | 最终值 |
|---|---|
| `/var/www/memoria-h5` 链接目标 | `待主线补全：应为 memoria-releases/20260715-224555` |
| Nginx `memoria-ip` enable + reload 时间（UTC） | `待主线补全` |
| `nginx -t` | `待主线补全：success + 验收时间` |
| `GET /memoria-h5/` | `待主线补全：HTTP status` |
| `GET /memoria-h5/arbitrary-spa-route` | `待主线补全：HTTP status` |
| `GET /memoria-api/health/live` | `待主线补全：HTTP status + body status` |
| `GET /memoria-api/health/ready` | `待主线补全：HTTP status + release/provider` |
| `GET /memoria-api/internal/` | `待主线补全：应为 404` |
| Certbot deploy hook 安装路径 | `待主线补全` |
| 公网证书校验 | `待主线补全：trusted + SAN + notAfter` |
| 既有项目回归 | `待主线补全：EchoLife / PocketSparks / Goods Invoice / 8443` |

## 非阻塞规模化项

200 条真实中文录音、完整 AEC 设备矩阵和第 21 章 SLO 尚未形成规模化证据；它们不阻塞当前 H5 成品交付。

发布、备份、证书运维和回滚命令见 `docs/production-deployment.md`。
