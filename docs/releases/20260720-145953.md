# H5 Release 20260720-145953

## 目标

补发迁移后完成的三处 H5 onboarding 修正：从声纹步骤返回时恢复所选机器人并重新居中、窄屏按钮与详情不溢出、横屏布局保持完整。只切换 H5 静态产物，不重启 runtime、Control API、Agent、LiveKit 或数据服务。

## 本地门禁

- `npm --prefix apps/h5 test`：12 个测试文件、115 项测试全部通过。
- `npm --prefix apps/h5 run build`：production build 成功。
- 新增回归覆盖从声纹步骤返回后仍选中并定位原卡片。

## 工件与发布

- H5：`/var/www/memoria-h5` -> `/var/www/memoria-releases/20260720-145953`
- Runtime：保持 `/opt/memoria/current` -> `/opt/memoria/releases/20260720-140053`
- H5 直接回滚点：`20260720-140053`
- H5 archive SHA-256：`0f28e50ebc3e382e765b19271044d2375f960216b6ed956c0aac4ee38853e417`
- `index.html`：`8c79e4084b5a039264d8cfe5017aaf01264871366c40e449c0ea1f83d39bb022`
- 主 JS：`7b4e347d709b51ac02b324ef376967f35925d85e6e7025fc7e498c943d6bd609`
- 主 CSS：`41371d6db758ffb49dc075f518c39baf49212845d2295471adf936b7185b9452`

上传 archive、正式 release 文件与公网下载结果的三项哈希一致。切换前后三个应用容器的 `StartedAt` 未变化，未发生 runtime 重启。

## 线上验收

- 公网根 H5、兼容 H5、Control API ready 与 `/wms/` 均为 200；ready 仍绑定 runtime `20260720-140053`、provider `qwen`，9 项 core check 全绿。
- 注册后可进入五机器人选择页；选择玄墨进入声纹页再返回后，玄墨仍为选中状态且卡片可见。
- 375×667 与 667×375 均无横向溢出；横屏页面宽高分别稳定为 667×375。
- 浏览器 console warning/error 为 0。
- 本次创建的验收账号已通过 `AccountDataGovernance` 删除；删除后 SQLite 完整性为 `ok`，未产生声纹、档案或对象记录。

## 未验证边界

内置浏览器不提供麦克风能力，真实手机麦克风授权、三段声纹录制、全双工听感和弱网/AEC 仍需设备验收。
