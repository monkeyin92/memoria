# 20260811-230535 Release

## 状态与范围

- 候选目标：把 ESP32-S3 从上游 `xiaozhi.me`/本地联调链路一次切到 Memoria 生产链路；H5 保持
  `20260808-171749`，不在本次切换范围。
- Runtime 增加 Device Bootstrap/Claim/Binding/Activation、独立 Device Media Gateway、Agent
  hardware dispatch 和 Nginx WSS 路由。小程序保留首页与 AI 对话，同时增加扫码认领、设备管理和
  初始化流程。
- Device Onboarding 生产权威固定为 PostgreSQL + FORCE RLS；Control API 使用
  `memoria_device_onboarding_api`，制造登记只允许短时使用
  `memoria_device_onboarding_maintenance`。生产不得回退 SQLite/offline mock。
- Activation 使用独立 32 字节 Ed25519 seed；Device Gateway ticket、Mini Program ticket、Auth、
  LiveKit 和内部 capability token 必须相互独立。设备媒体固定为 Opus：上行 16 kHz、下行 24 kHz、
  20 ms frame。

## 发布前证据与回滚

- 生产发布前备份目录：`/var/backups/memoria/path2-20260811-230535`，18 个文件均为
  `root:root 0600`，`sha256sum -c` 已通过。
- SQLite backup SHA-256：
  `4186e23b59115a449410f18072348d85a13dea40074b35ae82dab2660518a51b`。
- PostgreSQL custom dump SHA-256：
  `95532263ef7e9f95264463e0323778c3edaf6a756cd932788b7b742a7495ed96`；临时复制进 PG17 容器后
  `pg_restore -l` 已通过。
- Runtime 回滚目标固定为 `20260808-171749` / commit
  `9812fac155ef4f46a74d0d8dbfaf197fe9c5fa5a`。数据库变更为 additive/forward-only；代码回滚不删除
  新角色、表或 RLS policy。
- 板端完整回滚镜像：`firmware/esp32/artifacts/backups/pre-path2-upstream-working-merged.bin`，
  SHA-256 `ea3b37904e42c42a8334b9808871e8bebff2f72f9ed02dbc4000ec35fdf1e250`。

## 必须通过的上线门禁

- 干净 commit/tag、五个 linux/amd64 镜像的 commit/tag/role provenance、canonical manifest 与工件
  SHA-256 全部一致。
- PostgreSQL 角色、表、FORCE RLS、无上下文不可见、actor/device 隔离和并发 CAS 测试通过；生产
  Control API 启动时不得出现 SQLite onboarding 文件。
- Control、Agent、Mini Program Gateway、Device Media Gateway 和 Speaker Model healthy；公网
  `/health/ready` 为同 release ready，Nginx `-t` 通过。
- 正点原子 ESP32-S3 只更新 identity 分区后，必须完成生产 Activation、WSS 握手、真实语音上行、
  Agent/ASR/LLM/TTS 下行和 playback receipt；单纯容器健康或 WebSocket 101 不能代替设备验收。
