# 20260812-114447 Release

## 状态与范围

- 基于生产健康版本 `20260812-111951`，完成 ESP32 Path 2 真机欢迎语和后续对话的媒体边界修复。
- Agent/小程序共享桥接的内部 `generation_id` 合法地从 `0` 开始；设备协议把 `0` 保留为“尚无
  播放代次”，因此原网关把首个 `audio_reset` 判为 stale 并以 4400 关闭真实设备 WSS。设备网关
  现在统一映射 `内部代次 N → 设备代次 N+1`，并把设备按钮/播放回执映射回内部代次；欢迎语、
  正常回答和打断后的新代次使用同一规则，Agent 的权威 generation fence 不变。
- 网关协议拒绝日志新增 `handshake/sender/receiver/room-disconnect` 组件和固定服务端原因，不记录
  设备 JSON、音频、票据、用户文本或 secret，后续同类故障可以直接区分收发方向。
- 生产环境生成器使用同一 HMAC 材料生成 Control 的
  `MEMORIA_RUNTIME_PROFILE_SIGNING_SECRET` 和 Agent 的
  `MEMORIA_RUNTIME_PROFILE_VERIFY_KEY`，拆分后两端只获得各自别名；生产缺失或不一致时 fail
  closed。这固化了 `20260812-111951` 验收期间已安全接通的 root-only 临时配置。
- 本次不含数据库 DDL、数据修订、固件或 H5 变更。回滚目标为 `20260812-111951`；回滚代码时保留
  当前 Runtime Profile 验签配置不会扩大 Agent 权限。

## 发布门禁

- Mini Program Bridge、Device Media Gateway、生产 env 生成与 compose 共 `133` 项测试通过；
  Device Media Gateway 独立 `17` 项通过。
- 首代次 WebSocket 回归证明内部 generation `0` 实际发出设备 generation `1` 的
  `playback.flush` 与 Opus 音频帧，`playback.ended` 再还原为内部 generation `0`；后续代次和
  uint32 溢出边界均有测试。
- Ruff、相关源码严格 mypy 与 `git diff --check` 通过。
- 切流后必须用真实 ESP32 证明 WSS 保持连接、上行语音进入 Agent、ASR/LLM/TTS 完成、扬声器
  实际播放且服务器收到 `playback.ended`。合同和容器健康不能替代真实声学闭环。
