# 20260812-003754 Release

## 状态与范围

- 取代未切流的 `20260812-002625` 候选，同时包含首次 `self_use` 绑定所需的 Identity owner actor
  修复。
- 修复 Device Onboarding PostgreSQL FORCE RLS 下的系统过期转换：精确 lookup 仍保持只读，只有锁定
  session/claim 行后，才能从该权威行派生 device 写上下文并执行 expiry mutation。
- 不扩大客户端 actor/device 权限，不允许 lookup GUC 直接获得写权限；无数据库 DDL、密钥、固件或
  Nginx 结构变更。
- 回滚目标保持 `20260811-230535`。

## 发布门禁

- PostgreSQL 17 真实适配器测试必须覆盖 session expiry、claim expiry、RLS、并发和完整
  Bootstrap/Claim/Activation API，共 9 项通过。
- Control 绑定测试、Ruff、严格 mypy、五个 amd64 镜像 provenance、线上 12/12 readiness 与供应商
  smoke 必须通过。
- 过期 claim 只能返回业务态，不得再产生 500；随后使用新扫码会话完成绑定并签发 Activation v2。
- 未取得真实 ESP32 Activation ACK、设备 WSS、语音上下行与 playback receipt 前，不得宣称板端验收完成。
