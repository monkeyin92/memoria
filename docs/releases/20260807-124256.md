# Memory Rebuild Image Entrypoint Release 20260807-124256

## Scope

- Include `scripts/rebuild_memory_projections.py` in the Control API full and delta image paths.
- Invoke the maintenance command as `python -m scripts.rebuild_memory_projections` so the image
  keeps `/app` on `sys.path` and can import the production `services` package.
- Restore the Docker-context exclusion for regenerated H5 QA captures.

## Release Gate

- The prior `20260807-115142` rollout stopped before truncating projections because its image
  omitted the maintenance script. Its evidence ledger and user-facing H5 were unchanged.
- This tag must pass fresh commit-bound artifact verification, isolated server smoke, projection
  rebuild, RLS/orphan checks, fixed Chinese memory evaluation, readiness and public acceptance.
- `20260806-213522` remains the runtime and H5 rollback target until those gates pass.

## Deployment Result

- Source commit: `9dd25d95b2228180c4bbf9e7d24bef13b78fcf10`; annotated tag:
  `20260807-124256`.
- GitHub Actions [`31148538676`](https://github.com/monkeyin92/memoria/actions/runs/31148538676)
  passed Python, H5, Mini Program, Go/media-edge and Trivy jobs.
- Commit-bound artifact hashes: source `2ef28151df2612de8338a32d4e0fd22d5e9f1eab5bbf1c428a0643b40e1d4100`,
  images `5aba191d75f080de728790d73e996418d444bbdb6a19e1d22527df66223f5fd3`,
  H5 `fe99e0316e07442e5aed967b66b15654c7186f3951e8bb214ac59e8028d4d12e`,
  verifier `e949258605f7b2f8ce216dabf4cd06cbead1f7ab22a1c1defbb4c19bb4a96314`,
  manifest `6194a3f9c82d6e6b127408ac8e97fb60617075d3dccbcb7e2eae0055003e4436`.
- Server verifier, image import and isolated smoke passed. SQLite double snapshot hashes match
  `e9ff7e099a090d05f96b15dbe93dfe4f44a4554d637c42f799fb100e77fff43b`; the root-only
  PostgreSQL pre-rebuild dump hash is
  `ff0a6cd8cc739db4b01c4408f2677df9f4c0e46e35184245bf91e5646823351e`.
- After all archive writers stopped, rebuild reported `compiled=4`, `ignored=1126`, `failed=0`.
  The immutable evidence manifest remains
  `608562844dd3b188a7209dd9562629c0b565bdfe4d8c79debb24b02cadf73639`; all compile
  outbox rows completed. The four historic owner turns did not contain a confirmable memory, so
  claim/search/vector projections remain zero rather than fabricating a backfill.
- All 18 orphan and foreign-key checks are zero; RLS passed for 48 tables and 17 account scopes.
  The fixed Chinese evaluation has 13/13 cases with zero candidate, contradiction and
  cross-account leakage. Runtime memory context is enabled for future confirmed owner turns.
- Runtime and H5 both point to `20260807-124256`; immutable H5 union contains 232 assets.
  Provider readiness is `ready` for this tag, including LiveKit, FunASR, LLM, Doubao and nine
  core checks. Public 8443 root/H5/SPA/live/ready/WMS routes return 200; internal and retired
  legacy routes return 404. Browser registration view loaded as `Memoria` with no console logs.
- Direct rollback target remains runtime/H5 `20260806-213522`. A real logged-in, multi-day
  microphone conversation was not injected into production; that remains a device acceptance
  activity, not evidence fabricated by the release canary.
