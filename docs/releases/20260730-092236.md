# 动态学习角色与危机支持 Release 20260730-092236

## 目标

- 将 Companion 的互动方式收口到唯一的 Control ResponsePlan：每轮按
  `危机支持 > 语言学习 > 引导式学习 > 普通陪伴` 动态选择，不创建持久角色状态或第二套路由。
- 外语学习先复用已有目标、水平与场景，信息不足时最多追问一到两个关键问题，随后给出短计划并
  立即开练；知识学习先给线索或思路框架，用户尝试或明确索要结论后再逐步给答案。
- 明确轻生、自伤、已行动或伤害方法请求不再被旧违禁词短路成“我不知道”；先提供不依赖
  LLM、私人记忆或工具的固定危机支持，并让语音进入 supportive、禁笑和 listener-cue 抑制。

## 发布范围

- 仅切换 runtime；H5 保持 `20260729-113831`，小程序不上传、不提审、不发布。
- 生产当前回滚基线为 runtime `20260729-193333`。切流前必须重新读取实时软链并创建 SQLite
  一致快照、PostgreSQL custom dump 与四份 root-only env 备份；故障先恢复 env，再原子切回
  发布前 runtime，不自动回滚数据库或 MinIO。
- 发布工件必须绑定本 release 的 clean source commit 与 annotated tag；本机和服务器均验证
  source、四个 `linux/amd64` 镜像、候选 H5、manifest 与独立 verifier。

## 发布前门禁

- Python 全量 `1488 passed, 29 skipped`。
- Ruff、strict mypy `177 source files`、`git diff --check` 与离线 E2E 通过。
- 定向回归覆盖 canonical ResponsePlan、明确/一般自伤语义分流、Control API 固定危机回复、
  Agent 本地安全降级白名单、supportive SpeechPlan、禁笑与 listener cue 抑制。
- commit-bound server smoke 新增五类 ResponsePlan canary：本人危机固定直出、第三方求助、
  受害者紧急求助、一般预防咨询和危害实施拒答；任一合同不符都不得继续切流。
- 仓库远端为 `PRIVATE`；提交和工件必须排除现有小程序素材、lockfile、上传脚本、`.workbuddy`
  与本机私钥。

## 生产结果

- 待 commit-bound 工件、服务器隔离 smoke、Provider/readiness、容器、Nginx、公网 API 与
  固定危机 ResponsePlan 验收完成后补录。
