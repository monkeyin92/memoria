# 动态学习角色与危机支持 Release 20260730-092236

## 目标

- 将 Companion 的互动方式收口到唯一的 Control ResponsePlan：每轮按
  `危机支持 > 语言学习 > 引导式学习 > 普通陪伴` 动态选择，不创建持久角色状态或第二套路由。
- 外语学习先复用已有目标、水平与场景，信息不足时最多追问一到两个关键问题，随后给出短计划并
  立即开练；知识学习先给线索或思路框架，用户尝试或明确索要结论后再逐步给答案。
- 明确轻生、自伤、已行动或伤害方法请求不再被旧违禁词短路成“我不知道”；先提供不依赖
  LLM、私人记忆或工具的固定危机支持，并让语音进入 supportive、禁笑和 listener-cue 抑制。

## 发布范围

- 仅切换 runtime；H5 保持 `20260729-113831`，小程序不上传、不提审、不发布。
- 生产当前回滚基线为 runtime `20260729-193333`。切流前必须重新读取实时软链并创建 SQLite
  一致快照、PostgreSQL custom dump 与四份 root-only env 备份；故障先恢复 env，再原子切回
  发布前 runtime，不自动回滚数据库或 MinIO。
- 发布工件必须绑定本 release 的 clean source commit 与 annotated tag；本机和服务器均验证
  source、四个 `linux/amd64` 镜像、候选 H5、manifest 与独立 verifier。

## 发布前门禁

- Python 全量 `1488 passed, 29 skipped`。
- Ruff、strict mypy `177 source files`、`git diff --check` 与离线 E2E 通过。
- 定向回归覆盖 canonical ResponsePlan、明确/一般自伤语义分流、Control API 固定危机回复、
  Agent 本地安全降级白名单、supportive SpeechPlan、禁笑与 listener cue 抑制。
- commit-bound server smoke 新增五类 ResponsePlan canary：本人危机固定直出、第三方求助、
  受害者紧急求助、一般预防咨询和危害实施拒答；任一合同不符都不得继续切流。
- 仓库远端为 `PRIVATE`；提交和工件必须排除现有小程序素材、lockfile、上传脚本、`.workbuddy`
  与本机私钥。

## 生产结果

- source commit / annotated tag：
  `d6295b50039c37efe789e51a6495b938d229701b / 20260730-092236`；`main` 与 tag 已推送到
  私有远端，tag 解引用后精确指向该 source commit。
- source、4 个 `linux/amd64` 镜像、候选 H5、manifest 与独立 verifier 在本机和服务器均完成
  commit-bound 复验；服务器隔离 smoke 通过候选 H5/SPA/API、owner-only 默认值、
  SQLite restart 与五类 ResponsePlan 合同。
- runtime 于 `2026-07-30T01:59:55Z` 原子切换到本 tag。4 个 runtime 容器均为
  `healthy / restart=0`，镜像 revision/version 标签精确绑定本 commit/tag，切换后严重错误
  日志为 0。
- required Provider/readiness 通过 LiveKit、Qwen、FunASR、Interrupt Semantic 与豆包 TTS；
  五个批准音色的字级时间戳均为允许的 `ok/scaled`，无 `degraded`。readiness 绑定
  `20260730-092236`，9 项 core 全部 ready，LLM/TTS 为 Qwen/豆包。
- 生产 ResponsePlan canary 通过：本人已服药过量固定直出危机支持；第三方求助、
  受害者紧急求助与一般预防咨询交给统一当轮策略；危害实施请求固定回答
  “我不知道。”。过程未输出内部 token。
- Nginx 配置与域名 TLS SAN 通过；公网 `https://aigcnice.com:8443` 根 H5、兼容
  H5、SPA、live 与 ready 均为 200，internal、PocketSparks 与 Goods Invoice 路径为 404。
  WMS 保持 `enabled / inactive`，443 首页跟随既有 302 跳转后为 200，本轮未操作它。
- H5 未切换，仍指向 `20260729-113831`；小程序没有上传、提审或发布，Control API
  下发的媒体入口仍为
  `wss://aigcnice.com:8443/memoria-mini-media/v1/mini-program/media`。
- 直接 runtime 回滚点为 `20260729-193333`；root-only 备份目录：
  `/var/backups/memoria/runtime-switch-20260730-092236-from-20260729-193333-20260730T015529Z/`。
  SQLite 一致快照与 PostgreSQL custom dump 均已验证，四份环境文件以
  `root:root 0600` 保留；普通 runtime 回滚不自动恢复数据库或 MinIO。
- 固定工件 SHA-256：source
  `1b4a92ab4026ca8cf978a13053bc44c1557256e039b91d9163d298eb59755a1e`；images
  `fd3fb78fb28c5f9fc8ae1a1f56ea419848622431119531ee0b2bc1245c91732e`；候选 H5
  `0ebb74814c36deefb08c273adf16ff4021eb2b07cc09390b9ea9499175783c58`；manifest
  `0bb9ffce22b7d46c3747826582efd28dd322ce0c85677a3437bb44e782e4380e`；verifier
  `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101`。SQLite / PostgreSQL
  备份 SHA-256 分别为
  `52f57fcf17f5eccc7f6370147dc8272a319f4c7aabafe6ce108cb0d97d41d252` /
  `1c551e32ac56e7e063fd2dd45e73de52a79a5a870ed92b89c13ec9d8afc9ee9b`。
- 本轮验收证明路由、固定危机回复、Provider 协议与服务端可用性；真实模型对开放式
  语义的长尾判断、真机危机话术听感和极端网络下的语音交互仍需人工验收。
