# H5 当前发言卡滚动收口 Release 20260730-233824

## 发布目标

- 延续 `20260730-232440` 的明确退出按钮、单条当前发言、乐观用户展示、助手流式替换和
  single-flight 行为。
- 对话卡在普通竖屏和低高度横屏均由卡片自身纵向滚动，长回答不再溢出卡片或贴入固定底栏。

## 发布前门禁

- H5 全量测试：23 个测试文件、253 项通过。
- H5 production build 通过；现有 Rollup 主分片大于 500 KiB 警告仍存在。
- 小程序全量测试：85 项通过；JavaScript syntax 与 `git diff --check` 通过。
- 线上 `20260730-232440` 已实测 390×844、360×740、760×390；本版本需复验卡片
  `overflow-y: auto`、无横向溢出、44px 退出按钮和单条发言。

## 发布与回滚

- 仅切换 H5 静态 release；runtime 保持 `20260730-224522`，不重启任何服务。
- 直接 H5 回滚点为 `20260730-232440`；其上一回滚点为 `20260730-224522`。
- 小程序 `0.8.65` 从本版本最终源码提交上传体验版，不提审、不正式发布。

## 部署结果

- source commit / annotated tag：
  `863f2da95ce042bbdb9aa01272692a089dd82f7b / 20260730-233824`，均已推送远端。
  source / H5 工件 SHA-256 分别为
  `85fa1515af4db8fde746a67f6f33bf5c5d35c4d6f388c5016d937796f42ca11f` /
  `ab8d4b77d2591db3933738443e39af35b2f5e58e273c1333175fa2888cbabb77`。
- 候选自带 111 个资源；与当前历史集合的 106 个同名资源逐项一致，追加合并后为 201 个，
  没有 immutable collision。`index.html`、主 JavaScript、主 CSS SHA-256 分别为
  `e30978be8be92d28caab0ca4ef2a071a9e45fe9892d1ffc64f0b208ea00f50f9`、
  `439966daaac5cf8ed8c060cbe6c7f6fe7f88183651577e5c41f652c82dead369`、
  `aaf6bbc204ea1455efbc7d904e2b976645113dfef5422355f969c8f36041e7a9`。
- 第一次切换后的探针把构建资源错误请求到 `/assets/`，而生产实际基路径为
  `/memoria-h5/assets/`，门禁按设计自动回滚到 `20260730-232440`。修正探针后使用同一工件
  重试，H5 于 `2026-07-30T15:42:40Z` 原子切换；runtime 仍为 `20260730-224522`，四个
  runtime 容器的 `StartedAt / RestartCount / Health` 切换前后完全一致。
- 公网 release provenance、根页面、兼容页面、主 JavaScript、主 CSS 和 API readiness
  均通过。现有登录态下实测发送后立即只显示用户内容，助手回答到达后仍只有一个说话对象且
  用户内容消失；390×844、360×740、760×390 均无横向溢出，退出按钮为 44px，长回答卡
  `overflow-x: hidden / overflow-y: auto`。当前主 JavaScript 日志没有 warning/error。
- 小程序 `0.8.65` 从上述干净提交通过微信开发者工具上传体验版；任务
  `confirmation_upload_1b7f3850-31b1-487c-bcc3-69fefb7060d4` 返回
  `success / execution_success`，总包 `656,661` 字节。未提审、未正式发布；H5 和小程序
  真机触摸、软键盘、设备安全区与弱网表现仍需真实手机验收。
