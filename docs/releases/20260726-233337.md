# 小程序打断重放抑制与话轮延迟 Release 20260726-233337

## 发布结果

- source commit / annotated tag：
  `8176c91dcadcecd02a7af37f7876a360aba5eee6 / 20260726-233337`；
  tag 解引用后精确指向该 commit。
- 第一次保护性切换中，候选 Provider smoke 与 readiness 已通过，但发布侧 JSON 断言
  混用了管道与 here-doc，校验进程读到空 stdin。内置 trap 于
  2026-07-26 16:01:22 UTC 按设计自动恢复 `20260726-215154` 和四份旧 env；
  这不是候选功能或 Provider 门禁失败。
- 修正断言命令后，同一不可变候选于 2026-07-26 16:03:34 UTC 第二次原子激活；
  runtime 为 `20260726-233337`。H5 始终保持 `20260723-192611`，
  小程序体验版保持 `0.8.49`。
- 四个 runtime 容器均为 healthy、restart 0；readiness 为
  `ready / 20260726-233337`，Agent ready、9/9 core、LiveKit、FunASR、Qwen 和
  Doubao 全部通过。
- Agent 文件 env、容器 env 与运行时配置均为
  `ENDPOINTING_MIN_DELAY_S=0.90 / ENDPOINTING_MAX_DELAY_S=1.50 /
  FALSE_INTERRUPTION_TIMEOUT_S=1.70`。
- 本轮完成的是服务器、Provider、路由和配置门禁；真实手机外放下的“停一下”、单次
  确认音、不重复回答及慢语速不拆轮仍待用户复测，不得写成真机通过。

## 发布目标

- AI 播放期间用户说“停一下”后，若 FunASR endpoint final 只重放上一条用户问题，
  立即按控制话轮收口，不再创建第二个 LLM 回答。
- 保留“嗯，你说。”确认音，并让 suppression/yield 事件准确记录
  `interrupt_replay / interrupt_replayed_previous_turn`。
- 修正生产 endpointing 配置漂移，把自建链路从 `1.50 / 2.20` 收敛到代码既有目标
  `0.90 / 1.50`；`FALSE_INTERRUPTION_TIMEOUT_S` 保持 `1.70`。

## 真机根因

- 取证会话：`c67bc334-6ba9-428c-a76c-7bc67e86427d`。
- root-only 归档：
  `/var/backups/memoria/device-session-20260726-20260726-224112/`。
- 第一次“你叫什么名字？”对应 gen4；用户打断后，ASR 又提交了相同 final，形成 gen6。
  服务端没有旧 generation 复活或双 TTS，问题是第二个相同用户话轮被重新提交。
- 该 speech epoch 的 sticky route 为 `interrupt_then_chat`，所以旧实现既不会播放纯控制
  确认音，也会让重放 final 进入 LLM。
- 同一会话多次 `end_of_turn_delay=2.200s`；容器实参仍为
  `ENDPOINTING_MIN_DELAY_S=1.50 / ENDPOINTING_MAX_DELAY_S=2.20`，覆盖了代码中
  `0.90 / 1.50` 的自建默认值。

## 实现

- `UtteranceRouter` 新增 `INTERRUPT_REPLAY`，统一接收当前 speech epoch 的 sticky
  interrupt route 和上一已提交用户文本。
- 精确重放优先于普通 explicit+content；即使上一问题本身以“不是 / 不对”等词开头，
  也不会再次进入 chat。
- Runtime 只保存会话状态，并统一按 `should_interrupt / enter_chat` 执行 clear、
  paused reply、确认音和 StopResponse，不再新增平行意图规则。
- 非播放期重复同一问题、播放期真正的新问题、纯控制词和 enrollment 优先级保持不变。
- endpointing 默认值集中校验；生成脚本、开发示例、生产模板和发布手册统一为
  `0.90 / 1.50 / 1.70`。`verify_env` 拒绝非有限值、`min > max` 和
  `false timeout < min`，发布手册另以精确 grep 锁定本版本目标值。

## 边界

- LiveKit `TurnDetector v1-mini` 负责判断“用户何时说完”，不是
  `stop / resume / chat` 意图分类器。
- 本次没有增加新的停词列表，也没有把停止动作改成 LLM 路由。停止仍走低延迟、确定性、
  fail-closed 快车道；后续如需模型语义，只应辅助 ambiguous final。
- 两轮真实样本的 transcription delay 已达 `2.13s / 2.34s`，下调 endpoint max
  不能消除供应商转写延迟。真机仍需覆盖慢语速、1–2 秒句中停顿和长句，防止拆轮。
- H5 与小程序源码不变；生产 H5 继续固定 `20260723-192611`，小程序体验版继续
  固定 `0.8.49`。

## 本地门禁

- 打断、Router、目标说话人、生产接线与 env 生成定向：`188 passed`。
- Python 全量：`1248 passed, 27 skipped`。
- Ruff：通过。
- `mypy services --strict`：通过。
- `git diff --check`：通过。
- 三个历史大文件仍不满足全文件 `ruff format --check`；其 `HEAD` 版本已有相同格式差异，
  本次未运行写入型全文件格式化，避免扩大无关 diff。

## 固定工件与生产门禁

- SHA-256：
  - source archive：
    `640bf02dc93e889ab4420b4898be0d3203e5ffb8e4d5527f17ca8612e9754040`
  - images archive：
    `abf9eb12e06c9817951aeddde0e45f0724baa64b5ab4f9719c736019ff3a2f22`
  - H5 artifact（已构建但未切换）：
    `d9d0da29e9a7ab489db01e1486bbcafe66222f9bc0144a23da084a08ac2aaecb`
  - release manifest：
    `728c2745e5be359b1f70df64b7d22ce55428513dc237dd94c1e4ae936945de74`
  - release verifier：
    `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101`
- 服务器 Linux/amd64 镜像 ID：
  - Agent：
    `sha256:6c454baa169fa9db98cc4c5c58c5bc33afc803abdb6afe20ca05b2fc0a75584a`
  - Control API：
    `sha256:0591c674d89674d3dd810c0a65643b3a8852a1b199d54575f51637ecf557bb25`
  - Speaker Model：
    `sha256:18dfad7c6ffd49a199c43b0c5771558591c1e5c6f5ba14ad7a9bd246df2a9d60`
  - Mini Program Gateway：
    `sha256:64319f6dc62d970d82459a675eac3a585547497c381e43e401fec5c85eada98d`
- SQLite 快照 SHA-256：
  `12ce08cedd46fd07595492f354c77f11a65dca31cb36fc7fc74d851bcf604edc`；
  PostgreSQL custom dump SHA-256：
  `aedcf4d9cef0668e841b04ad991ecbbf5e920cdb065d4a622f81d4ebe8ea051d`。
- root-only 发布证据：
  `/var/backups/memoria/runtime-switch-20260726-233337-from-20260726-215154-20260726-235734/`。
  `rollback.txt` 与三份 `*.failed.log` 保留第一次自动回滚，
  `activated-at.txt` 与 `state.after.txt` 保留第二次成功激活；目录中的完整 env/inspect
  快照按敏感凭据处理，不得复制到普通文档或聊天。
- 当前正式 IP 入口的 root、兼容 H5、任意 SPA、API live/ready 和 WMS 均为 200；
  internal、PocketSparks 与 Goods Invoice 原路径均为 404。H5 provenance 精确为
  `20260723-192611`，Nginx、WMS、readiness timer 和 certbot timer 均正常。
- 切换窗口存在预期的启动期 health retry，随后全部恢复；稳定复核时四容器 healthy、
  restart 0，启动后结构化 ERROR、CRITICAL、Traceback 和 WARNING 计数均为 0。
- 旧域名 `aginice.cn` 仍指向旧服务器并返回 502，不是当前发布入口；验收使用
  `122.51.108.140:8443` 或 `aigcnice.com:8443`。

## 发布与回滚

- 当前 runtime 为 `20260726-233337`；直接回滚点为 `20260726-215154`。
- 回滚必须同时恢复 `/opt/memoria/current` 和证据目录中的四份旧 env；旧 Agent timing
  为 `1.50 / 2.20 / 1.70`。恢复后重建四个 runtime 容器，并要求 readiness 精确返回
  `ready / 20260726-215154`。
- H5 固定 `20260723-192611`，无需随 runtime 回滚；PostgreSQL、MinIO、LiveKit、
  SQLite 用户数据和小程序版本均不自动回滚。
- 第一次自动回滚已经真实验证上述路径。第二次成功切换后的回滚条件仍包括：
  基础门禁失败、真实手机明显拆轮、late transcript 增长、重复 generation、
  确认音缺失或新问题无法正常提交。
- 回滚前必须先冻结目标会话的 Agent/Gateway/Control 脱敏日志。

## 真机验收

1. AI 回答中说“停一下”，确认立即停播并只说一次“嗯，你说。”。
2. 不再重复回答上一问题；随后提出真正新问题可正常回答。
3. 正常短句、长句、慢语速和 1–2 秒句中停顿不被错误拆轮。
4. 同一会话核对 `interrupt_replay`、单次 ack、单次 generation bump 和 endpointing
   实际值；回滚前先冻结 Agent/Gateway/Control 脱敏日志。
