# Release 20260717-154800

## 目标

修复声纹登记竞态（登记话被当成对话 → 多一句「跳过声纹」）、第二声轨叠音、音量忽大忽小。

## 变更摘要

- `PENDING` 登记期间拒绝 `accept_user_turn`（`speaker_enrolling`）。
- 登记失败欢迎语不再说「跳过声纹」，改为普通邀请开聊。
- 默认关闭 listener cues（避免 BackgroundAudioPlayer 第二音轨）。
- 打断 duck 由 0.25 → 0.55；CosyVoice 软峰值保护。

## 生产

- readiness：`20260717-154800 / qwen` PASS。
- `LISTENER_CUES_ENABLED=false`，`COSYVOICE_VOLUME=70`。
- Runtime/H5 → `20260717-154800`。

## 回滚

- 上一版：`20260717-153500`。
