# Memoria Runtime Release 20260817-135129

## 发布目标

- 收敛 Agent/Voice Core 的 generation-bound 委派输出所有权，避免委派失败、超时、取消、空结果或迟到 generation 与普通回复重复输出。
- 补齐 Speaker Authority 稳定策略拒绝的主体能力矩阵，并保持未知 403、5xx 与网络异常按 transport failure fail closed。
- 为 Runtime Profile 拒绝原因提供固定低基数指标；补齐 Media Turn fence、重试、空音频帧和协议合同回归。

## 发布范围

- 生产候选 runtime：Agent、Control API、Speaker Model、Mini Program Gateway、Device Media Gateway 的固定 amd64 工件；本次实际切流只重建 Agent 与 Voice Core Media Bridge，Control API、Media Edge 及其余服务保留原运行代际。
- H5、小程序源码、ESP32 固件和设备身份区不切换、不刷写、不触发设备语音或电脑外放。
- `jsonschema` 仅加入可复现 dev extra；因 `pyproject.toml`/`uv.lock` 变化，按规程执行本机固定依赖完整构建。

## 发布前门禁

- Agent 全量 `1558`；Control API 全量 `586`。
- Speaker/Router/Runtime Profile 聚焦 `165`；Voice Core 聚焦 `126`；Control Speaker API `4`。
- 固件源码、Device v2 合同、架构状态与验收器 `251`。
- `uv run mypy services --strict`：399 source files；Ruff、Go 全量、模块预算、`git diff --check` 通过。
- `uv sync --frozen --extra dev` 通过；`--all-extras` 的既有 macOS ARM `vosk==0.3.45` 可用性限制不属于本 release 回归。

## 验收边界

本地门禁只证明 `code / wired / local verified`。本 release 不改变
`direct_real_device_verified=false`、`full_duplex_verified=false`，不宣称 Exact DAC、AEC Reference、
双讲、精确 Actual Heard、100/500 轮或 T14 微信独立性通过。部署后仍需以服务器 readiness、容器健康、
业务 API 和错误/指标复核记录 `enabled` 与 `production_runtime_verified`；真实设备和声学验收保持独立。

## 回滚

- 部署前冻结 `/opt/memoria/current` 的实时旧 runtime、旧 env、数据快照和 root-only rollback receipt。
- 直接 runtime 回滚目标以部署前实时读取的 release tag 为准；H5 保持旧软链，本 release 不切 H5。
- 任何 readiness、Provider、容器健康或错误日志门禁失败，按 `docs/production-deployment.md` 的完整 runtime 回滚流程恢复旧 runtime/env；不自动回滚数据库或对象数据。

## 提交、推送与生产切流

- 代码提交：`91f179069055720795fadafc1839045c1cc61f31`（`fix(voice): close delegated output and authority fences`）。发布 tag `20260817-135129` 已指向该提交，后续文档记录使用 docs-only 提交，不移动发布 tag。
- 已推送：本地 `main` 与 `origin/main` 在代码切流前均指向上述提交。
- 发布工件已完成服务器校验：source tar `a207f0c7f3e820d38e03934aae98ca0e78446415335f149bb2d7bf9d078ca025`；images tar `29c60d63d1d18a80abf76f366cbeedf4f613aa2b98d1d9012a2d82ed8bed0130`；H5 tar `df8a270e21ff3f5d712e3313ca9ed47289c7d4fe771f6d3545b8ec48bbaa0959`；manifest `45130d0983466560c446b2143fdc1ec71dfde4e224fb3699591aee1bd9a22e21`。
- 第一次切流于 `2026-08-17T06:20:54Z` 触发自动回滚：Agent/Bridge、具名 gRPC Health、Edge redial 和 Provider smoke 已通过，但公网 readiness 仍报告既有 `smokes:not_run`；回滚路径实际恢复旧运行态。完成 readiness mark、LiveKit、Provider 与 `verify_env` 后，第二次切流自 `2026-08-17T06:25:41Z` 成功，Bridge 于 `2026-08-17T06:26:12Z` 完成 Edge 自动重拨。

## 当前生产结果（2026-08-17T06:34:16Z 延迟复核）

- Agent：`memoria-agent:20260817-135129`，image ID `sha256:163c4d534fa06a4b796a5217fcaead32ea97162a108bef1c4c6237f94238cb4e`，OCI revision 与代码提交一致；容器 healthy、restart count 0。
- Voice Core Media Bridge：同一候选 image/image ID，容器 healthy、restart count 0；具名 `memoria.media.v1.VoiceMediaBridge=SERVING`。
- Media Edge：继续运行 `memoria-media-edge:20260816-bridge-liveness-83af813`，未重建、未重启；mTLS `/readyz` 为 `200 ready`。
- Edge 指标：redial attempts/successes `3/3`，liveness healthy `1`，channel generation `4`，active device connections/media sessions `0/0`。自 `2026-08-17T06:26:12Z` 起 Agent、Bridge、Edge 目标错误计数均为 0（Traceback、stale sequence、ASR tail timeout、retry exhausted、output timeout、speaker/authority 403、closed client、ERROR）。
- 公网域名与 IP 的根页、H5、SPA、API live/ready 和 WMS 均返回 200；内部保护路由按预期返回 404。域名证书有效至 `2026-10-18`，IP 证书有效至 `2026-08-21`，certbot renewal timer 当前 enabled/active。
- 当前运行软链仍为 runtime `20260812-173008` 与 H5 `20260808-171749`；回滚快照、Agent/Bridge rollback 镜像和证据目录均保留。服务器证据：`/opt/memoria/direct-canaries/20260817-135129/CUTOVER_RESULT.txt`。

## 验收结论

- 本次层级：`code=true`、`wired=true`、`enabled=true`（仅 Agent/Bridge 候选）、`production_runtime_verified=true`（零活跃设备/媒体会话范围）。
- 明确未通过/未证明：`direct_real_device_verified=false`、`full_duplex_verified=false`；T1–T14 仍为 `0 pass / 14 blocked / 0 failed`。Exact DAC、AEC Reference、Double-talk、Actual Heard、100/500 轮和 T14 微信独立性不因本次服务器验收改变。
