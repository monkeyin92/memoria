# Doubao 字幕比例对齐门禁 Release 20260806-213522

## 根因与修复

- 候选 `20260806-211652` 的真实 Doubao provider smoke 在部分批准设计音色得到稳定的
  340–514 ms 原始字幕/PCM 末尾漂移；最高相对误差约 16.2%。原先仅以 300 ms 绝对阈值
  判断，错误地把可线性校准的长语音标记为 `degraded`。
- 共享的 `align_subtitle_words` 现在同时检查绝对和相对误差：原样 `ok` 要求 `<=120 ms`
  且 `<=20%`；缩放 `scaled` 要求 `<=700 ms` 且 `<=20%`；其余仍为 `degraded`，不会生成
  精确 actual-heard 字幕。

## 回归范围

- 协议单测覆盖长语音 500 ms / 14.3% 漂移可缩放、短语音 500 ms / 33.3% 漂移拒绝，以及
  超过 700 ms 的绝对误差拒绝。
- Doubao mock 改为一秒 PCM 的真实比例场景，端到端覆盖 `scaled` 与 `degraded` 回调和
  precise transcript 发布边界。
- 不启用 StreamCore、coturn 或 media-runtime profile；生产仍保持
  `python_authoritative + LiveKit`，只有所有真实 provider/readiness 通过后才切 H5。

## 部署结果

- source commit / annotated tag：
  `7b794c9e38d3d89b5d76db22df6b71f3482b8f5f / 20260806-213522`，分支和 tag 都已推送。
- 本地和服务器均完成 commit-bound 验签。固定工件 SHA-256：source
  `f2ca3620d2c613af4ddd4e3521cf72f91d007c51ae1c1b2091d7ff463714c300`，images
  `504a0c0fd67f16ebf74164c25278edd409523ab87d7f369333d5c4a2b6c568f8`，H5
  `6556fa54039a78bf172536626b39b1bc681c33d4f8a86623037d7519ec17cae0`，manifest
  `f5287da4dc0d2cc1e10b99e32c2edc2a70ec730ccf15f643e99b971457871079`。
- 四个 `linux/amd64` 镜像从已验签的 `20260806-211652` 候选镜像增量构建；依赖输入没有
  差异，所有新镜像均重新绑定本 commit/tag。上传以该候选的只读 image archive 为 rsync
  basis，只传输 40.68 MB 新数据；服务器隔离 H5/SPA/API/SQLite restart smoke 通过。
- 首次 runtime 激活在安装 Gateway 候选 env 前发现操作映射的文件名错误，自动恢复旧 env 和
  `20260802-142257`，且尚未移动 runtime/H5 软链。修正映射后重新激活成功；这次 pre-link
  失败没有影响用户数据或公网 H5。
- runtime 与 H5 已原子切换至 `20260806-213522`。四个核心容器 healthy/restart 0；控制面
  `/health/ready` 绑定本 tag，9/9 core、Agent heartbeat 与 LiveKit 均 ready。四个容器镜像标签
  和 OCI revision 都匹配本 commit。
- required provider/readiness 全部通过：LiveKit、Qwen forced public search、DeepSeek、
  FunASR、Doubao 24 kHz mono PCM/时间戳/取消与五个设计音色、Interrupt Semantic 五类样本。
  五音色结果均为允许的 `scaled`。生产 Open-Meteo 南京天气 canary 已返回实时天气文本，模型
  为 `open-meteo+qwen-plus`，不会回落到静态“不知道”。
- Nginx 语法通过。H5 以追加式 immutable union 激活，候选包含 228 个当前和历史资源并逐一
  通过本机 HTTPS 资产探针；公网 IP `8443` 的根 H5、兼容 H5、SPA、live、ready、WMS 均为
  200，internal、PocketSparks 和 Goods Invoice 均为 404。in-app browser 能打开注册首屏，
  没有 console warning/error。
- 直接 runtime/H5 回滚点均为 `20260802-142257`。SQLite 双副本 SHA-256 为
  `b5fdecf031d13853af6fbbdae882a50bee1eab0d569c8ecb29905137a12ba82f`，root-only 数据和
  env 回滚目录是 `/var/backups/memoria/runtime-switch-20260806-213522/`。
- `aigcnice.com:8443` 的 SNI ready 检查通过。域名 `443` 从外部网络仍在 TLS ClientHello
  后被重置，但服务器 loopback 的 `https://aigcnice.com/wms/` 正常；本轮没有修改该既有
  443 网络路径或 Nginx 配置。StreamCore、coturn、media-runtime profile、物理设备 playout ACK
  与硬件 AEC 仍未启用或验收，`go_authoritative` 保持关闭。
