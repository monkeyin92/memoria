# 语音话轮归并与打断止血 20260730-224522

## 发布目标

- 修复一个 LiveKit 逻辑话轮包含多个 VAD epoch 时，旧识别结果在后续两三轮才出现的结构性风险。
- 降低 H5 短插话门槛，候选插话开始时立即静音助手，并恢复不结束会话的“停止回答”兜底按钮。
- 补齐麦克风上行、FunASR 数值时序和话轮归并诊断；遥测不采集识别文本、设备 ID、标签或原始音频。
- 把话轮归并和 H5 音频遥测从既有大文件中拆成独立模块，保持 UtteranceRouter、GenerationFence
  和主人隐私权限边界不变。

## 主要改动

- 新增 `SpeechEpochAssembler`：以 ASR final segment 关联 committed turn，支持一个逻辑话轮
  合并多个 VAD epoch；迟到、陈旧或不相关 segment 进入隔离路径，不能串入下一轮。
- `DuplexRuntime` 只负责接线和 fence，话轮归并与音频 Trace 校验分别下沉到独立模块。
- FunASR final 增加 `task_epoch / sentence_id / begin_ms / end_ms / duration_ms` 数值元数据，
  供服务端对齐和诊断使用。
- H5 新增 `LiveKitAudioTelemetry`，统一采集浏览器实际麦克风设置、上行 RTP 和助手下行质量；
  `useVoiceSession` 不再直接承担 WebRTC stats 轮询。
- H5 默认 `INTERRUPTION_MIN_DURATION_S` 从 `0.45` 调整为 `0.35`；检测到候选插话时先将助手
  音量降为 0，误触发仍由既有 false-interruption 恢复路径处理。

## 本地门禁

- Python 全量测试通过；Ruff 与 strict mypy 通过。
- H5 `23` 个测试文件、`251` 项测试通过；production build 通过。
- JavaScript syntax、`git diff --check` 与浏览器窄屏布局检查通过；浏览器 console
  warning/error 为空。
- 本轮本地验证没有请求真实麦克风权限。儿童语音识别率、短命令召回、外放/AEC 听感和弱网表现
  仍必须使用同一套真机语料做 A/B，不能由单元测试或桌面浏览器替代。

## 发布与回滚边界

- 发布前生产基线为 runtime `20260730-204546`、H5 `20260730-202610`，四个服务 healthy，
  9/9 core ready。
- 本次最初只计划切换 runtime 与 H5；随后经用户明确要求，补充上传小程序 `0.8.64` 体验版。
  小程序提审和正式发布仍不在本次范围。
- 直接回滚目标固定为 runtime `20260730-204546`、H5 `20260730-202610`。切流前必须另外生成
  SQLite、PostgreSQL 和四份生产 env 的 root-only 备份，并在本节补录实际路径与摘要。

## 部署结果

- source commit / annotated tag：
  `c719bd667bdba2a6fdd73a5b68844f7d9f5ea8bc / 20260730-224522`，均已推送远端。
  source/images/H5/manifest/verifier SHA-256 分别为
  `35bd93be6e3ad82ba2fe9a309af025aff942f972ecdf387d0c9522057c68db77`、
  `4c0f197d5e5373d0d2dfb0fc23ad8fbba79aa19b8984a5bcf45bce91edaa81c8`、
  `4d7baa5d5b99f2f8aae60d762cb0c21ae3deb9403c2a127da19191f21ab78dcb`、
  `67542e057b53f3277e76b0f8ba2f7b84a34d0074815717e84e919a8c25676e84`、
  `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101`。
- 候选通过 manifest/imported-image provenance、H5 immutable collision/append-only union 和
  隔离 server smoke。第一次 runtime 切换的 Interrupt Semantic 请求瞬时超时，门禁 fail closed，
  H5 未切换且自动恢复 `20260730-204546`；旧版 readiness 和全部 Provider 重新通过后，使用同一
  工件重试成功，没有放宽超时或跳过门禁。
- runtime 于 `2026-07-30T14:56:32Z` 原子切换，H5 于
  `2026-07-30T14:58:10Z` 原子切换。真实 LiveKit、FunASR、Qwen、豆包五音色和
  Interrupt Semantic 全部通过；9/9 core ready，四容器 healthy/restart=0，严重错误日志为 0。
- 小程序 `0.8.64` 随后从干净提交
  `782fac90db71901e1c6c4a3ae62da4b7c6bd751e` 通过已登录微信开发者工具上传体验版；任务
  `confirmation_upload_8e16a0d0-a9ac-4f26-af32-09e63d6b6192` 返回
  `success / execution_success`，包体 `655,523` 字节。上传前 85/85 测试与 JavaScript
  syntax 均通过；未提审、未正式发布，真机声学验收仍开放。
- 公网 H5、SPA、API live/ready 和 8443 WMS 均为 200，三个隔离旧路径为 404；H5
  `memoria-release.json` 精确绑定本次 commit/tag，候选和当前 `index.html` SHA-256 均为
  `b1861fe54328598493689e54e3f2f112b97cc90dc776bb5562a82bb5e97d6549`。1,820 次历史
  immutable asset 回查通过，媒体网关无效 ticket 按协议关闭为 `4401`。
- IP 与域名证书 SAN、Nginx 配置和服务器 loopback WMS 443 路由通过；发布终端到公网
  `aigcnice.com:443` 的 curl 仍出现既有 TLS `SSL_ERROR_SYSCALL`，未影响本次 8443 Memoria
  链路，不能写成公网 WMS 443 已从该终端验收。
- 直接回滚点为 runtime `20260730-204546`、H5 `20260730-202610`。root-only 备份位于
  `/var/backups/memoria/runtime-switch-20260730-224522-from-20260730-204546-20260730T145214Z/`；
  SQLite / PostgreSQL dump SHA-256 为
  `62fb5b2a9fe2c16c5ee872a8d910f8f8426406b405cac05e1258bdce11e98774` /
  `29258af9753529c22ab74b99c644eac6b5e56547e21ff98de19081c3ba962027`。
- 儿童语音识别率、含 500–800 ms 停顿的真机跨段话轮、短命令召回和外放/AEC 听感仍是
  iPhone/Android 真机开放验收项；本次发布只证明代码回归、Provider 和在线服务链路通过。
