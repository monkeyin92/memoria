# Memoria Release 20260715-201018

- 状态：STAGED，尚未激活公网路由
- 目标服务器：`110.42.235.198`（不记录登录凭据）
- 目标域名：`https://aginice.cn`
- 上一个 Memoria release：无（首次发布）

## 工件

| 工件 | 版本或 SHA256 |
|---|---|
| Control API image | `sha256:31a070a3ef6328067992777c11a52d16bb79961b52956c12fdc93caf310badf2` |
| Agent image | `sha256:3c9fcfab8c18d716c353b94e1601384500e8d23e79fecd2ca32af1cb46325631` |
| H5 `index.html` | `f39a6e7f3186ca1d35f9ea5debe56fe0c5ca41d0af540a8461ca574f50fe75d8` |
| H5 main JS | `80a8c9e89042c7691fe6456888a10e947c5836bc77fd9d6153f7304c5c2c052e` |
| H5 main CSS | `c67db5c02057bd4e857b9ab76a793c821dd780a010dc0203c2b3701cba1f1553` |
| Control API Dockerfile | `b0745aed6740b7483384eec54c3703b2d15acbcc6f6ab62e9411ac07aedcc119` |
| Agent Dockerfile | `193b67e0a147947c3e3abb2340ca38bef7bdb0fa8d683c93a0aeedb8f9cfd659` |

## 服务器落点

- 发布目录：`/opt/memoria/releases/20260715-201018`
- H5 版本目录：`/var/www/memoria-releases/20260715-201018`
- H5 当前链接：`/var/www/memoria-h5`
- SQLite 目录：`/var/lib/memoria`，UID/GID `65532:65532`
- Control API 预留端口：`127.0.0.1:8791`

## 已验证

- 两个镜像均为 Linux AMD64。
- 容器内 Python 源码可导入，Agent 缺生产配置时 fail-fast。
- `verify_env.py` 与 `provider_smoke_test.py` 已进入 Agent 镜像。
- 本地 Ruff、mypy、181 tests 全通过。
- H5 9/9 tests 与 production build 通过；本地与服务器 H5 校验和一致。
- Nginx snippets 和限流文件已安装但未 include；当前 `nginx -t` 通过。
- 真实站点 include 图的临时替换验证通过，配置已恢复且未 reload。
- 回环部署 smoke 通过 H5、SPA fallback、API proxy 和 SQLite 重启持久性，临时资源已清理。
- `/etc/memoria.env` 权限为 `root:root 0600`；LiveKit 三项 presence 与 Control API 生产校验通过，值未记录。
- LiveKit Cloud room-service 真实鉴权通过。
- DashScope 已安全注入；CosyVoice 真实 24 kHz PCM/字时间戳 smoke 与 FunASR 中间/最终/字时间戳 smoke 均通过。
- 已修正 CosyVoice 当前 `result-generated` 事件包络、FunASR `finish-task.payload.input` 与官方上下文结构；公共域名下省略会触发引擎 `428` 的可选语音指令。
- 未修改或重启其他线上项目。

## 激活前门禁

- [x] 安全创建 `/etc/memoria.env`，权限 `root:root 0600`，并注入用户提供的 LiveKit 配置。
- [x] 安全注入 DashScope 配置。
- [ ] 获授权后安全注入 DeepSeek 配置。
- [ ] `verify_env OK`。
- [ ] `provider_smoke_test PASS: FunASR, DeepSeek, CosyVoice`，不得为 `SKIP`。
- [ ] Compose 两服务 healthy。
- [ ] Nginx include、`nginx -t`、reload。
- [ ] H5、SPA fallback、API、LiveKit session、真实麦克风、停止回答、消息落库、DeepSeek 回顾和重启持久性验收。

发布与回滚命令见 `docs/production-deployment.md`。
