# Release 20260719-000731

## 目标

1. CosyVoice 主链切到 **v3.5-flash + 声音设计音色**（默认 `warm_companion`）。
2. H5 暂时下线两个端到端后端：**Omni Flash** / **Audio Flash**，只保留级联。
3. 用 **delta 镜像构建** 规避生产机 3.6GB 内存上冷构建卡死。

## 变更摘要

- `cosyvoice-v3.5-flash` + freeform Instruct；`infra/voices/designed_voice_ids.json` 5 个设计音色。
- 字级时间戳与 PCM 偏差大时仍线性拉齐（status 可为 `degraded`）。
- H5 去掉模型切换 UI，固定 `cascade`。
- Agent 镜像包含 designed voice registry。

## 部署方式

生产机 BuildKit 缓存几乎为空，完整 `compose build` 会重装整包 venv，极易卡住。  
本 release 使用 delta build：

```text
FROM memoria-agent:20260718-211231 + COPY app code
FROM memoria-control-api:20260718-211231 + COPY app code
```

约 46s 完成。脚本：`scripts/delta_build_images.sh`（依赖未变时使用）。

## 门禁

```text
livekit_smoke_test PASS
provider_smoke_test PASS: FunASR, Qwen, CosyVoice
  CosyVoice: cosyvoice-v3.5-flash / warmboy designed voice / freeform
readiness refresh PASS: 20260719-000731 (qwen)
两容器 healthy
公网 root/h5/live/ready 200；internal 404
```

## 回滚

- Runtime：`/opt/memoria/current` → `releases/20260718-211231`
- H5：`/var/www/memoria-h5` → `memoria-releases/20260718-214500`
- env 备份：`/var/backups/memoria/memoria.env-pre-20260719-000731`
- SQLite：`/var/backups/memoria/memoria-pre-20260719-000731.sqlite3`
