# 长短期记忆召回与历史清理 Release 20260807-113413

## 发布范围

- 同一 owner 会话保留最近 16 条权威话轮，并将更早的 owner 对话压缩为有界滚动摘录；访客、
  ambiguous 和不具备 authority 的内容不会进入 owner 上下文。
- 跨天召回增加相对日期和已确认人物过滤，并将当前话轮的受限 owner 上下文作为中文检索的
  前缀；二、三字中文词 fallback 只在已通过权限过滤的 owner material 上运行。
- “请/帮我记住……”中的低风险 owner 自我事实可自动确认；敏感内容、关系、冲突、缺少
  generation fence 或来源不确定的内容仍保持 candidate，不能直接进入已确认记忆。
- 公共只读实时天气查询不再被 owner-only tool policy 错误阻断；H5 回顾没有当天记录时仍保持
  今天选中，并使用“当天/当日”的历史日期文案。
- 新增 PostgreSQL memory projection rebuild 脚本，重放 immutable archive evidence，并保护
  self-model relationship profile 引用的稳定人物/关系 ID。

## 清理范围

- 删除无运行、构建或入站引用的 H5 历史视觉 QA 证据、旧首页原型和明确标注已替代的实施计划；
  同时移除相应的 Docker ignore 死路径。
- README、交接和生产文档不再保留过期 release tag、固定测试数量或固定证书日期；历史发布、
  ADR、恢复演练、`services/legacy`、CosyVoice/Omni 路径和 Mem0/TurboVec 评测实验保持保留。

## 发布前门禁

- Python：Ruff、strict mypy、pgvector 条件下的完整 coverage suite 通过：`1904 passed, 2 skipped`，
  总 coverage `87.82%`（门槛 85%）。
- Media contract：Buf `1.72.0` lint、Python protobuf 生成一致性、runtime smoke、replay/chaos/load
  与 offline E2E 均通过。
- H5：`301 passed` 与 production build 通过；小程序 `86 passed`，Go `gofmt`、`vet`、test 和
  race test 均通过。
- 固定中文记忆评测 `13/13`：`Recall@5/10=0.7692`、`nDCG@10=0.6727`、时间正确率 `1.0`，
  candidate、冲突和跨账户泄漏均为 `0`。本地 PostgreSQL 17/pgvector projection rebuild、恢复和
  self-model 外键保护合同通过。

## 生产维护与回滚

- 发布前 runtime/H5 健康基线为 `20260806-213522`，四个应用容器 healthy，`/health/ready` 为
  9/9；该 tag 是 runtime 与 H5 的直接回滚点。
- 生产 Archive 有既有 immutable evidence，`memory_claims` 和 `memory_search_documents` 需要在
  维护窗口重建。必须先停止写入或切只读，再以 root-only `memoria_admin` maintenance DSN 运行
  `scripts/rebuild_memory_projections.py --confirm-rebuild`；应用 `memoria_app` DSN 没有
  `BYPASSRLS`，不得替代。
- rebuild 后必须确认 `failed_events=0`、compile outbox 没有待处理项，并重跑中文记忆评测、
  权限/冲突泄漏、orphan 和 RLS 门禁。数据库是派生 projection 的重建，不与 runtime 自动回滚；
  若维护验收失败，保留证据并将 runtime/H5 切回 `20260806-213522`。

## 部署结果

- 待本 release 的 commit-bound artifacts、隔离 smoke、runtime/H5 切换、projection rebuild 和
  公网验收完成后补充。
