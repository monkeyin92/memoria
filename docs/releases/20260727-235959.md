# 小程序受控话轮与 H5 语意打断 Release 20260727-235959

## 发布状态

- source commit / annotated tag：
  `a8d953f3c9a333659400f84653f75d3325797fa3 / 20260727-235959`。
- 后端 runtime 已原子激活为 `20260727-235959`；生产 H5 继续固定
  `20260723-192611`，没有切换静态站点软链。
- 微信小程序 `0.8.53` 已上传体验版，包体 `609,479` 字节；
  未提交审核、未正式发布。

## 发布内容

- 小程序改为受控话轮：收到 `thinking / thinking_silent / tool_waiting / speaking /
  interruption_pending / recovering` 时暂停录音上行。
- Agent 回到 listening 后，客户端仍等待 pending PCM 和全部已排程 WebAudio source
  结束，才恢复录音；用户主动静音优先。
- 删除小程序“轻触打断”和本地 `interruptPlayback` 路径；播放期口头控制词不会停止
  当前回答或进入下一轮。
- Gateway 无论 AEC 是否可用都标记小程序平台；Agent 对该平台关闭 LiveKit
  interruption、KWS、歧义语意复核和播放期转写接纳。
- H5 默认保持 `barge_in_enabled=true`；Cascade `UtteranceRouter` 与 Omni 备用
  transport 均保留“等等、等一下、停一下、先别说”等控制语意回归。
- 架构决策见
  `docs/adr/0025-controlled-miniprogram-turns-and-h5-semantic-barge-in.md`；
  完成度见 `docs/voice-architecture-progress.md`。

## 固定工件

| 工件 | SHA-256 |
| --- | --- |
| source | `faeee7e8f5b47a6e09991068c82807c44b5e65320ff856948d65c77cd92ddd38` |
| images | `02e6fe781e35b014f6c4a4ba50a0e2d9a9ce06219a8803a8f569eea900715a4c` |
| H5 candidate | `ca45dcd7896f93941567cce8c41095347cf8eea2a084a21287832fcd57a6e8c3` |
| manifest | `f93db037f2340a8fefe2963433fac5256cd210915dd26e1005f67b965f82e9f7` |
| verifier | `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101` |

服务器导入后的 Linux/amd64 镜像：

| 角色 | 镜像 ID |
| --- | --- |
| Agent | `sha256:a1cc8bacb8aa4845e5977d70d730c8b79ad73e327e1f37084e264d504aa0c6f7` |
| Control API | `sha256:85e9f0bf3d3430d6216192454ae686d2bdfc630abf828fb32dc0d4e96b974bfb` |
| Speaker Model | `sha256:52770c15d18542fd2c72c7bdfd43e19d0f6d57428039b857c986462120681a28` |
| Mini Program Gateway | `sha256:f4d1847c49756e2aee5ab4ff1355ef1ced5f42965ed9826fdc4ad0260f8318a1` |

## 发布门禁

- Ruff：通过。
- strict mypy：160 个 source files 通过。
- Python：`1310 passed, 27 skipped`。
- H5：`19 files / 236 tests passed`，production build 通过。
- 微信小程序：`48/48`，全部 JavaScript syntax check 和 JSON 配置检查通过。
- `scripts/run_e2e.py --profile offline`：通过。
- commit-bound source、manifest、release verifier、四角色 Linux/amd64 镜像标签和
  imported image 校验通过。

## 生产验收

- `agent / control-api / speaker-model / miniprogram-gateway` 四容器均为
  `healthy`、restart 0。
- readiness 为 `ready / 20260727-235959`，9/9 core checks 与 Agent heartbeat 通过。
- LiveKit、FunASR、Qwen、Doubao 和 InterruptSemantic provider smoke 通过。
- Nginx 语法、WMS、readiness timer、certbot timer、公网根 H5、兼容 H5、SPA、
  API live/ready 和 WMS 路由通过；internal、PocketSparks、Goods Invoice 保持 404。
- 小程序公网 WSS 成功升级；无效 ticket 的 hello 按协议关闭为 `4401`。
- 四容器最近十五分钟未发现 traceback、关键 provider、provenance、
  archive durable/spool 或连接拒绝错误。

## 备份与回滚

- 直接回滚 runtime：`20260727-221555`。
- 固定 H5：`20260723-192611`。
- 回滚证据：
  `/var/backups/memoria/runtime-switch-20260727-235959-from-20260727-221555-20260727-235959/`。
- SQLite 快照 SHA-256：
  `b9a77c6df5d97e6234795b7a08a0a145c079378822d2aa4aca347b5e25ea1ec4`。
- PostgreSQL custom dump SHA-256：
  `29fc666cfd9ea75219939dd5f170bd5e6b0147be53fbf1e22e12c8a6be19338a`。
- runtime 回滚恢复四份最小权限 env 并切回 `20260727-221555`；
  不自动恢复 SQLite、PostgreSQL、MinIO 或 H5。

## 选择性清理

- 生产删除本轮 `2.2 GiB` incoming、未激活 H5 候选、全部过时临时候选 env、
  旧 runtime/source/image `20260727-204609` 和三套更老 runtime-switch 备份。
- 生产只保留 runtime/source/image `20260727-235959` 与直接回滚
  `20260727-221555`；H5 只保留 `20260723-192611`；服务器根分区约
  `28 GiB` 已用、`86 GiB` 可用。
- 本机删除临时 release worktree、约 `2.1 GiB` 发布工件、测试/构建缓存和旧
  `20260727-204609` 四角色镜像标签；Data 卷约 `288 GiB` 可用。
- 未运行全局 Docker prune；未删除数据库、Docker volumes、Vosk 模型、WMS、
  其他项目镜像、跨项目构建缓存或用户未跟踪素材。

## 未闭环

- 自动化、生产门禁和体验版上传不能替代 iOS/Android 真机受控话轮验收。
- 小程序仍需验证播放期上行 PCM 为零、本地尾音后恢复、不吞首字、手动静音优先、
  弱网、前后台和系统录音中断。
- H5 仍需真实浏览器/设备验证控制语意与非打断反例；当前生产 H5 代码版本没有切换。
