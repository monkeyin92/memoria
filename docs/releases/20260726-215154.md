# 小程序“等下”同义回声保护 Release 20260726-215154

## 发布结果

- source commit / annotated tag：
  `7edd751500a14290d24f6cb799a07e426877121c / 20260726-215154`。
- runtime 于 2026-07-26 22:21:13 CST 从 `20260726-133033` 原子切换到本版本；
  H5 继续保持 `20260723-192611`，小程序体验版继续保持 `0.8.49`。
- 四个 runtime 容器均为 healthy、restart 0；readiness 精确绑定本版本，Agent
  LiveKit 注册与 heartbeat ready，9/9 core checks、LiveKit、FunASR、Qwen 和 Doubao
  全部通过。
- 真实手机外放双讲仍是最终验收项，自动构建、隔离 smoke 和合成 WSS 不替代真机结果。

## 发布目标

- 保留 `20260726-181813` 对 FunASR 口语短写“等下。”的纯打断支持。
- 修复助手正在播放“等一下”时，回声被 ASR 缩成“等下”后绕过短文本回声门禁、导致
  助手自我打断的问题。
- 不修改 AEC 阈值、Router 优先级、TargetSpeakerFocus、H5 或小程序客户端。

## 根因与修复

- `等下` 是规范化后完全匹配的纯打断别名，但现有 `_looks_like_assistant_echo` 对少于
  4 字的文本直接跳过。
- Runtime 的精确子串检查也无法把“等下”关联到助手正文中的“等一下”，因此该回声可能
  进入可信无 VAD 控制通道；短命令的 TargetSpeakerFocus 允许 unconfirmed explicit control，
  最终可能错误停止当前播放。
- 共享回声门禁现在只增加一条同义映射：ASR 文本完全等于“等下”，且助手正文包含
  “等下”或“等一下”时返回 `assistant_echo`。该判断发生在可信控制准入之前。
- 助手未说上述词时，真实用户“等下 / 等下。”保持纯打断；“我等下再说 / 等下我想问……”
  继续是普通聊天。
- 安全取舍为 fail closed：助手正说“等一下”时，主人只说“等下”也会被保守视为回声，
  可改说“停一下”。这避免短文本在无法可靠完成声纹确认时自我打断。

## 验收边界

- 当前生产 runtime：`20260726-215154`；直接回滚点为 `20260726-133033`。
- H5 固定 `20260723-192611`；小程序体验版固定 `0.8.49`，本轮无客户端源码变化。
- 合成 WSS 夹具不能模拟手机扬声器的真实回声、时延与非线性失真，只用于区分
  gateway transcription、UI 门禁和 Agent trace，不作为真机通过门禁。
- 最终成功标准仍是真实手机外放双讲：AI 播放约 0.6 秒后说“等一下 / 等下”，实际立即停播，
  同一会话出现 `trusted_unanchored_interrupt_cmd`、`explicit_interrupt_cmd`、
  `assistant_state=interrupted` 与新的 `audio_reset`。

## 本地门禁

- 红测证明旧实现会把“助手说等一下 / ASR 等下。”放入可信打断；修复后 Guard 返回
  `assistant_echo`，Runtime 不创建 trusted control epoch。
- Python 全量 1263 collected，1236 passed、27 skipped。
- 打断相关定向 114 passed；Ruff、Agent strict mypy、`git diff --check` 通过。
- 小程序逻辑测试 32/32；H5 19 files / 232 tests 与 production build 通过。

## 固定工件与生产门禁

- SHA-256：
  - source archive：
    `9d750472423184ea200753371b5b3b2d8426d9d13da5af4e4aeb5e5ee20f42b5`
  - images archive：
    `3e9d28c2c4170e2c48867b7dff13ab6a6c0f31aa133adbee5376b5b60fc1682b`
  - H5 artifact：
    `455626cb6b0b13be013e4e1b0646164034d11f64de59c78ae9b04b9deb31fbfe`
  - release manifest：
    `c08b22b976fd7c726bd7180f27adac472d1e9158edf9022ffb40d5ea0506d1aa`
  - release verifier：
    `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101`
- 服务器导入的 Linux amd64 镜像 ID：
  - Agent：`sha256:37494d52a382cdf2db291c8c964c0a381a7103b11cad51cf7578d0954e2ce029`
  - Control API：`sha256:0d80313642b6c85520c9bea3ede49a53704c296f1645e41d3aa4459f74af8c1c`
  - Speaker Model：`sha256:a07668ef6cf3277caecd360883c241486289776641bcd00c17d4df17fead8af6`
  - Mini Program Gateway：
    `sha256:735e492b36b58ba06f999199290db005170f0cdb697a87a95fd1a10f54aac840`
- 服务器二次 verifier/manifest/三件套校验、183 个 immutable H5 资源无碰撞合并、
  `smoke_server_deployment.sh` 和唯一候选 Agent
  `duplex-zh-agent-candidate-20260726-215154` 的 LiveKit 注册/heartbeat 均通过。
- SQLite backup API 双快照通过 `integrity_check=ok / foreign_key_check=0`，SHA-256
  均为 `829cbe79b21d041964211a1c6173e0200e4f22cec95ce231f258b371f0764e6f`；
  PostgreSQL custom dump SHA-256 为
  `4c1307f38609375ec3d1044d36bc2118028df2ef7fc5d13603ea7be825afbcfe`，
  已用同版本容器 `pg_restore --list` 验证。
- 四份 root-only env、Nginx 和切换前后状态保存在：
  `/var/backups/memoria/runtime-switch-20260726-215154-from-20260726-133033-20260726-221931/`。
- 公网 root、兼容 H5、API live/ready 均为 200，内部 readiness 路径仍为 404；
  H5 元数据仍精确为 `20260723-192611`。服务器直连 WMS 三次均为 200，Nginx、WMS、
  readiness timer 和 certbot timer 均 active。
- 新容器启动窗口内，Traceback、ModuleNotFoundError、ImportError、provenance bind、
  archive spool、durable evidence、Connection refused 和 ERROR 标记均为 0。
