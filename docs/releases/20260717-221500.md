# Release 20260717-221500

## 目标

根据生产日志拦截「平板视频 + 短句等一下」混录进对话；解释 Omni 报错路径。

## 证据（上一轮 3ba87b4d）

入库用户句：`悟空。 嗨，欢迎来到我的世界。 寒丝洞。 嗯，等一下，等一下。`  
另有一次 pure far_field 拒收 `utt=0.616 rms=0.035 ref=0.166`。

## 变更

- **media_polluted**：远场静音/媒体时长 >> 近场主人语音时拒绝 turn_commit（视频台词 + 短句「等一下」）。
- 嵌入/统计按注册近场 RMS 分 near/far；`far_field` 比例 0.32→0.40。
- 放行/拒绝均打 `near_ms/far_ms` 与 `speaker_accept` 日志；`turn_committed` 带 text 前 80 字。
- H5 Omni：上游 `error` 事件上报 code（本地已改，本 tag 未强制切 H5）。

## 回滚

`20260717-220800`
