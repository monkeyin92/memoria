# 实时联网查询修复 Release 20260731-114616

## 目标与根因

- 修复用户询问“今天南京天气怎么样”后被回答“不能查询”的线上回归。生产会话已确认请求正常
  到达 Qwen、没有超时或打断；但共享对话模型是 `qwen-turbo`，百炼当前模型能力页明确标为
  不支持联网搜索，传入 `enable_search=true` 不会赋予该能力。
- 上一版完成态还没有把“暂时不清楚／不能查询／没办法直接查”等自我拒答识别为未完成，因而会
  清掉实时待办，使紧随其后的追问退化成普通闲聊。

## 实现与边界

- 普通对话继续使用 `qwen-turbo`；仅已识别的天气、新闻、行情等实时请求，使用独立
  `qwen-plus` OpenAI-compatible 调用，并设置 `enable_search=true`、
  `search_options.forced_search=true` 和 `search_strategy=turbo`。
- 独立调用只发送固定安全提示与当前 `PendingRealtimeRequest.query`，不发送历史、主人资料、
  记忆、工具、会话 ID 或其他私有上下文；结果仍经过既有 GenerationFence、语音预算与
  owner/public scope 门禁。
- Provider 超时、非 2xx、坏响应、空响应或自我拒答统一播报“我不知道。”并保留待办；相邻同一
  scope 的“人呢／查到了吗／你不能帮我查吗”可重试原公开问题，新话题、跨 scope 或迟到 generation
  仍丢弃待办。
- 仅计划切换 runtime；H5 与小程序源码、体验版和正式版均不改动。

## 发布前证据

- 精确回归先红后绿：覆盖“不能查询”“暂时不清楚”“没办法直接查”和“你不能帮我查吗”的连续
  对话；独立 resolver 单测锁定强制搜索 payload、仅当前 query 的隐私边界、失败降级和超时。
- `services/agent/tests/unit` 全量通过，Ruff、strict mypy 和 `git diff --check` 通过。
- 生产凭据的只读 provider canary 已用 `qwen-plus`、强制搜索和公开问题“今天南京天气怎么样？”
  返回 HTTP 200、约 5.63 秒、直接给出当天南京天气；未输出或记录密钥。

## 工件、部署与验收

- source commit / annotated tag：
  `679916fca3dd1f425ce8e0f57175b0daeef41ac3 / 20260731-114616`。source、images、候选 H5、
  manifest、verifier SHA-256 分别为
  `8270c10cd971861a23798c126d30b7934e0ca3fd1cf8b95d257548c6da00d915`、
  `dfc68eaefe4ea1be07e48a9aa769a770f3ec4257a383e4b387938e808e7c9e45`、
  `8e1a561c700ac693aa73d8219a7d7242cc9aaae7240a863383bda4173fd78eb4`、
  `2d54c100248692301c457427f2037cb5729a7de5ec753c86ebc4ea30b4d3dd8e`、
  `66f3425a249ce82e489e52f93d3b0135b3dafc02c7335fcf4c87e5a1ac9ec101`。
- 从 `20260731-102620` 的只读 `images.tar` basis 增量上传：总工件约 2.56 GiB，实际发送约
  30.52 MiB；服务器对 source/images/H5、manifest、verifier 和四个已导入镜像的 amd64、commit、
  tag、role 均复核通过。候选 H5 immutable union 和隔离 server smoke 通过。
- runtime 于 `2026-07-31T03:58Z` 原子切换；H5 保持 `20260730-233824`，小程序未上传、未提审、
  未正式发布。四个 runtime 容器均为 `healthy / restart=0`，Control `/health/ready` 的 9/9 core、
  Agent heartbeat、LiveKit、FunASR、Qwen 和豆包均为 ready。
- 新 Agent 容器内实际运行 `QwenRealtimeSearch`，以公开 query “今天南京天气怎么样？”调用
  `qwen-plus + forced_search` 成功返回可用结果（373 字符）；Nginx 语法和公网 root、H5、SPA、
  API live/ready 均为 HTTP 200。运行窗口未见 Agent 的 `Traceback`、`ModuleNotFoundError` 或
  `ImportError`。

## 备份、回滚与开放验收

- 直接 runtime 回滚点为 `20260731-102620`，H5 回滚点为 `20260730-233824`。最终 root-only
  备份目录：
  `/var/backups/memoria/runtime-switch-20260731-114616-from-20260731-102620-20260731T035816Z/`。
  SQLite 一致快照 SHA-256 为
  `1f53fd61fbdecb39803675014b53297cb426c5de51ee145062ed791b618776b5`；`memoria` PostgreSQL
  custom dump SHA-256 为
  `a08df5db63ed513f69ce19da1a007659ef2970db3ee216dd8b394852a69e78cc`，并经同容器
  `pg_restore -l` 验证（420 条目）。四份 env、前后指针和 SHA256SUMS 均为 root-only。
- 首次备份尝试发现数据库管理角色实际为 `memoria_admin` 而不是旧脚本假定的 `postgres`，在任何
  runtime 指针变更前停止；改用已验证角色重新生成 custom dump 后才切换。该部署脚本假设需另行
  收敛，未在本次 runtime 行为修复中扩展修改。
- PostgreSQL WAL archive 既有权限问题仍不构成 PITR 保护；本轮依赖上述逻辑 dump 和 SQLite
  一致快照。真实用户 H5/小程序语音链路及连续“你不能帮我查吗”体验仍待产品负责人复测，不能由
  Provider canary 代替。
