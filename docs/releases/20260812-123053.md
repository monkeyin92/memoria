# 20260812-123053 Release

## 状态与范围

- 基于生产健康版本 `20260812-114447`，修复 ESP32 Path 2 欢迎语前首个 Agent UI 事件导致的
  WSS 4400。新版本的安全诊断日志已在真实生产仿真会话中把原因定位为
  `component=sender reason=bridge UI event is not allowlisted`：首个事件是服务端 `audio_trace`
  遥测，不属于硬件屏幕或播放合同，却被旧网关当作未知协议事件终止了整个媒体会话。
- 设备网关现在以 Agent 权威 `UI_EVENT_TYPES` 为单一已知事件集合：
  `assistant_state/expression/subtitle/transcript` 映射到硬件协议；其余已登记的遥测、输入策略、声纹、
  listener cue 和话轮事件由网关安全消费，不下发设备。未登记的新事件仍 fail closed，不会把任意
  Agent 数据转发到硬件，也不会用开放式 catch-all 隐藏协议漂移。
- 被消费的桥接事件仍调用 `outbound_sent`，保持共享桥接的发送完成语义；之后的 reset barrier、
  generation `N → N+1` 映射和音频帧顺序不变。
- 本次不含数据库 DDL、数据修订、固件、env 或 H5 变更。回滚目标为 `20260812-114447`。

## 发布门禁

- Mini Program Bridge + Device Media Gateway 共 `79` 项测试通过；Device Media Gateway 独立
  `19` 项通过。
- WebSocket 回归在首代次 reset 前注入真实 `audio_trace`，证明网关先安全消费遥测，再发送设备
  generation `1` 的 `playback.flush` 和 Opus 音频；未知 UI 事件仍按 4400 fail closed。
- Ruff、相关源码严格 mypy 与 `git diff --check` 通过。
- 切流后必须重新运行设备签名的公网仿真客户端，证明 challenge、session、WSS、首代次 flush、
  下行 Opus 和 `playback.ended`；最后仍需真实板卡麦克风、扬声器和用户听感验收。
