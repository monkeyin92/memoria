# 实时天气、长对话打断与话轮路由 Release 20260807-163916

## 发布范围

- Open-Meteo 天气回答改为短句；区分当前天气、今天、明天和后天，并按目标日期请求
  `daily` 预报，不再把明天的问题落到今天的数据。
- 实时查询不再被静态“我不知道/我不清楚/我不确定”兜底文本提前截断；天气等可查询问题
  仍会进入后台检索并返回结果。
- 播放期 ASR 的 interim/final 判定重新绑定 canonical 文本和当前 speech epoch；低信息回声
  片段（例如单字或混合解码尾巴）进入 PlaybackInputGuard 隔离，合法短问句和大写产品名不被
  误拦，真实长话轮不消耗 feedback circuit 配额。
- `够了`、`好了`、`行了`、`可以了`、`不用了` 及带“知道了”的结束语统一走
  `UtteranceRouter` 控制路径，并同步 Cascade/Omni 词表。

## 根因证据

- 生产会话 `431dca76-53ac-452f-b390-ab295abb29aa` 中，“明天上海的天气”两次命中今天的
  forecast；旧天气 formatter 同时输出体感、风速和日高低温，造成回答僵硬且过长。
- 同一会话出现 `canonical_user_turn_resolved raw_len=37 canonical_len=0 discarded_epochs=1`，
  以及 `其。`、`对谢ght.` 等播放回声/迟到 ASR 尾巴；interim 的“继续播放”决策还可能被不同
  canonical final 复用。生产默认 `FUNASR_CONTEXT_ENABLED=false`，因此主要问题是播放期门禁和
  generation 绑定，而不是无限增长的 FunASR 上下文。

## 发布前门禁

- `uv run pytest -q`
- H5 `npm test`（26 个测试文件、304 项）
- H5 `npm run build`
- Ruff check/format、strict mypy、`git diff --check`
- 实际 Open-Meteo 网络查询成功，示例结果为上海明天天气预报并包含目标日期数据。

## 部署结果

- Source commit / annotated tag：
  `79cd479110f4778a093901efb0964242720f390a / 20260807-163916`；GitHub Actions
  [`31163079588`](https://github.com/monkeyin92/memoria/actions/runs/31163079588) 的 Python、H5、
  Mini Program、Go/media-edge 与 Trivy job 均通过。
- Commit-bound 工件 SHA-256：source
  `acdcd556dbbbcdc08f135f81a3591baa39be866500eef13b39a2008ad91094e1`，images
  `7be2b0a5b09daaed9d6b2307cf959d120b286f7dfae56c5fad88ee96a9eea3fc`，H5
  `522464fee2186cecd838f9b2564111ea61998539bf332928a4c5eac95a024205`，verifier
  `e949258605f7b2f8ce216dabf4cd06cbead1f7ab22a1c1defbb4c19bb4a96314`，manifest
  `d6f52601303ad96483a3035680a338408c2fca9cba443e5386976a8a519e3da6`。本地与服务器 verifier、
  镜像导入和隔离 server smoke 均通过。
- runtime/H5 已原子切换到 `20260807-163916`。四个应用容器 healthy；`/health/ready` 绑定该
  tag，9/9 core checks、Agent heartbeat、LiveKit、FunASR、DeepSeek 和 Doubao 均 ready。
  Provider smoke 实际通过 Qwen 公网检索、Doubao PCM/词级时间戳、FunASR 和五类
  InterruptSemantic 样本。
- 生产 Agent 容器实际查询“明天上海的天气怎么样？”返回“上海明天雷雨，27.9到34.1度，
  降水概率94%。”；部署镜像中的 router/guard 断言确认结束控制词、低信息回声隔离和 canonical
  final 重判路径均有效。
- SQLite 发布前双份 root-only 备份
  `/var/backups/memoria/memoria-pre-20260807-163916.sqlite3` 与
  `/var/lib/memoria/memoria-pre-20260807-163916.sqlite3` 的 SHA-256 均为
  `5760056e1149c1d44181dbc076f46f7c15da40bd06306ca25e807bb00db50ccf`；完整性和外键检查均
  通过。四份旧 env 也已 root-only 备份。
- H5 immutable asset union 含 240 个资源，并逐项验证 4536 个历史 asset URL 均能从新软链
  返回。公网 `8443` 根 H5、兼容 H5、SPA、live、ready 与 WMS 路由均为 200；内部及废弃
  路由为 404。服务器本地 `aigcnice.com:443/wms/` 为 200，WMS 保持 `inactive / enabled`。
- 浏览器验收确认新 H5 注册页正常加载、无 console error、1280px viewport 无横向溢出。未使用
  生产账号，也未申请麦克风权限，因此真实手机声学、完整登录后 LiveKit 对话和播放期实机打断
  仍需单独验收，不能由本次容器/浏览器 smoke 代替。
- 直接 runtime/H5 回滚目标为 `20260807-144153`；不自动回滚 SQLite、PostgreSQL 或 MinIO。
