<view class="page-shell">
  <view class="back-row"><navigator class="back-link" url="/pages/profile/index" open-type="switchTab">‹ 我的</navigator></view>
  <view class="page-head">
    <text class="page-title">隐私授权</text>
    <text class="page-subtitle">每一项，都可以随时收回</text>
  </view>
  <text wx:if="{{loading}}" class="loading-text">正在读取授权状态…</text>
  <text wx:if="{{error}}" class="error-text">{{error}}</text>

  <view class="l2-card">
    <view class="auth-card-head">
      <text class="l2-card-title">{{consent ? '原始语音归档已授权' : '原始语音归档'}}</text>
      <switch checked="{{consent ? true : false}}" color="#326e70" bindchange="onRawAudioSwitch" aria-label="原始语音归档" />
    </view>
    <text class="setting-desc">原始语音用于整理回顾，默认 7 天后自动删除。云端加密传输与存储，只有你能查看。</text>
    <text wx:if="{{consent}}" class="setting-desc">授权时间：{{consent.granted_at}}</text>
    <text wx:if="{{consent}}" class="setting-desc">策略：{{consent.retention_policy}}</text>
    <text wx:else class="setting-desc">机器人的对话音频是否归档需要单独明确同意。小程序只做授权管理，不采集或中转实时音频。</text>
    <view class="auth-card-foot">
      <button wx:if="{{!consent}}" class="primary-button btn-sm empty-button" loading="{{acting}}" bindtap="grant">明确授权原始语音归档</button>
      <button wx:else class="danger-link" bindtap="revoke">撤回并删除</button>
    </view>
  </view>

  <view class="l2-card">
    <view class="auth-card-head">
      <text class="l2-card-title">记忆档案</text>
      <switch checked="{{memoryOn}}" color="#326e70" bindchange="onMemorySwitch" aria-label="记忆档案" />
    </view>
    <text class="setting-desc">你确认过的「记住的事」会放进档案，陪伴才有连续性。档案加密存储在云端，仅家庭管理员可查看。</text>
    <text class="setting-desc">待确认草稿不会自动变成记忆；日常回顾由 AI 整理，可能有偏差。</text>
    <view class="auth-card-foot">
      <button class="danger-link" bindtap="revokeMemoryTip">撤回并删除</button>
    </view>
  </view>

  <view class="l2-card">
    <view class="auth-card-head">
      <text class="l2-card-title">声音样本</text>
      <switch checked="{{voiceOn}}" color="#326e70" bindchange="onVoiceSwitch" aria-label="声音样本" />
    </view>
    <text class="setting-desc">声音复刻需要本人朗读授权语句。样本仅用于生成声音，不作他用。</text>
    <text class="setting-desc">默认不采集麦克风。只有你主动录制自定义声音样本时才会申请麦克风权限，样本用于声音复刻，不用于小程序内对话。</text>
    <view class="auth-card-foot">
      <button class="danger-link" bindtap="revokeVoiceTip">撤回并删除</button>
    </view>
  </view>

  <text class="privacy-note">撤回应立即生效，可证明删除。&#10;删除范围与完成时间，会以书面回执告知。</text>
</view>
